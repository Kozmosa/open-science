# 2026-05-26 工作日志

## 15:30 — 更新仓库级 Git 工作流与卫生约束

根据已确认的 `master / develop / worktree` 开发范式，更新了 `AGENTS.md` 与 `PROJECT_BASIS.md` 中的长期约束，补充了提交粒度、正式分支前缀、`master`/`develop` 角色、worktree-first 开发、主工作区保持干净、agent 临时 worktree 生命周期、以及 `git fetch --prune origin` 的卫生巡检要求。这样后续 agent 与人工协作时，都能直接遵循统一的分支治理与仓库清理规则，而不是依赖会话上下文记忆。

验证结果：
- 已确认 `AGENTS.md` 增加了 agent 执行层面的 Git workflow、worktree hygiene、commit 约束
- 已确认 `PROJECT_BASIS.md` 增加了长期有效的分支模型与 Git 卫生规则
- 变更仅涉及文档约束，无代码逻辑修改

## 15:42 — 细化提交约束：worklog 可随功能提交，根级约束文件需独立提交

进一步细化了提交规则：`docs/LLM-Working/worklog/` 下的当日工作日志不要求单独使用 `docs:` 或 `chore:` commit，而应默认跟随对应的 `feat:`、`fix:`、`refactor:` 等工作批次一起提交；相对地，`AGENTS.md`、`CLAUDE.md`、`PROJECT_BASIS.md` 等仓库根级长期约束文件一旦修改，必须使用单独的 `docs:` 或 `chore:` commit 记录，不过同一个提交可以同时包含多个根级约束文件更新。

验证结果：
- 已同步更新 `AGENTS.md` 与 `PROJECT_BASIS.md` 中的提交规范条目
- 新规则与现有“一个 commit 对应一个逻辑工作批次”的原则保持一致

## 15:55 — 将 CLAUDE.md 中的仓库知识补齐到 AGENTS.md

为避免长期出现 `CLAUDE.md` 规则比 `AGENTS.md` 更全、导致不同 agent 入口拿到的信息不一致，本次把 `CLAUDE.md` 中仍未出现在 `AGENTS.md` 的关键仓库知识补齐到了 `AGENTS.md`。新增内容包括项目总览、前端命令约束、docs build pipeline、前端共享布局组件、Tailwind 与 `@dnd-kit` 的实现注意事项、API key middleware 例外路径、runtime fallback 经验、spec/plan 路径规范以及 note conventions。更新后，`AGENTS.md` 在仓库工作约束层面已成为 `CLAUDE.md` 的超集。

验证结果：
- 已确认 `AGENTS.md` 包含 `CLAUDE.md` 中原先缺失的项目级命令与架构约束
- 未删除 `AGENTS.md` 既有的更严格 Git/worktree/commit 规则

## 16:05 — 收口 CLAUDE.md 到 AGENTS.md 单一入口

为避免 `CLAUDE.md` 与 `AGENTS.md` 长期双份维护并再次产生信息漂移，将 `CLAUDE.md` 本体收口为最小入口文件，只保留对 `AGENTS.md` 的引用。这样后续仓库级上下文、命令约束、架构说明和 Git/worktree 规则都以 `AGENTS.md` 为单一事实来源维护，减少重复更新成本。

验证结果：
- `CLAUDE.md` 已简化为指向 `AGENTS.md` 的入口说明
- 仓库级约束信息仍完整保留在 `AGENTS.md` 中
