2026-08-10 11:57 changelog：修复 Task archive/unarchive 前端 endpoint 对裸 TaskSummaryResponse 的适配，补齐 MSW 两条生命周期路由及 legacy 状态投影，并增加 feature endpoint 回归覆盖；Vitest、lint、production build、transport drift check 与 git diff --check 均通过；关联提交：fix: align task archive response handling
2026-08-10 11:58 changelog：修复 ConversationApplicationService.list_items 的 Project collaborator 读取脱敏；新增结构化 Item payload 递归脱敏入口，保留 owner/admin 原始 payload，并用正式 Interface 回归测试确认 durable turn_items 不变。聚焦 pytest、串行 unit marker 与 ruff 检查通过；完整 unit/L0 的既有 literature_metrics async 测试在 30 秒 pytest-timeout，未进入 frontend 阶段。关联提交：fix: redact shared conversation items
2026-08-10 11:59 CST changelog：修复 Conversation worker 的 steer/interrupt durable control 状态转换：steer 在 adapter 调用前以 CAS 进入 delivering，adapter 异常 fail-closed 为 delivery_unknown 且不污染 Turn 终态；interrupt accepted 在同一 execution transaction 记录 RuntimeExecution/Turn interrupted 时 durable 完成为 completed 并写入 completed_at。新增 SQLite repository 与真实 worker 回归，验证 steer guard、adapter failure 和 interrupt completion；focused conversation execution/repository/worker tests、Ruff、Ty、dependency/instruction checks 与 git diff --check 通过；L0 backend fast 在共享主机约 60–90% 处出现失败/停滞，未将其记为通过。关联提交：本次唯一提交 `fix: enforce conversation control transitions`（最终 SHA 随交付报告）。
2026-08-10 12:23 changelog：复现 active spec 本地 Markdown 链接审计的 2 条断链，确认两处均应指向已归档的 `2026-07-11-project-task-workspace-domain-design.md`；修复 Literature 与 Console spec 的相对链接，未改 active inventory 或其他历史文档。验证：active-spec link parser（10 条本地链接，0 条断链）、`UV_CACHE_DIR=/tmp/uv-cache uv run --no-project python scripts/check_agent_instructions.py` 与 `git diff --check` 均通过；仅涉及内部 spec/worklog，未运行 docs-site build；关联提交主题：`docs: repair active spec links`
2026-08-10 12:39 changelog：将 Task Actions 的 “Interrupt current Turn” 绑定到现有 Conversation Turn interrupt feature hook，按 taskId+turnId 复用稳定幂等 key，并移除该菜单对 Task-level cancel 的调用；补充 active/no-active Turn 页面回归、具体 `/turns/{turn_id}/interrupt` transport 断言及查询刷新验证。验证：相关 Vitest（27 tests）、transport drift、lint、production build 与 `git diff --check` 均通过；关联提交主题：`fix: wire task menu interrupt action`。
2026-08-10 12:41 CST changelog：根据 reviewer 反馈补齐 Conversation execution terminal reconciliation 与 stale control recovery：Runtime/Turn 在 completed、interrupted、failed 终态同事务收敛 requested/delivering controls，interrupt 使用 evidence 内 durable CAS claim 防止并发 adapter 重复，CancelledError 在传播前写入 delivery_unknown 并确保 Turn interrupted；新增真实 terminal、stale recovery、并发单调用和 cancellation 回归。focused tests、Ruff、Ty 与 diff check 通过；L0 backend fast 重跑仍在共享主机约 60% 停滞并以 timeout 124 结束。关联提交：本次唯一提交 `fix: enforce conversation control transitions`（最终 SHA 随交付报告）。
2026-08-10 12:47 changelog：修正根 README 的 HTTP compatibility contract，明确产品仅使用 `/api`，root 与 product `/v1` aliases 已删除，`/v1/models` 与 `/v1/messages` 为独立长期支持的外部模型协议接口；保留其他未涉及的包名、CLI、环境变量和状态目录说明。验证：route source 与 generated OpenAPI 只读核对、`check_agent_instructions.py`、相关 transport contract 测试与 `git diff --check` 通过；README-only 未运行 docs-site build；关联提交主题：docs: correct product route compatibility
2026-08-10 12:56 changelog：根据 reviewer 反馈收敛 Task interrupt action owner：TasksPage 单独挂载 useTaskActions 并将同一 action/pending 传入 TaskDetailPage、TaskActionsMenu 与 Header，避免重复幂等控制；补充 turns query invalidation、并发禁用、选中 Task 切换及组件/page 回归。验证：focused Vitest（38 tests）、transport drift、lint、production build 与 `git diff --check` 均通过；关联提交主题：`fix: wire task menu interrupt action`。
2026-08-10 13:05 CST changelog：将公开架构文档 Production retirement checkpoint 从 PR/合并前临时说明改为 release owner 与用户显式授权的长期 production gate，保留 backup/rollback/telemetry evidence fail-closed；将验收审计指针改为 release record。验证：`npm --prefix docs-site run build`、`python scripts/check_agent_instructions.py` 与 `git diff --check` 均通过；关联提交主题：docs: remove stale pr language from architecture
2026-08-10 13:16 CST changelog：补齐公开 compatibility inventory 中仍由 WebUI 调用的 `/api/tasks/{task_id}/fork` legacy one-shot Task fork，记录 Conversation Domain / Task HTTP Adapter owner、当前 `TasksPage` caller、fail-closed 保留状态及迁移至 fork preview/confirm 的方向；验证：docs-site build、route/caller 只读检查、`check_agent_instructions.py` 与 `git diff --check` 均通过；关联提交主题：`docs: inventory task fork compatibility`
2026-08-10 13:19 changelog：根据第三轮 reviewer 反馈为 Task interrupt 增加同 tick 请求锁存、按 task+turn 维护幂等 key，并捕获发起请求的 Task 以确保切换选中项时仅刷新原 Task；补充 hook 并发与切换回归。验证：focused Vitest（46 tests）、transport drift、lint、production build 与 `git diff --check` 均通过；关联提交主题：`fix: wire task menu interrupt action`。
2026-08-10 13:36 CST changelog：删除 `ProjectContextService` 内被后一定义覆盖且仅递归调用自身的 `_create_active_snapshot_for_task_in_transaction` 死代码，保留实际 active-version Snapshot 实现；新增 AST 静态回归确保类内仅有一个 helper 且仍调用 active version、assembly 与 insert 流程。验证：Project Context、API Project Context、Request Context 测试，Ruff check/format、Ty、agent-instruction/dependency checks 与 git diff-check 通过；L0 在 backend fast 约 90% 出现两项失败后长时间无输出并被中断，未将其记为通过。关联提交：本次唯一提交 `refactor: remove shadowed context helper`（最终 SHA 随交付报告）。
2026-08-10 13:45 CST changelog：移除 `SessionManager` 中被后一定义遮蔽的 `_as_tenant` 实现，保留带 `hasattr` 兼容行为的唯一 context manager；新增 AST 单定义检查，并覆盖有/无 `run_as_user` adapter 的租户设置与恢复。验证：`tests/test_terminal_sessions.py`（28 passed）、Ruff check/format、Ty、agent-instruction/dependency checks 与 `git diff --check` 通过；更广 terminal pytest 在共享主机无输出超过 60 秒后按约束中止，未计为通过；关联提交主题：`refactor: remove shadowed tenant context manager`（最终 SHA 随交付报告）。
2026-08-10 13:56 CST changelog：修正两份已归档 implemented spec 的 `doc_state` 漂移：compatibility telemetry correctness 从 `current` 改为 `historical`（正文有 Historical warning），Literature transport contract 从 `current` 改为 `archived`（与同类已归档 HTTP cutover 语义一致）；归档 inventory 原有条目已完整，无需改写。验证：YAML frontmatter/spec inventory（archived specs=63、YAML frontmatter=9、archived_current=0、active=4、目标值匹配）、`check_agent_instructions.py` 与 `git diff --check` 通过；仅内部 docs，未运行 docs-site build。关联提交主题：`docs: correct archived spec document state`。
2026-08-10 14:00 CST changelog：修复 Conversation Task 取消与 worker 竞态：cancel transaction 将 queued/claimed submissions 通过正式 transition 终止并写入 finished/cancellation evidence，claim/reclaim 与 begin/accept 均以 open Task 做 CAS/transaction gate；accept+RuntimeExecution 改为同事务，worker 对取消 claim 安全跳过且 delivery 已开始时 fail-closed 为 delivery_unknown。补充 queued/claimed cancel、cancel-vs-claim、cancel-vs-accept、next-Turn waiting、worker skip 回归；focused conversation tests、Ruff、Ty、instruction/dependency checks 与 git diff --check 通过，uv pytest 因共享环境无法联网解析 hatchling，使用既有 venv 串行 pytest 验证；关联提交：`fix: cancel pending conversation submissions`（最终 SHA 随交付报告）。
2026-08-10 14:10 CST changelog：将已被接受的文献追踪服务重设计取代的 literature fetch proposal 移入 `docs/archive/proposals/`，保留历史正文并补充 superseded 状态、后继 spec 指针和原路径说明；同步修正 active spec 链接。验证：链接/frontmatter 检查、`check_agent_instructions.py` 与 `git diff --check` 通过；关联提交主题：`docs: archive superseded literature fetch proposal`（最终 SHA 随交付报告）。
2026-08-10 14:22 CST changelog：确认 `src/ainrf/api/errors.py::raise_structured_error` 仅为归档 observability proposal 中明确标注的 opt-in、unused-by-any-route helper，未进入 API package exports、ROUTERS、transport assembly、测试、动态/字符串入口或当前 contract；删除整个 79 行未使用模块。验证：focused API tests、agent-instruction/dependency checks、Ruff check/format、Ty、compileall、package imports 与 `git diff --check` 通过；完整 `bash scripts/test.sh api` 未通过，未改动的 `tests/api/test_files_routes.py` 有 5 项 timeout，且与本次删除无因果关系；关联提交主题：`refactor: remove unused api error helper`。
2026-08-10 14:25 changelog：移除 `src/ainrf/api/schemas.py` 中 14 个仅由自身引用的旧 Task detail/output/pause/resume/prompt response/request schema，保留当前 TaskSummary、TaskHealth 与 Conversation transport；全仓引用审计确认无 route/import/test/frontend/OpenAPI caller。验证：transport contract、focused API/domain/conversation tests、Ruff、Ty、instruction/dependency 与 diff-check 通过；完整 API marker 仅剩 `tests/api/test_files_routes.py` 的 5 个 sandbox/localhost 超时，排除该环境敏感组后 API marker 全部通过；frontend `check:transport` 通过；关联提交：`refactor: remove unused task api schemas`。
2026-08-10 15:20 changelog：审计并归档已完成的 Issue #76 caller/retirement inventory，将历史正文从 `docs/LLM-Working/` 根目录移入 `docs/archive/working-notes/`，仅补充 `doc_state: historical`、原路径与归档说明；全仓引用核对无真实 caller，历史 PR/parallel-slice wording 保持不改且未发现机器私有绝对路径。验证：归档 frontmatter、Markdown link/caller 审计、`UV_CACHE_DIR=/tmp/uv-cache uv run --no-project python scripts/check_agent_instructions.py` 与 `git diff --check` 通过；仅内部 docs，未运行 docs-site build。
2026-08-10 15:26 CST changelog：删除 Conversation contracts 中仅由单个单元测试自证的 legacy compatibility cluster（CompatibilityBehavior、LegacyCompatibilityContract、LEGACY_COMPATIBILITY）及其测试 import/test，保留正式 Task/Turn/Submission/Runtime/Control/Fork contracts；全仓 caller、动态/字符串入口与导出审计未发现生产或当前 contract 依赖。验证：Conversation focused tests 76 passed，generated transport drift/type-authority check、Ruff check/format、Ty、dependency-direction、agent-instruction 与 `git diff --check` 通过；生产模式 `/api/sessions` 404 probe 在共享主机 app startup 超时；标准 `bash scripts/ci.sh l1` 在首个 agent-instruction stage 因专用 worktree 缺少 hatchling 且 DNS 无法访问 PyPI 退出，未作为本切片通过证据。关联提交主题：refactor: remove test-only legacy compatibility contract
2026-08-10 15:30 CST changelog：根据 reviewer 反馈补齐取消与 acceptance recovery：ready next-Turn 可被取消并在 Task reopen 后重新 admission，delivery_unknown 保持 finished_at 为空的可 reconciliation 状态，取消 claim 以 evidence 永久 fence；delivered acceptance replay 在闭合 Task 仍幂等，缺 RuntimeExecution 时原子补建并对 active/terminal/mismatched Runtime 做冲突保护；baseline 保持真实 agentic_researcher v33，migration 034 只升级 next-Turn transition guard。新增四条 repro 与迁移测试；focused Conversation tests、Ruff、Ty、instruction/dependency checks 与 `git diff --check` 全部通过；关联提交：`fix: cancel pending conversation submissions`（amend）。
2026-08-10 16:30 CST changelog：按第二轮 reviewer 反馈移除 SQLite table rebuild/foreign-key PRAGMA 变更，migration 034 改为 savepoint 内 DROP/CREATE trigger，补 fresh/v33 parity 与注入异常 rollback 回归；delivery_unknown→delivered 明确清除旧 failure_code，native runtime identity 增加跨 Task/Turn DomainConflict fence 并验证 rollback。全量 Conversation 与 migration tests、Ruff、Ty、instruction/dependency checks、`git diff --check` 通过；关联提交：`fix: cancel pending conversation submissions`（amend）。
2026-08-10 17:00 CST changelog：收紧 migration runner 原子性：fresh connection 使用显式 `BEGIN IMMEDIATE`，调用方已有事务使用 `ainrf_run_pending` savepoint，并在 migration、schema-version 写入、savepoint release 失败时恢复原始 v33 schema/version。新增真实 SQLite authorizer fault-injection 回归，覆盖 nested RELEASE 拒绝与 `_schema_version` INSERT 拒绝；Conversation 与 migration focused pytest、Ruff format/check、Ty、instruction/dependency checks 与 `git diff --check` 全部通过（instruction check 使用仓库 venv，因 uv cache 为只读跳过 uv wrapper）。关联提交：`fix: cancel pending conversation submissions`（amend）。
2026-08-10 17:13 CST changelog：更正 13:16 worklog 条目，将已完成的 Task fork compatibility inventory 关联从临时状态标记改为稳定提交主题引用，并保留 append-only 历史；验证：`uv run python scripts/check_agent_instructions.py` 与 `git diff --check` 均通过；关联提交主题：`docs: correct worklog commit reference`
2026-08-10 18:00 CST changelog：修复 fresh baseline 的 `executescript` 隐式提交，按 `sqlite3.complete_statement` 逐完整语句执行以保持 DDL 与 schema-version 写入在同一事务；新增 caller-owned transaction 成功/失败回归，保留 TEMP 与 ordinary 写入。nested RELEASE 被拒绝时仅回滚迁移 savepoint、保留外层事务并以唯一 savepoint 名支持重试；新增真实 fresh rollback、caller transaction 与外层写入保留 repro。Conversation/migration focused pytest、Ruff format/check、Ty、instruction/dependency checks 与 `git diff --check` 全部通过；关联提交：`fix: cancel pending conversation submissions`（amend）。
