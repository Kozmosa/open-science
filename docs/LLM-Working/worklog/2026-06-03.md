2026-06-03 22:38 changelog: 完成 AgenticResearcher 重构可用性审计，检查相对 origin/master 的本地提交与未提交变更，确认 harness_engine 三个 engine 仍为 stub、AgenticResearcherService 尚未接入执行/输出/取消链路，前端任务协议迁移未闭合；验证结果：`uv run pytest tests/` 因旧 `ainrf.task_harness` 测试导入失败在收集阶段中断，`uv run ruff check src tests`、`uv run ty check`、`cd frontend && node_modules/.bin/tsc -b` 均失败，窄测 `tests/test_agentic_researcher.py tests/test_harness_engine.py` 通过。
2026-06-03 22:48 changelog: 完成 AgenticResearcher 重构收口计划编写，新增 transient implementation plan，覆盖 engine 迁移、AgenticResearcherService 执行循环、任务 API、前端协议、旧测试迁移、E2E 与最终验证；验证结果：本批次为计划文档编写，未运行新增测试。
2026-06-03 22:57 changelog: 完成阶段 2 HarnessEngine 合同收口，将执行引擎协议改为 start/emit + pause/resume/send_input/cancel，新增 EngineEvent 与 typed unsupported-operation error，并同步三个 engine stub 与单元测试；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_harness_engine.py tests/test_agentic_researcher.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/harness_engine tests/test_harness_engine.py` 均通过。
2026-06-03 23:02 changelog: 完成阶段 3 Claude Code 最小执行链，迁移 Claude subprocess 执行、session-meta token 轮询和进程取消跟踪，扩展 AgenticResearcherService 后台 runner、状态机、输出事件持久化和 `/tasks/{task_id}/output` 回放，并让任务创建后调度执行；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agentic_researcher.py tests/test_harness_engine.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/agentic_researcher src/ainrf/harness_engine tests/test_agentic_researcher.py tests/test_harness_engine.py` 均通过。
2026-06-03 23:10 changelog: 完成阶段 4 Agent SDK engine 迁移，恢复 Claude Agent SDK query 执行、provider 环境覆盖、SDK 消息到 EngineEvent 的转换、pause/resume/send_input/cancel 会话控制、token_usage 汇总和 session checkpoint 持久化，并迁移 token 追踪测试到 harness_engine 合同；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests/test_harness_engine.py tests/test_token_track.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_harness_engine.py tests/test_token_track.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/harness_engine tests/test_harness_engine.py tests/test_token_track.py` 均通过。
2026-06-03 23:14 changelog: 完成阶段 5 Codex App Server engine 迁移，恢复 JSON-RPC stdio 连接、initialize/thread/turn 生命周期、approval 自动响应、Codex item 事件转换、pause/resume/send_input/cancel 控制和 thread checkpoint 持久化，并更新 harness engine 测试覆盖进程提前退出路径；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests/test_harness_engine.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_harness_engine.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/harness_engine tests/test_harness_engine.py` 均通过。
2026-06-03 23:18 changelog: 完成阶段 6 任务运行控制 API 收口，新增 AgenticResearcherService 的 pause/resume/send_prompt 与 rerun 调度能力，补齐 session_state_path 传递和 system lifecycle 状态映射，并恢复 `/tasks/{task_id}/stream`、`/pause`、`/resume`、`/prompt` 路由；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agentic_researcher src/ainrf/api/routes/tasks.py tests/test_agentic_researcher.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agentic_researcher.py tests/test_harness_engine.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/agentic_researcher src/ainrf/api/routes/tasks.py tests/test_agentic_researcher.py` 均通过。
2026-06-03 23:59 changelog: 完成阶段 7 旧 TaskHarness 测试迁移与后端全量验证，移除依赖已删除 `ainrf.task_harness` 包的过期测试，新增当前 AgenticResearcher 任务 API 创建/输出/SSE/追加输入覆盖，修复任务启动异常边界、terminal websocket EOF 挂起、skill import 安全策略测试预期、CLI Rich 换行路径断言，以及 3 个全量 ty 类型错误；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 389 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests scripts/perf/backend/analyze_db.py` 均通过。
