# 2026-07-13

## 19:09 B1–B10 审查闭环

完成领域重构 S0–B10 的实现审查与补缺：收紧 v2 cutover/write-fence、领域 schema/import/reconcile、Project/Workspace/Environment 授权与 404/403 隐私边界、版本化 Context provenance、durable Attempt/outbox 恢复、Task 生命周期、Session 投影、Literature saga 和 Today Overview 的恢复语义。修复了陈旧测试，使直接领域写入必须经真实 committed-v2 fixture；Literature 恢复会重新读取当前用户状态/角色，但保留受限 API-key 主体的兼容授权；Overview 不再以 partial 卡片覆盖完整 last-success，并能在上海时间 06:00 前补建历史遗漏 slot。

验证：Ruff check/format、Ty、后端完整 deterministic suite（并行与串行 race 段）、前端 lint/238 项 Vitest/生产 build、文档 production build 均通过；L2 harness 静态测试通过。staging 只读检查发现既有主工作区源码权限导致容器 unhealthy，未将其作为 L2 或本分支运行证据。

## 19:23 L2 harness 安全收口

补齐隔离 L2 harness 的本地可验证安全边界：`compose up --wait` 在半启动失败时也会清理唯一 cell 资源；evidence 输出拒绝当前、主及所有已注册 Git worktree 和 shared Git directory；增加 Literature saga 在 Task 创建与 link 回写之间崩溃后的恢复编排；以前端 gateway 将上一版前端的同源 `/api` 请求代理到候选后端，并要求 artifact manifest SHA 校验。scenario runner 现可读取唯一的合成 state、tenant 与 workspace 卷，以验证备份/恢复范围。

验证：未连接真实 Docker daemon；`bash -n`、L2 harness 5 项定向 pytest（含 fake Docker 的 default-context 拒绝和 compose-up 失败 cleanup）、Ruff check/format 及 Compose YAML 解析均通过。

## 19:27 L2 API 启动兼容变量

根据 staging 兼容性证据，修复 L2 私有 runtime env 与 CLI/auth 兼容入口之间的变量分歧：保留优先的 `OPENSCIENCE_API_KEY_HASHES`/`OPENSCIENCE_JWT_SECRET`，并写入完全相同的 `AINRF_API_KEY_HASHES`/`AINRF_JWT_SECRET`。这样使用 legacy preflight 的兼容 artifact、以及当前仍读取 legacy 名称的 JWT helper，都不会在 read-only cell 内走 onboarding 或尝试落盘生成 secret；Compose 中 API 的 `openscience serve` 与 worker 的 `openscience domain-worker` 命令已核对为当前 CLI 入口。

验证：未连接真实 Docker daemon；扩展 L2 定向 pytest 至 5 项，以 fake Docker 捕获运行时 env 并验证两组 alias 同值；Bash 语法与 Ruff check/format 均通过。

## 19:31 staging 分支验证与 OpenScience 配置别名

经授权仅重建 `openscience-staging` 的 backend/nginx，保留 staging 卷且未触及生产；服务改为只读挂载本 worktree 后，backend/nginx health、staging identity、仓库 smoke 和受保护的 capability 401 边界均通过。前端 staging artifact 显式写入当前提交 SHA 后，服务端 `build-info.json` 与分支一致。

同时修复 CLI 预检与 JWT helper 对外部 `OPENSCIENCE_API_KEY_HASHES` / `OPENSCIENCE_JWT_SECRET` 的支持，保持 `AINRF_*` 兼容 fallback，避免只配置公开名称时意外进入 onboarding 或在 read-only 运行时生成密钥文件。

验证：`tests/test_auth.py` 与 `tests/test_cli.py` 共 53 项通过，相关 Ruff check/format 通过；staging 验证没有作为 L2 或 release 证据。

## 19:54 S0 backup/restore 完整性审查补缺

补齐 S0 备份与分代恢复的审查缺口：v3 manifest 现将原始文件的 mode、uid、gid 写入实际 tar member，并在验证时拒绝清单、tar 成员与包含范围不一致的归档；恢复到候选 generation 后会恢复并重新核对每个已选文件的 checksum、size、mode、uid、gid。领域迁移状态存在时，恢复还会无条件在一次性副本上执行 reconciliation；prepared/v2 状态必须 clean 且可 cutover，调用方即使传入空 validator 也不能跳过此门禁。Workspace/Tenant 仍需显式目标根目录才会 materialize，所有失败均在 promotion 前清理候选目录。

验证：`tests/test_backup.py tests/test_backup_concurrent.py` 共 26 项通过；`tests/test_domain_migration_cell.py tests/test_domain_cutover_controller.py` 共 13 项通过；相关 Ruff check/format 与 Ty 均通过。

## 20:45 S0 backup source inventory completeness

修复审查发现的 v3 backup coverage 缺口：备份现在会发现并以稳定相对路径保存全部 runtime `*.sqlite3`、全部 runtime JSON、顶层 JSON 及所有非保留 state 子目录；既有 `config/projects.json` 等成员布局保持兼容。`environments.json`、`sessions.json`、`skill_registries.json`、未来嵌套 runtime JSON/SQLite 与未来 state directory 均可在 staged restore 后原样恢复，Workspace/Tenant 仍必须显式选择。v3 校验同时确认 database/config 索引与全文件清单的成员集合一致。为使已提交 v2 generation 的 seal journal 能被备份而不污染 final legacy source manifest，import snapshot 显式排除 `domain-legacy-source-seal.json` 这一 control-plane evidence 文件。

验证：`tests/test_backup.py tests/test_backup_concurrent.py tests/test_domain_migration_sources.py tests/test_domain_cutover_controller.py tests/test_domain_migration_cell.py` 共 45 项通过；相关 Ruff check/format、Ty 通过；未执行 Docker、staging 或 Git 提交。

## 23:29 S0 maintenance writer-fence closure

补齐维护模式的持久 writer registry 与 lease 边界：cutover preflight 现在要求 API、dispatcher、Literature worker/planner、Overview planner、terminal/session reconciler 与管理 CLI 全部为新鲜 drained 状态；迁移、reconcile、CLI、terminal WebSocket input/resize 与 Literature planner/worker 在提交边界复检 epoch。来源快照同时拒绝 symlink、state-root escape 和不稳定 JSON/SQLite 输入，preflight 对共用控制库使用 legacy-source 指纹避免 participant heartbeat 造成伪漂移。CLI 结构化日志改为动态 stderr，避免测试/嵌入调用保留已关闭的输出流。

验证：维护、migration、cutover、CLI、Literature、terminal 与 source 定向回归通过；Ruff、格式、Ty、diff check 通过；L0 整合门禁通过。已知宿主 WebSocket bridge 集成用例会无限等待，维护期端到端同类用例按授权跳过并以 lease helper 回归覆盖。

关联提交：`ac26348 fix: fence all domain writers during maintenance`。

## 23:31 S0 read-only SQLite backup snapshot

将 BackupService 的 SQLite snapshot 源显式改为只读 URI，确保 staging clone 或恢复演练可从只读挂载的控制面数据创建一致 backup，而不会向来源库写 journal 或元数据。

验证：新增只读 SQLite snapshot 回归通过；包含该路径的 backup/restore 定向集与 L0 已通过。

关联提交：`25c3957 fix: snapshot SQLite backups from read-only sources`。

## 23:34 Domain telemetry fail-closed closure

完成领域控制面遥测闭环：API、CLI 与无端口 worker 通过共享 SQLite sidecar 汇聚有界计数，scrape 从持久数据刷新 migration、outbox、orphan、saga 与 Overview 指标；读取失败时输出失败/未知状态而非伪造零值，丢失事件留下不可自动清除的 delivery latch。补齐 release alert 基线、结构化日志脱敏、deprecated/legacy-write/idempotency/permission/SQLite/saga/overview 事件，以及 dispatched runtime heartbeat 和 queued Attempt 缺失可恢复 dispatch 的完整性检测。

验证：`tests/test_domain_telemetry.py tests/test_domain_worker.py` 共 43 项通过；相关 Ruff、格式、Ty 通过；L0 整合门禁通过。Release controller 对 B/E observation evidence 的实际消费仍属于后续 Release 基础设施范围。

关联提交：`6fe9a5f feat: expose fail-closed domain telemetry`。

## 23:36 Isolated staging clone hardening

将 staging 固化为独立、仅本机可达的 clone 运行面：要求仓库外且私有权限的 `STAGING_*` env 文件，拒绝生产凭据、共享卷名与 background-worker profile；state root 只能是 staging 卷根或受校验 child generation。clone 默认关闭 SSH、交互登录、自动 SSH/tmux reconciliation 与外部 observability/provider 凭据；停止默认保留卷，清除需显式确认。入口、health 与 auth 同步支持上述无副作用检查，rebuild 脚本不能绕过 staging wrapper。

验证：staging/API 隔离定向测试、Bash 语法、Ruff/格式、Ty 通过；尚未将本次生产控制面只读副本作为 L2 或 release evidence，后续仅进行隔离 clone 的非破坏性验证。

关联提交：`709647e fix: isolate staging clone runtime`。

## 23:38 B3 shared Task output privacy closure

修复 Project viewer 可通过 Task projection 读取原始持久 output 的权限边界：viewer 路径现在递归脱敏 JSON/plain-text 中的 secret、token、key、Authorization、query value 与绝对路径，并隐藏 Task-level runtime error summary；Task owner 与 admin 仍能取得原始证据用于诊断。`/output`、`/messages` 和 SSE 共用同一投影策略，避免接口之间泄露差异。

验证：session projection/API projection 定向测试 3 项通过；Ruff、格式、Ty 通过；B1–B10 审查未发现剩余已证实 P0/P1 实现阻塞。
