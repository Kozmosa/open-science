# 2026-08-02 Worklog

2026-08-02 00:13 CST changelog：完成个人终端会话控制区高密度布局调整。删除重复的操作说明，将打开、关闭、重置三个精简按钮与状态徽标合并到同一控制行；会话来源、加载状态、WebSocket、环境、会话名和附着 ID 收敛为右上角小字号紧凑信息面板，窄屏自动回落为单列，同时保留错误、未选择环境、空闲及已脱离提示。中英文按钮文案同步改为 Open/Close/Reset 与打开/关闭/重置。验证：TerminalBenchCard 专项 11/11 tests、generated transport drift、TypeScript project build、Vite production build、bundle budget 与 document metadata 全部通过；未操作 production。

2026-08-02 00:21 CST changelog：继续收敛个人终端会话详情交互。移除页面内联元数据面板，在状态徽标后增加“详情”按钮；会话环境、加载状态、会话名、附着 ID、WebSocket URL 与 backend detail 改为按需在可聚焦恢复的对话框展示。backend detail 从空值变为新内容或内容发生变化时通过 warning toast 提醒，并以 ref 去重相同消息；错误、未选择环境、空闲和已脱离等操作提示仍保留在主控制区。验证：TerminalBenchCard 专项 12/12 tests、generated transport drift、TypeScript project build、Vite production build、bundle budget、document metadata 与 `git diff --check` 全部通过；未操作 production。

2026-08-02 00:29 CST changelog：完成顶部命令面板入口的平台化精简。将搜索图标和“搜索页面和操作…”入口改为 Command 图标与“命令/Command”标签；根据浏览器平台显示快捷键提示，macOS/iOS 使用 Command 图标配合 `⇧ P`，Windows/Linux 显示 `Ctrl + Shift + P`，并为按钮提供包含平台快捷键与用途的稳定可访问名称。命令面板内部搜索 placeholder 与既有 Ctrl/Cmd+Shift+P 键盘行为保持不变。验证：App 与 Shell contracts 专项 15/15 tests、generated transport drift、TypeScript project build、Vite production build、bundle budget 与 document metadata 全部通过；未操作 production。

2026-08-02 00:38 CST changelog：将命令面板从标准标题式 Dialog 外观改为独立 Spotlight 表面。基于 Radix modal primitives 直接组合上移的紧凑面板、轻量遮罩、背景模糊与高层柔和阴影，移除可见标题栏和右上角关闭按钮，以加高搜索框作为唯一视觉起点并提升结果行密度；仍保留屏幕阅读器专用标题、Escape/遮罩关闭、焦点捕获及关闭后的触发器焦点恢复。验证：App 专项 10/10 tests、generated transport drift、TypeScript project build、Vite production build、bundle budget、document metadata 与 `git diff --check` 全部通过；未操作 production。

2026-08-02 00:42 CST changelog：移除 Spotlight 搜索输入框聚焦时由全局 `*:focus-visible` 规则产生的蓝色矩形 outline，并显式禁用该 Command input 的 focus-visible ring；其他表单与可交互控件继续保留全局键盘焦点提示，Spotlight 内仍通过输入光标和当前选中命令行表达焦点。同步增加命令面板回归断言。验证：App 专项测试与前端 production build 通过；未操作 production。

2026-08-02 00:47 CST changelog：修正 Spotlight 输入框焦点线仍被 CSS 加载顺序覆盖的问题。弃用依赖 Tailwind utility 排序的局部覆盖，为 Command input 增加专属 data attribute，并在全局 focus 规则之后以精确 `input[data-command-input]:focus/:focus-visible` 选择器显式清除 outline 与 box-shadow；覆盖范围仅限命令输入框。同步将回归测试改为验证稳定组件标记。验证：App 专项测试与前端 production build 通过；未操作 production。

2026-08-02 00:52 CST changelog：依据产品级视觉决策删除全局 `*:focus-visible` 蓝色 outline 基线，并同步撤销 Command input 为覆盖该基线而引入的 data attribute、`!important` outline/box-shadow 例外和对应测试耦合。Button、Input、Select 等需要焦点反馈的设计系统组件继续使用各自明确的 focus ring，不再由无差别全局矩形边框兜底。验证：App 专项测试与前端 production build 通过；未操作 production。

2026-08-02 01:21 CST changelog：新增设计系统 `LiquidGlass` 深 Module，以基础半透明材质、能力检测后的 SVG 折射层和 requestAnimationFrame 驱动的指针交互层提供 `subtle` / `regular` / `prominent` 三档效果，并首先应用到 Spotlight 命令面板；外观设置新增持久化“动画效果”开关，关闭时统一撤掉折射和交互层，仅保留静态基础玻璃，同时兼容旧设置文档默认开启。补充 motion preference 注入、Dialog 原语导出、中英文文案及设置迁移/玻璃降级/命令面板回归测试。验证：28 项专项测试、frontend lint、production build、bundle budget、文档元数据和 `dev.sh smoke --profile full --mode dev` 全部通过；未操作 production。

2026-08-02 01:28 CST changelog：完善移动端导航抽屉交互，为左右 Sheet 增加独立的滑入/滑出动画并同步处理遮罩淡入淡出；导航抽屉右上角由叉号改为三横线菜单按钮，保留“关闭导航”无障碍标签。将外观设置的动画开关落实为全局 motion 属性，使关闭动画时所有动画和 transition 均优雅降级为即时切换。验证：App 与 overlay primitive 共 16 项测试、frontend lint、production build、bundle budget 和文档元数据通过；未操作 production。

2026-08-02 01:35 CST changelog：优化桌面导航与任务页中等宽度布局。桌面侧边栏将 OpenScience 产品 Logo 本身改为展开/收起 Toggle，移除额外箭头按钮；任务页响应式单栏阈值由手机宽度扩展至 1023px，在空间不足时先展示完整任务列表，选择任务后整块切换到详情并提供返回入口，宽屏继续使用可调双栏。任务列表头部、搜索与列表区域统一增加标准内边距，避免内容紧贴外框。验证：App 与 TasksPage 共 34 项测试、frontend lint、production build、bundle budget 和文档元数据通过；未操作 production。
