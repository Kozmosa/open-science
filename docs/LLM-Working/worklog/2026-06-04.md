2026-06-04 00:08 changelog: 完成阶段 8 前端任务协议收口，将任务类型、mock API、Projects/Tasks 创建入口、任务节点与详情页迁移到 AgenticResearcher 扁平任务字段，补齐 TaskCreateForm 的真实 workspace/environment/project 绑定与可访问标签，并把 TasksPage 单测从旧 TaskHarness draft/task_input contract 改为当前 researcher_type/harness_engine/prompt/skills contract；验证结果：`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run` 通过 138 项、`cd frontend && npm run build` 均通过。
2026-06-04 00:24 changelog: 完成阶段 9 项目任务 API 与任务 E2E 收口，将 `/projects/{project_id}/tasks` 改为复用任务序列化并支持 `include_archived`、`limit`、`sort` 查询参数，补充项目任务过滤 API 测试，并把 AgenticResearcher/task-retry E2E fixtures 迁移到当前扁平任务协议与真实 workspace/environment id；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py tests/test_api_v1_routes.py` 通过 13 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/api/routes/projects.py tests/test_api_tasks.py`、`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && node_modules/.bin/playwright test __tests__/e2e/agentic-researcher.spec.ts __tests__/e2e/task-retry.spec.ts` 通过 9 项。
2026-06-04 00:29 changelog: 完成阶段 10 Projects E2E 登录态修复，为 projects 页面 E2E 增加 auth refresh/me 与项目、任务、edge、workspace、environment API mock，并将断言收窄到唯一的 New Task 按钮，避免 ReactFlow 容器造成 strict mode 歧义；验证结果：`cd frontend && node_modules/.bin/playwright test --workers=1` 通过 13 项，`cd frontend && node_modules/.bin/tsc -b` 通过。
2026-06-04 00:34 changelog: 完成阶段 11 Python 静态质量收口，对 `ruff format --check src tests` 指出的 24 个 Python 文件执行机械格式化，使后端格式检查恢复绿色；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 均通过，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 390 项。
2026-06-04 00:38 changelog: 完成阶段 12 最终集成验证，复跑后端/前端主验证并执行服务启动 smoke；验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 390 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests` 均通过，`cd frontend && npm run test:run` 通过 138 项，`cd frontend && npm run build` 通过，`cd frontend && node_modules/.bin/playwright test --workers=1` 通过 13 项；`AINRF_API_KEY_HASHES=... uv run ainrf serve --host 127.0.0.1 --port 8765 --state-root /tmp/ainrf-smoke` 可启动并响应 `/health`，本机返回 degraded/503 的原因是当前环境中已移出 roadmap 的 code-server 检查与暂无补全计划的 SSH/container 连通性检查，不阻断本次 AgenticResearcher/HarnessEngine 重构可用性结论。
2026-06-04 00:40 changelog: 完成任务运行时回归修复，后端启动时迁移旧 AgenticResearcher SQLite 中的 `pending`/`canceled` 任务状态，恢复 `/tasks/{task_id}/messages` 历史消息分页接口，并为原生 EventSource 使用 query API key 的 `/tasks/{task_id}/stream` 请求放行任务流访问，避免 owner id 不匹配导致 403；同步补充任务 API 测试覆盖旧状态迁移、消息接口和 API key stream。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py` 通过 4 项，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 392 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 均通过；关联提交：fix: restore task runtime compatibility。
2026-06-04 00:58 changelog: 完成引擎执行与任务详情回归修复，任务摘要接口恢复返回 working directory 与执行 command，前端摘要栏重新展示命令；Claude Code 与 Agent SDK 在无显式 provider 配置时清理隐式 Anthropic 环境变量，避免启动 shell 的过期 key 覆盖本机 Claude 配置；Agent SDK 与 Codex app-server 暂停把 partial delta 作为独立消息落库，避免 Hello/!/Hello! 重复显示，并避免 Agent SDK 已发 terminal failed result 后追加反直觉 stderr；同时调整前端 provider 顺序，避免登录刷新完成前请求 `/settings/codex-defaults` 产生启动期 401。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_harness_engine.py tests/test_agentic_researcher.py tests/test_api_tasks.py` 通过 27 项，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 397 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run` 均通过。
2026-06-04 01:06 changelog: 完成任务列表 legacy workspace 容错修复，`AgenticResearcherService` 在任务引用的 workspace 记录缺失时回退到 `state_root/workspace/<workspace_id>`，避免任务摘要 command/workdir 序列化把历史 `default-workspace` 任务升级成 `/tasks` 500；补充 API 回归测试覆盖缺失 workspace 的任务列表响应。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py` 通过 5 项，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 398 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 均通过。
2026-06-04 01:20 changelog: 完成任务持久化可见性排查与修复，确认 `~/.ainrf/runtime/agentic_researcher.sqlite3` 中历史任务仍存在，重启后“空列表”是 `/tasks` 与 `/projects/{project_id}/tasks` 对 admin 用户仍按当前 owner id 过滤导致；后端任务列表现在对 admin 取消 owner 过滤，普通用户继续只看自己的任务，并让项目任务列表复用一致的运行时摘要序列化；补充回归测试覆盖 admin 可看到其他 owner 的全局任务和项目任务。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py` 通过 6 项，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 399 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff format --check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check` 均通过。
2026-06-04 08:29 changelog: 完成 Task 新建对话框 skills 选择 UI 重做，基于当前 `TaskCreatePayload.skills: string[]` 协议实现二态分组选择器，恢复按 package 分组、展开/收起、点击选择/取消和组级全选/清空的交互风格，同时保留 i18n 与暗色语义 token；Tasks/Projects 两个创建入口都从 `/skills` 加载可选技能并传入对话框，单测覆盖分组 chip 选择与 payload。验证结果：`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（143 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 10:00 changelog: 完成 Codex app-server task 消息归一化与权限默认修复，确认任务 50f257547437 的异常来自初始 prompt 未以 user role 落库、Codex userMessage echo 与前端/后端未解包 wrapped tool event；现在任务启动会先持久化初始 user prompt，message/thinking/tool_call/tool_result 保留结构化 JSON，API 与前端统一解包 Codex {event_type,payload} 事件并去重 user echo，Agent SDK/Codex app-server 不再落库用户 echo，Codex app-server 默认使用 danger-full-access sandbox/sandboxPolicy 以匹配 codex-app-server 工作区的 yolo 编辑权限；同时修复任务详情 non-chat 消息组展开后被 effect 重新折叠的前端交互 bug。验证结果：`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_agentic_researcher.py tests/test_api_tasks.py tests/test_harness_engine.py` 通过 32 项，`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/` 通过 402 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agentic_researcher/service.py src/ainrf/api/routes/tasks.py src/ainrf/harness_engine/engines/agent_sdk.py src/ainrf/harness_engine/engines/codex_app_server.py tests/test_agentic_researcher.py tests/test_api_tasks.py tests/test_harness_engine.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（144 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 10:21 changelog: 完成 Task 创建对话框 workspace/environment/project 绑定 UI 恢复，基于当前扁平 `TaskCreatePayload` 协议在 `TaskCreateForm` 内维护 selected project/workspace/environment ids，不恢复旧 task draft 或 TaskHarness binding 对象；Tasks 全局入口可选择 project、workspace、environment，Projects 画布入口锁定当前 project 但允许选择 workspace/environment，并保留 skills 分组选择器与暗色语义样式；补充 TasksPage 与 ProjectsPage 回归测试覆盖用户选择非默认绑定后的 create payload。验证结果：`cd frontend && npm run test:run -- __tests__/pages/TasksPage.test.tsx __tests__/pages/ProjectsPage.test.tsx` 通过 23 项，`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（146 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 10:36 changelog: 完成 Task 详情页 agent 活动触发类刷新问题修复，根因是前端把所有 SSE `lifecycle` 输出都当作任务元数据变更处理，Codex app-server 的 `turn_started` 等非状态 system lifecycle 会在普通 agent 活动时触发 task/list refetch，焦点在详情页时表现为页面反复刷新；现在 `useTaskOutputStream` 只在 status lifecycle 或会改变任务状态的 system subtype（task_paused/task_completed/task_failed）上刷新任务元数据，普通 turn/tool lifecycle 只追加到输出流不重拉详情。补充回归测试覆盖非状态 lifecycle 不 refetch task metadata 且不重开 EventSource。验证结果：`cd frontend && npm run test:run -- __tests__/pages/TasksPage.test.tsx` 通过 21 项，`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（147 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 11:52 changelog: 完成 /sessions 与 /timeline 的 task-backed 数据源切换和 UI 语义收敛，确认原实现读取独立 SessionService 的 task_sessions/task_attempts 表，Task 创建与运行不会写入该表，导致真实任务数据与 Sessions/Timeline 页面脱节；现在 Sessions 页面以 `getTasks`/`getTask` 展示真实 task run 列表和详情，Timeline 页面以 `getTasks` 绘制真实 task run 时间条，并将导航与页面文案从研究会话改为任务运行历史/任务运行时间线。补充 SessionsPage/TimelinePage 回归测试，断言页面调用 tasks API 并渲染真实 task 标题、引擎和运行详情。验证结果：`cd frontend && npm run test:run -- __tests__/pages/SessionsPage.test.tsx __tests__/pages/TimelinePage.test.tsx __tests__/unit/i18n/messages.test.ts __tests__/pages/TasksPage.test.tsx` 通过 32 项，`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（147 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 12:10 changelog: 完成 Project Canvas task-edges 404 修复，确认日志中的 `GET/POST /projects/default/task-edges` 404 来自前端已有画布边持久化调用但后端只定义了 TaskEdge schema、没有注册路由或存储；ProjectCanvas 在后端 edges 为空时会自动持久化布局推导出的 initialEdges，因此缺路由会产生连续 POST 404。现在 ProjectRegistryService 增加持久化 `task_edges.json` 的 list/create/delete 方法，create 按 project/source/target 幂等返回已有 edge；后端注册 `GET/POST /projects/{project_id}/task-edges` 与 `DELETE /task-edges/{edge_id}`，OpenAPI 同步包含 /v1 前缀版本。验证结果：新增 API 回归先失败于 404 后通过；`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_v1_routes.py tests/test_api_tasks.py` 通过 20 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/projects/models.py src/ainrf/projects/service.py src/ainrf/projects/__init__.py src/ainrf/api/routes/projects.py src/ainrf/api/app.py tests/test_api_v1_routes.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`（404 项通过）均通过。
2026-06-04 12:39 changelog: 完成文献刷新阻塞与刷新结果不同步修复，确认 `POST /literature/subscriptions/{id}/fetch` 虽返回 202，但后台协程内部调用 `fetch_for_subscription` 时执行同步 `arxiv.Client().results()`，阻塞同一个 uvicorn event loop，导致其它 tab 请求排队并在抓取完成后集中打印；同时前端收到 202 后立即 refetch papers，后台未完成时会看到空结果。现在 arXiv 同步抓取被 `asyncio.to_thread` offload 到 worker thread，新增 `/literature/subscriptions/{id}/fetch-status` 查询后台抓取状态，前端刷新按钮会触发 fetch 后轮询状态到 completed/failed/timeout，再 refetch papers。验证结果：新增 event-loop blocking 回归先失败后通过；`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_literature_fetcher.py tests/test_api_literature.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/literature/fetcher.py src/ainrf/api/routes/literature.py tests/test_literature_fetcher.py tests/test_api_literature.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`（406 项通过）、`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（147 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 13:03 changelog: 完成任务创建与论文转换的三项任务预设 UI 恢复，确认当前 TaskCreateForm 与 ConvertToTaskDialog 直接暴露 researcher/engine 字段，绕过了原任务预设入口；现在新增共享 task preset helper，两个入口都展示 Raw Prompt、Structured Research、Reproduce Baseline 三项预设，并映射到当前扁平 `TaskCreatePayload` 字段而不恢复旧对象协议：Raw Prompt=vanilla+claude-code，Structured Research=aris-researcher+claude-code，Reproduce Baseline=vanilla+codex-app-server，用户仍可在预设下方手动调整 researcher/engine。补充 TasksPage 与 ConvertToTaskDialog 回归测试覆盖预设选择后的 payload。验证结果：`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run -- __tests__/pages/TasksPage.test.tsx __tests__/components/literature/ConvertToTaskDialog.test.tsx __tests__/unit/i18n/messages.test.ts`（24 项通过）、`cd frontend && npm run test:run`（149 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 13:41 changelog: 完成归档按钮与论文转换任务预设修复，确认归档按钮对 succeeded/failed 任务会调用 `DELETE /tasks/{id}`，但后端只执行 `cancel_running_task`，因此终态任务返回 409；现在后端对运行中任务仍 cancel，对已结束任务执行 archive 语义并按当前模型标记为 cancelled，使默认列表隐藏而 include_archived=true 可见。论文转换方面，Structured Research 不再只传摘要，而是生成包含 arXiv 原文/PDF 链接、全文阅读要求、`/research-pipeline` skill 调用要求和保存 Markdown 报告路径的系统研究 prompt；新增 Overview/概要总结预设，传标题、摘要、原文链接，要求生成中文文献导读 Markdown 并保存到工作区磁盘。验证结果：新增 archive 与转换 prompt 回归先失败后通过；`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py tests/test_agentic_researcher.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agentic_researcher/service.py src/ainrf/api/routes/tasks.py tests/test_api_tasks.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`（407 项通过）、`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（150 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 14:08 changelog: 完成 task 级 token 用量追踪与全局统计 API，确认原系统只有旧 SessionService attempt 级 token_usage_json/cost 聚合和 task_output 事件里零散保存的 EngineEvent.token_usage，真实 AgenticResearcher tasks 没有持久化 token_usage_json，也没有所有 tasks 的总用量统计；现在 tasks 表新增 token_usage_json 并自动迁移，AgenticResearcherService 在处理带 token_usage 的 engine event 时聚合到 task，progressive token event 会累加，final system/status usage 会覆盖同源 progressive usage 以避免 Agent SDK 双计；TaskSummary/TaskDetail 返回 token_usage_json，新增 `GET /tasks/token-usage?include_archived=true|false` 汇总 task_count、tasks_with_usage、total_tokens、total_cost_usd、total、by_model、by_engine。前端类型与 endpoint 同步新增 TaskTokenUsageSummary/getTaskTokenUsageSummary。验证结果：新增 token aggregation 回归先失败于缺 token_usage_json 后通过；`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py tests/test_token_track.py` 通过 30 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agentic_researcher/models.py src/ainrf/agentic_researcher/service.py src/ainrf/api/routes/tasks.py src/ainrf/api/schemas.py tests/test_api_tasks.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`（408 项通过）、`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（150 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 14:48 changelog: 完成资源监控面板任务用量卡片，扩展 `/tasks/token-usage` 统计 total_duration_ms、median_duration_ms 和 top_tasks，任务耗时按 started_at 到 completed_at 计算并汇总所有已完成区间，top_tasks 按 total_tokens 降序取前 5；前端新增 Resources 面板 Task Usage 卡片，展示总 token、任务总耗时、任务耗时中位数和 token 消耗最高任务列表，并补齐中英文 i18n 与 TaskTokenUsageSummary 类型。验证结果：新增后端 summary 回归先失败于缺 duration/top_tasks 后通过，新增 ResourcesPage UI 回归先失败于缺 Task Usage 卡片后通过；`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py tests/test_token_track.py` 通过 30 项，`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/agentic_researcher/service.py src/ainrf/api/schemas.py tests/test_api_tasks.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/`（408 项通过）、`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run`（151 项通过）、`cd frontend && npm run build` 均通过。
2026-06-04 15:05 changelog: 完成资源监控 Task Usage 卡片拖拽布局修复，将 Task Usage 从 CardGrid 外部固定位置移入统一 CardGrid，作为全局 taskUsage card 参与拖拽排序并保持和 System/Processes 卡片一致的 drag handle；useCardLayout 新增 taskUsage 卡片类型，默认顺序为 taskUsage/system/processes，并迁移旧 localStorage 布局，在旧的 ['system','processes'] 顺序后追加 taskUsage，避免老用户因持久化布局看不到新卡片。验证结果：新增 ResourcesPage 与 useCardLayout 回归先失败后通过；`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run -- __tests__/pages/ResourcesPage.test.tsx __tests__/unit/hooks/useCardLayout.test.ts __tests__/unit/i18n/messages.test.ts`（12 项通过）、`cd frontend && npm run test:run`（153 项通过）、`cd frontend && npm run build` 均通过。
