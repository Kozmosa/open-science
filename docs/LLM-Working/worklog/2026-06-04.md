2026-06-04 00:08 changelog: 完成阶段 8 前端任务协议收口，将任务类型、mock API、Projects/Tasks 创建入口、任务节点与详情页迁移到 AgenticResearcher 扁平任务字段，补齐 TaskCreateForm 的真实 workspace/environment/project 绑定与可访问标签，并把 TasksPage 单测从旧 TaskHarness draft/task_input contract 改为当前 researcher_type/harness_engine/prompt/skills contract；验证结果：`cd frontend && node_modules/.bin/tsc -b`、`cd frontend && npm run test:run` 通过 138 项、`cd frontend && npm run build` 均通过。
