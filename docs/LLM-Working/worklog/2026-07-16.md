# 2026-07-16

- 23:19 CST changelog：完成 `Breakwater agent runtime research` 专项调研；拉取 `zclllyybb/Breakwater` 的 `open-source` 快照 `e1cbe5e0e7f57f9426a56f95a36cc747007f271c`，沿 Lark/Jira/GitHub 触发、case/slot/queue、Codex app-server JSON-RPC、远端交付验证、凭据传播、状态页、事件遥测与 token 账本逐层复核源码和测试，并新增 `[[projects/breakwater-agent-runtime]]` 及文档索引入口；同时对照 OpenScience 当前 stdio/attempt-scoped Codex engine，给出 case 聚合、delivery verifier、durable lease、协议 schema 门禁和 `thread/tokenUsage/updated` 接入建议。验证：Breakwater 上游 `112` 个 unittest 全部通过；本机 `codex-cli 0.144.4` WebSocket app-server 启动成功，`/readyz` 与 `/healthz` 均返回 `200`，实验协议 schema 生成成功；`bash scripts/ci.sh l1-docs` 构建成功（首次因 worktree 未安装 VitePress 依赖失败，执行 `npm --prefix docs-site ci` 后通过）。关联提交：`0c9fd2d docs: analyze Breakwater agent runtime`。
