# 2026-07-16

- 00:33 changelog：完成开发环境反馈链改善的第一批，实现按 worktree、branch 与 fixture profile 稳定派生的开发实例契约，隔离 frontend/API/CDP 端口以及 repo 外的 state、runtime、logs 和凭据路径；开发 API key 仅写入权限为 `0600` 的实例文件，公开 marker 不包含密钥，并补充 detached HEAD、环境覆盖、路径拒绝与凭据稳定性测试。验证结果：定向 pytest 7 项、Ruff、format check、Ty 和完整 `bash scripts/ci.sh l0` 均通过，前端为 58 files / 299 tests；关联提交：本提交 `feat: isolate worktree development instances`。

- 00:53 changelog：完成 deterministic frontend fixture profiles。`frontend-dev prepare` 现支持 full、empty、permissions、failures、large 五种隔离 profile，使用固定领域 ID、时间和终态投影覆盖权限、失败、Today/Literature 与大列表场景；large 固定为 40 Projects、120 Workspaces、500 个终态 Tasks 和 250 Papers。profile marker 锁定 fixture schema，切换或升级要求受控 reset；所有 seeder 最终断言没有可 claim 的 Task dispatch、Literature work item 或 outbox，避免启动开发栈时触发外部运行。验证结果：定向 pytest 9 项、Ruff、format check、Ty、shell syntax 和完整 L0 均通过，前端为 58 files / 299 tests；关联提交：本提交 `feat: add deterministic frontend fixture profiles`。

- 01:06 changelog：完成统一开发栈 lifecycle。新增 `scripts/dev.sh` 与带类型的进程管理实现，提供 prepare/up/down/status/logs/reset/env，使用 instance manifest 中的 PID、进程 start time 和命令指纹限定 ownership；端口冲突只报错、不扫描或清理未知进程，启动失败会回滚本批子进程。Vite proxy 凭据只保留在 server process 环境，不再注入 `VITE_*` 浏览器 bundle。`frontend-dev.sh` 和个人 `webui.sh` 收敛为兼容参数转发壳，personal state 永久禁止 reset。验证结果：脚本/fixture 定向 pytest 17 项、Ruff、format check、Ty、shell syntax 和完整 L0 通过；真实 synthetic full 栈在派生端口 43655/43656 完成 `up → status → logs → down → reset`，API、worker、Vite 均健康且 reset 后保持 stopped；关联提交：本提交 `feat: add unified development stack lifecycle`。

- 01:15 changelog：完成本地 reload、production preview 和 HTTP smoke 反馈链。dev API 通过 env-configured FastAPI factory 与 uvicorn StatReload 监视当前 worktree 的 `src/ainrf`；preview 在启动前强制执行 frontend production build，并使用无 reload API。新增 `dev.sh smoke`，仅经 Vite localhost proxy 验证 index、实际加载 asset、health、v2 capabilities 及 Project/Workspace projection。验证结果：定向 pytest 14 项、Ruff、format check、Ty 和完整 L0 通过；真实 dev 栈检测 `server.py` mtime 变化后从 server process 809898 reload 至 987208 且 health 恢复，dev/preview 两种模式 smoke 均返回 contract v2、2 Projects、2 Workspaces，preview 加载 production hashed asset；关联提交：本提交 `feat: enable reload and local preview smoke`。

- 01:24 changelog：完成 headless browser/DevTools preflight。`dev.sh doctor` 检查 uv、Node/npm、curl、frontend dependencies 与 runtime 权限；`doctor --browser` 按显式 Puppeteer path、PATH wrapper、Puppeteer cache 顺序发现 Chrome，拒绝 broken snap Chromium，检查 chrome-devtools-mcp 命令与 Claude/OMP 配置，并使用实例专属 CDP 端口和临时 user-data-dir 启动一次无 `--no-sandbox` 的 headless Chrome。preflight 结束后总是关闭进程并删除临时 profile，输出明确说明 session 工具可见性只能在重启后确认。验证结果：browser/stack 定向 pytest 14 项、Ruff、format check、Ty 和完整 L0 通过；真实检查发现 Chrome 149.0.7827.22、Protocol 1.3、chrome-devtools-mcp 及两处 MCP 配置，CDP 在 43657 响应后关闭，端口与临时目录均完成清理；关联提交：本提交 `feat: add headless browser tooling preflight`。

- 01:31 changelog：完成开发环境反馈链文档与治理收敛。`scripts/dev.sh` 现作为标准 worktree 开发入口写入 AGENTS、frontend testing rules、开发站点和 bitter lessons；文档明确 fast HMR/reload、local production preview、L0/L1 与 L2–L4 是不可互相替代的证据层，并记录五种 fixture profile、派生端口、ownership/reset 安全规则和 browser preflight/session restart 边界。客户端延期验收清单已从“headless 无法真实测试”修正为“主机 CDP 可用、仍待 browser-tool 可见 session 完整验收”。验证结果：最终 `bash scripts/ci.sh l1` exit 0，Ruff、format check、Ty、完整后端并行/串行测试、前端 58 files / 299 tests、production build/bundle budget 和 VitePress build 全部通过；未运行 Docker、L2、shared staging、production 或 Playwright；关联提交：本提交 `docs: standardize frontend development feedback lanes`。

- 14:44 changelog：完成 P1 窄屏 Tasks 工作台修复，在 767px 以下改为列表优先的显式状态机，选择 Task 后进入详情、支持返回列表，技术 Inspector 使用真实 Sheet 且关闭后不保留屏外 tab stop；显式 Task 深链仍直接进入详情，桌面三栏 SplitPane 与既有 URL 兼容保持不变。验证结果：Tasks 定向 29 项、frontend lint、production build/bundle budget 和完整 `bash scripts/ci.sh l0` 均通过，完整前端为 58 files / 301 tests；关联提交：本提交 `fix: make task workspace usable on narrow screens`。

- 14:49 changelog：完成 P1 当前用户默认 Project 解析修复，环境选择先从 v2 projection 获取 `is_default` Project，再以真实 Project ID 请求 environment refs，并让 Settings 与 Environments 的偏好、默认环境和 reference mutation 写入该 ID；旧 `projectDefaults.default` 只保留读取 fallback，未解析或不存在 Project 时不再发送 `/projects/default/environment-refs`。验证结果：Settings/Environments/EnvironmentSelector/FileBrowser/Terminal 定向 5 files / 28 tests、frontend lint、production build/bundle budget 和完整 L0 均通过，完整前端为 58 files / 302 tests；关联提交：本提交 `fix: resolve the authenticated default project`。

- 14:56 changelog：完成 P1 Task Retry 前端契约与缓存修复，`archived_task_id` 对齐后端 nullable 语义并补齐可选 Task/Attempt/dispatch 类型；retry 成功后以响应中的实际 Task/Project 刷新 Task detail、Attempt history、Project Task list 与 relationship cache，兼容 v2 同 Task 新 Attempt 和 legacy 新 Task 两种响应，不再固定刷新 `default` Project。验证结果：Tasks 定向 30 项、frontend lint、production build/bundle budget 和完整 L0 均通过，完整前端为 58 files / 303 tests；关联提交：本提交 `fix: align task retry response and cache refresh`。

- 15:02 changelog：完成 P2 未知 Task 状态的安全呈现，集中区分正式 TaskStatus 与运行时未知值；正式状态继续使用语义色和翻译，未知状态统一 humanize 并使用 warning tone，Tasks、Sessions 与消息标题不再把缺失翻译键直接暴露给用户，也不把 fixture 中的非法状态扩入正式领域契约。验证结果：相关定向 2 files / 16 tests、frontend lint、production build/bundle budget 和完整 `bash scripts/ci.sh l0` 均通过，完整前端为 58 files / 304 tests；关联提交：本提交 `fix: present unknown task statuses safely`。

- 15:12 changelog：完成 P2 领域投影原因的统一呈现，集中映射 Project attention、Workspace execution 与权限原因，兼容旧 `environment_grant_missing`/文本别名并对未来未知标识做可读化；Projects、Workspaces 与 TaskCreateFlow 不再暴露 snake_case 后端代码，且中英文环境各自返回明确说明。为避免通用领域 barrel 把页面辅助逻辑拉入入口，reason helper 使用页面级精确导入，production bundle budget 保持原阈值。验证结果：相关定向 4 files / 15 tests、frontend lint、production build/bundle budget 和完整 L0 均通过，完整前端为 59 files / 307 tests；关联提交：本提交 `fix: translate domain projection reasons`。
