# 2026-07-11

- 00:29 changelog：完成同机 development/staging/production 隔离改造。为 production、staging、GPU 部署拆分独立前端构建目录，固定 production 与 staging 的 Compose project 边界，避免 staging `down --remove-orphans` 误处理生产服务；增加环境化认证 Cookie、staging 独立观测配置和 8 CPU / 4 GiB 资源上限，并移除本地 WebUI 写入部署可见 `.env.local` 的行为。验证结果：完整后端测试 637 passed；针对性隔离与认证测试 33 passed；production/staging 前端目标目录连续构建成功且互不删除，前端 lint 通过；三个 Compose 文件均通过 `docker compose config --quiet`，修改范围 ruff/ty、Shell 语法与 `git diff --check` 均通过。全量前端测试的 2 个既有 Monaco/jsdom suite 仍因 `document.queryCommandSupported` 缺失失败，其余 41 files / 235 tests 通过；仓库全量 ty/ruff 仍有本批次之外的既有基线问题。

- 00:45 changelog：执行 production 重新部署时暴露运行镜像入口缺失：Dockerfile 仅复制兼容命令 `ainrf`，但容器 CMD 使用 `openscience`，新容器因此以 127 重启。补充复制主 CLI entrypoint 并增加 Dockerfile 构建契约测试；待缓存重建完成后验证后端、nginx 与前后端版本一致性。关联提交：7d90d76 `fix: isolate same-host environments`。

- 00:49 changelog：完成 production 恢复与部署链加固。补齐官方 uv 构建镜像后使用缓存重建后端，确认 `openscience` entrypoint 正常；安装宿主前端锁定依赖、生成 `dist/production` 并重建 nginx。现场发现部署 shell 未携带生产必填变量且严格 umask 令静态产物不可读，遂让部署脚本仅在变量缺失时从现有容器进程内复用运行配置，并在构建后统一执行只读权限发布。最终后端与 nginx 均 healthy，宿主和浏览器 bundle hash 一致，前后端 build-info 均为 `f2e989`。关联提交：f2e989a `fix: include openscience runtime entrypoint`。

- 01:25 changelog：完成 UI 层第一批 OpenScience 品牌收敛：替换静态页面标题、资源页模拟展示、前端日志前缀、终端样式选择器以及组件和 Vite 代理的内部命名，同时保留 `.ainrf` 运行路径、`ainrf_processes` API 字段、旧存储键与环境变量回退等兼容契约；在 WebUI README 和产品文档首页明确 OpenScience 官方缩写为 `osci`。验证结果：前端 build 通过，本次变更文件 ESLint 通过，针对性 12 tests 通过，docs-site build 通过；全量前端测试仅余 2 个已知 Monaco/jsdom suite 失败，全量 lint 仅余 `reportWebVitals.ts` 的 2 个既有 `any` 基线错误。关联提交：本提交 `fix: complete OpenScience UI branding`。

- 01:34 changelog：修复 Monaco 在 jsdom 测试环境初始化时缺少 `document.queryCommandSupported` 导致页面测试 suite 无法收集的问题，在 Vitest 公共 setup 中仅对缺失 API 的环境注入返回 `false` 的兼容 stub。验证结果：`EnvironmentsPage` 与 `FileBrowserPage` 针对性 13 tests 通过，完整前端测试 43 files / 248 tests 全部通过，修改文件 ESLint 通过。关联提交：本提交 `test: support Monaco in jsdom`。

- 04:12 changelog：完成五层混合 CI 的总体设计并落地 L0/L1。新增统一 `scripts/ci.sh`、GitHub-hosted backend/frontend/docs 三路确定性门禁和有界 worker；后端全量测试按 597 项并行安全测试与 58 项 race/contention 测试真串行分区，测试 HOME、JWT secret 与 tenant root 均隔离到临时目录；前端 correctness/perf suite 分离，MSW 未处理请求改为失败，Vitest 默认最多 4 workers，并修复 Monaco/jsdom、`reportWebVitals` 与 React `act(...)` 基线；Ruff、format 与 ty 历史基线清零。退役失真的 performance workflow，将 staging 入口改为要求候选 SHA、校验环境 identity 且不管理容器生命周期的非破坏性 GET smoke，并明确 L2-L4 后续边界。最终验证：完整 L1 通过（后端共 655 tests、Ruff、format、ty；前端 42 files / 233 tests、lint、production build；docs-site 24 pages build），独立前端 perf 1 file / 15 tests 通过，脚本语法、定向 CI/staging/backup/session-store 回归与 `git diff --check` 通过；未启动或操作 staging/production 容器。关联提交：c1a4f4a `test: support Monaco in jsdom`；4922d29 `fix: clear deterministic Python quality baseline`；c90d06c `refactor: establish deterministic L0/L1 CI`；d39ae69 `test: make frontend CI signals deterministic`；642e84b `docs: define five-layer hybrid CI`；8606321 `docs: align CI governance`。

- 11:23 changelog：完成 UI 品牌分支与最新 `master` 的整合。将品牌提交 rebase 到五层 CI 基线，按时间顺序合并双方 worklog，保留 `.ainrf` 路径、`ainrf_processes` 字段、旧存储键和环境变量回退等兼容 contract；重新验证前端 L1 全部通过（lint、43 files / 235 tests、production build），docs-site 24 pages build 通过，`git diff --check` 无误。关联提交：52c51aa `fix: complete OpenScience UI branding`。

- 12:36 changelog：完成文档站构建栈升级与正式 PromQL 高亮。将 Astro、Starlight、sharp 升级到当前最新版本并迁移到 Astro 7 `docsLoader()`，固定引入 Prometheus Community 的 Apache-2.0 PromQL TextMate grammar；新增 Shiki 固定 fixture 和构建产物验证，并用自定义 Starlight 404 路由消除最新版本探测缺失条目的构建噪声。验证结果：干净 `npm ci` 成功，Node 24 `--trace-deprecation` 构建 24 pages 与 Pagefind 索引成功，未再出现 DEP0190、PromQL 或 404 warning，grammar fixture 产生 18 个着色 token / 6 种颜色，依赖树与 `git diff --check` 通过。关联提交：本提交 `chore: upgrade documentation toolchain`。

- 12:38 changelog：完成前端重依赖拆包和 Monaco 真正按需加载。移除聚合全部依赖的 catch-all vendor，收窄页面对根组件 barrel 的导入并停止从根 barrel 导出 file-browser/terminal；将完整 Monaco、loader 配置、编辑器设置和主题探测移入独立 `MonacoTextViewer` 动态入口，仅在打开文本文件时加载，同时新增 lazy 行为测试与基于 Vite manifest 的入口、路由和大资产预算门禁。验证结果：frontend L1 全部通过（lint、44 files / 236 tests、production build），构建不再产生大 chunk warning；入口静态 JS 为 601.5 KiB / 190.1 KiB gzip，FileBrowser 打开文本前为 666.1 / 211.3 KiB，Monaco 增量 lazy 为 3641.7 / 920.0 KiB，所有非终端页面静态闭包均不含 Monaco/xterm。关联提交：本提交 `perf: lazy-load Monaco editor`。

- 14:53 changelog：修复 Tasks、Workspaces、Literature 三个 split-layout 页面在 PageShell 取消默认 padding 后贴边显示的问题，为三页补回与 Resources 一致的 12px 页面级 inset，并增加对应页面布局回归测试。验证结果：针对性 3 files / 33 tests、完整前端 45 files / 238 tests、ESLint 和 production build/bundle budget 全部通过；无头浏览器 computed style 与截图确认四个页面的实际内容起点一致，未改动 Projects、FileBrowser 等全幅布局。关联提交：本提交 `fix: restore split-page insets`。

- 15:20 changelog：统一 Tasks、Workspaces、Literature 三个 split-layout 页面留白与内容面板的底色。为 SplitPane 增加默认关闭的 `uniformSurface` 模式，仅在三页启用，使主内容、左右侧栏和 resize handle 与 PageShell 共用 surface 语义色；浅色主题消除 `#ffffff` 与 `#fafaf9` 的接缝，深色主题继续使用对应 surface，而 Projects、FileBrowser 等现有布局保持不变。验证结果：针对性 3 files / 33 tests、完整前端 45 files / 238 tests、ESLint 和 production build/bundle budget 全部通过；无头浏览器确认三页各区域 computed background 均为 `rgb(255, 255, 255)`。关联提交：本提交 `fix: unify split-page backgrounds`。

- 17:49 changelog：完成 OpenScience 文献追踪服务重设计说明，确认以“今日文献收件箱、关注方向、统一收集、本地文献库、按需摘要”为核心产品模型；定义公共论文与版本、用户状态、匹配关系、检查记录、来源快照、摘要和研究任务关联的持久化边界，并统一 `/api/literature` 下的 overview、topics、papers、summary、research-task 与 checks 接口组。说明同时固定了每日检查范围、分页覆盖证明、重复请求复用、分阶段恢复、旧数据/API 迁移和 UI 验收标准，具体后台队列框架留待下一阶段选型。验证结果：内部文档链接存在，新增 spec 未被 gitignore，章节与状态契约检查完成，`git diff --check` 通过；关联提交：7bcc1af `docs: redesign literature tracking service`。

- 18:00 changelog：完成产品文档站从 Astro + Starlight 到 VitePress 的迁移。将 23 篇 MDX 内容转换为 `docs-site/docs/` 下的 Markdown，使用 VitePress 原生首页、导航、主题、404 和本地搜索替代 Astro 专属首页组件与 Starlight 配置；保留并迁移 PromQL TextMate 语法高亮，统一所有站内链接以适配 VitePress base URL；移除 Astro/Starlight、sharp、旧构建校验及其专属源码，并同步 GitHub Pages job、README 与仓库级文档构建约定。验证结果：干净 `npm --prefix docs-site ci` 后的 `npm --prefix docs-site run build` 通过，成功生成全部静态页面且无死链或语法高亮 warning；`git diff --check` 通过。关联提交：本提交 `docs: migrate site to vitepress`。

- 18:45 changelog：完成新版文献追踪首轮实现。新增 provider 导向的论文/版本、关注方向、用户状态、检查、来源快照、工作项、outbox 与摘要持久化模型，并将旧订阅安全迁移为兼容关注方向；以 arXiv RSS 为默认来源、Dramatiq + 专用 Redis 为传输层，worker 仅消费持久化工作 ID，摘要与 RSS 结果分阶段保存。前端改为今日收件箱、关注方向管理、用户级阅读/收藏/忽略和按需摘要，移除旧订阅侧栏与高频内存轮询；production/staging Compose 均声明隔离 Redis、worker、planner 和凭据。验证结果：迁移/API/追踪定向测试 25 passed，Ruff、ty、前端页面 Vitest、ESLint、production build/bundle budget 与两份 Compose `config -q` 均通过；未启动或操作 staging/production 容器。关联提交：deb4289 `feat: redesign literature tracking`。

- 21:08 changelog：启动 OpenScience Console 的产品与视觉设计收敛。新增正式设计说明，确认 OpenScience 为用户可见品牌、`osci` 为官方缩写及 CSS 命名空间；确立纯白工作画布、克制蓝色主操作、科研工作台密度、Apple 式 TopBar 毛玻璃回退和三种页面原型（收件箱、工作台、观察型）。将旧 Prism 提案降为历史参考，明确导航分组、默认入口、全局搜索、角色边界、首批页面和主题策略仍待产品确认；尚未实施前端改动或运行部署操作。

- 21:28 changelog：根据产品确认将 OpenScience Console 设计提升为已接受的基础决策：登录默认进入“今日概览”，侧栏以研究/运行/管理的轻量分组展示，Projects 保持一级入口，Resources 对普通用户可见；首批交付全局外壳、概览、页面切换 Command Palette、Literature、Tasks、Resources，随后处理 Workspaces；首期仅打磨浅色主题。新增原创矢量 Open Orbit 标识草案，用打开的研究轨道与发现点表达 OpenScience，并在设计说明中固定其使用方式。验证结果：设计文档与 SVG 已建立，`git diff --check` 通过；尚未实施或部署前端代码。

- 21:49 changelog：确认并细化 OpenScience Console 交互设计：Open Orbit 作为首版标识继续使用；今日概览采用 Resources 式可拖拽卡片布局，保留默认行动优先级且不允许隐藏需要注意卡片；Command Palette 的文案随界面语言切换并兼容英文关键词；产品明确桌面优先，窄屏保证导航、查看、确认与轻量操作。验证结果：设计说明已同步更新，`git diff --check` 通过；尚未实施或部署前端代码。

- 21:52 changelog：确认 OpenScience Console 的入口和账户体验：今日概览成为设置默认值但保留个人入口切换；账户入口迁至侧栏底部；没有研究数据的新用户只看到引导其创建 Project 或 Task 的单一起步卡，不显示零值仪表盘。验证结果：设计说明已同步更新；尚未实施或部署前端代码。

- 22:38 changelog：完成 OpenScience Console 设计 review 与 `osci` 设计系统规范拆分。修正 TopBar/账户与页面标题职责冲突，明确概览卡片按用户隔离存储、固定尺寸且不提供键盘排序；将今日概览定义为上海时间每日 06:00 生成的持久化快照，每卡展示数据截止时间并提供幂等手动刷新。新增独立 `osci` 设计系统说明，固化浅色/深色主题、三层 token、第三方主题安全契约、Google Sans 本地托管许可门禁、组件状态和渐进迁移规则；确认采用 shadcn 的源码与交互模式，但由 `osci` 统一封装、禁止一次性全量迁移或业务直接耦合。验证结果：两份设计说明链接与关键契约检查完成，`git diff --check` 通过；尚未实施或部署前端代码。

- 22:49 changelog：进一步确认 `osci` 主题和组件收敛目标。第一批设置开放浅色、深色和跟随系统选择，默认浅色；第三方主题仅交付版本化底层格式和验证器。将 shadcn 策略从“选择性参考”收敛为“所有可覆盖的通用组件最终迁移到 `osci` 封装的 shadcn 衍生实现”，同时按 overlay、表单、展示组件分批迁移，保留 OpenScience 特有工作面。明确公共 CDN 不解决 Google Sans 授权且会引入网络、隐私和 CSP 依赖，字体仅在许可可记录时本地托管，否则使用系统字体回退。验证结果：两份规范同步更新，`git diff --check` 通过；尚未实施或部署前端代码。

- 22:54 changelog：完成字体决策和第一批页面结构细化。将 Google Sans 替换为本地托管、仅含拉丁字符的 Noto Sans Regular WOFF2，中文及中高字重标题/控件使用系统 UI 字体，要求随资产保存 OFL、来源和哈希。补充全局外壳在宽屏、折叠和窄屏下的结构，定义 Command Palette 尺寸；将今日概览细化为固定跨列的可拖拽卡片栅格，并新增每日 06:00 持久快照、过期回退、幂等手动刷新和概览接口边界。验证结果：两份规范关键章节检查完成，`git diff --check` 通过；尚未实施或部署前端代码。

- 22:59 changelog：补齐字体字重与首批代表页面设计。Noto Sans 拉丁子集扩展为 400/500/600/700 四个本地 WOFF2 字重，中文使用系统字体对应字重并禁止未声明字体合成。将 Literature 细化为轻量更新状态区、URL 持久筛选、紧凑论文条目和按需详情抽屉；将 Tasks 定义为统一 PageHeader、两栏主工作面和默认关闭的元数据抽屉，并把低频任务动作收纳进菜单；将 Resources 细化为按用户保存的固定尺寸卡片网格、可见时刷新、过期快照和单环境故障隔离。三页均补充加载、空、失败、部分完成和数据过期状态。验证结果：规范章节和字重契约检查完成，`git diff --check` 通过；尚未实施或部署前端代码。

- 23:06 changelog：建立 Project、Task、Workspace 独立领域设计。确认 Project 是 Task 组织与未来共享 memory/context 的边界，Task 是持久 coding-agent 会话并由 Attempt 承载运行重试，Workspace 是实际目录或 Git 仓库且作为 Task cwd。设计三层 Context、Project memory 审核沉淀、Task 移动的版本语义、Project 归档和 Workspace 文件保留；建议 Workspace 绑定 Environment、Project-Workspace 多对多、同一 Workspace 首期单写任务并在未来使用 Git worktree 隔离。将现有 Project Canvas 降为 Project Tasks 子视图，并明确当前 Workspace 单 Project、无 Environment 位置和跨引擎 retry 语义等迁移差异。验证结果：新领域 spec 与 Console spec 双向边界检查完成，`git diff --check` 通过；尚未实施或部署代码。

- 23:12 changelog：确认 Project/Task/Workspace 领域关系并补齐操作流程。Workspace 固定属于一个 Environment，Task 选择 Workspace 后继承 Environment；Project-Workspace 数据层支持多对多，但 UI 默认一 Project、一 Primary Workspace；同一 Workspace 首期使用持久 lease 限制单写 Task，未来通过 Git worktree 并行。Task 可移动 Project，历史 Context 不变且下一 Attempt 使用新 Context；Project 只允许归档/恢复，不提供永久删除。补充 Project、Task、Workspace 三种创建入口、组合权限校验、无 Workspace 项目的引导、默认 Environment 退役和版本化可重跑数据迁移。验证结果：领域与 Console 规范同步更新，`git diff --check` 通过；尚未实施或部署代码。

- 23:16 changelog：确认首期 Project Context 与任务关系设计。Project Brief 采用 Objective、Scope、Constraints、Shared Instructions、References 的简单结构和不可变版本；通过独立 ContextSource/Assembler 生成可追踪 snapshot，Task 默认固定 Context 版本并支持显式同步。Task 内容只能产生带来源的 Candidate，必须由用户接受、进入 Draft 并再次发布后才成为 Active Context，禁止自动写入。任务关系固定为自动 `derived_from`、人工 `depends_on` 与 `related_to`，首期不触发自动调度；补充 Context 权限、API、幂等与可复现验收边界。验证结果：领域与 Console 规范同步更新，`git diff --check` 通过；尚未实施或部署代码。

- 23:46 changelog：完成 Project/Task/Workspace 领域设计交叉 review 收口。补充 Project viewer/editor/owner、Task owner、Workspace owner 与 Admin 的权限能力表，明确首期只共享 Project、不共享 Workspace；将 AgenticResearcher Task → TaskAttempt → RuntimeSession 确定为唯一权威模型并设计旧 Sessions 数据迁移。Context 改为 Task 创建时固定版本、仅由用户手动更新，明确可复现重点是代码、数据、配置、步骤和产物而非逐 token 对话；移除 Workspace single-writer lease 方案。补充 Project–Workspace 单一 Primary、事务切换、幂等、审计和归档原子性，规定默认 Project 禁止归档、归档后 queued/paused 工作停止且不自动恢复。文献转任务改为复用标准 Task 创建入口并从 Workspace 派生 Environment，概览快照明确禁止触发 arXiv/LLM 外部调用；将旧 Session Chain、Task Retry、Retry E2E 和权限规范移入 `specs/archived/` 并添加替代入口。验证结果：活动 spec 冲突关键词复查无残留，归档链接目标存在，`git diff --check` 通过；本批次仅修改设计文档与既有 Logo 草案，未实施或部署代码。
