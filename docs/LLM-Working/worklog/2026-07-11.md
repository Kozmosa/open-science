# 2026-07-11

- 00:29 changelog：完成同机 development/staging/production 隔离改造。为 production、staging、GPU 部署拆分独立前端构建目录，固定 production 与 staging 的 Compose project 边界，避免 staging `down --remove-orphans` 误处理生产服务；增加环境化认证 Cookie、staging 独立观测配置和 8 CPU / 4 GiB 资源上限，并移除本地 WebUI 写入部署可见 `.env.local` 的行为。验证结果：完整后端测试 637 passed；针对性隔离与认证测试 33 passed；production/staging 前端目标目录连续构建成功且互不删除，前端 lint 通过；三个 Compose 文件均通过 `docker compose config --quiet`，修改范围 ruff/ty、Shell 语法与 `git diff --check` 均通过。全量前端测试的 2 个既有 Monaco/jsdom suite 仍因 `document.queryCommandSupported` 缺失失败，其余 41 files / 235 tests 通过；仓库全量 ty/ruff 仍有本批次之外的既有基线问题。

- 00:45 changelog：执行 production 重新部署时暴露运行镜像入口缺失：Dockerfile 仅复制兼容命令 `ainrf`，但容器 CMD 使用 `openscience`，新容器因此以 127 重启。补充复制主 CLI entrypoint 并增加 Dockerfile 构建契约测试；待缓存重建完成后验证后端、nginx 与前后端版本一致性。关联提交：7d90d76 `fix: isolate same-host environments`。

- 00:49 changelog：完成 production 恢复与部署链加固。补齐官方 uv 构建镜像后使用缓存重建后端，确认 `openscience` entrypoint 正常；安装宿主前端锁定依赖、生成 `dist/production` 并重建 nginx。现场发现部署 shell 未携带生产必填变量且严格 umask 令静态产物不可读，遂让部署脚本仅在变量缺失时从现有容器进程内复用运行配置，并在构建后统一执行只读权限发布。最终后端与 nginx 均 healthy，宿主和浏览器 bundle hash 一致，前后端 build-info 均为 `f2e989`。关联提交：f2e989a `fix: include openscience runtime entrypoint`。

- 01:25 changelog：完成 UI 层第一批 OpenScience 品牌收敛：替换静态页面标题、资源页模拟展示、前端日志前缀、终端样式选择器以及组件和 Vite 代理的内部命名，同时保留 `.ainrf` 运行路径、`ainrf_processes` API 字段、旧存储键与环境变量回退等兼容契约；在 WebUI README 和产品文档首页明确 OpenScience 官方缩写为 `osci`。验证结果：前端 build 通过，本次变更文件 ESLint 通过，针对性 12 tests 通过，docs-site build 通过；全量前端测试仅余 2 个已知 Monaco/jsdom suite 失败，全量 lint 仅余 `reportWebVitals.ts` 的 2 个既有 `any` 基线错误。关联提交：本提交 `fix: complete OpenScience UI branding`。

- 01:34 changelog：修复 Monaco 在 jsdom 测试环境初始化时缺少 `document.queryCommandSupported` 导致页面测试 suite 无法收集的问题，在 Vitest 公共 setup 中仅对缺失 API 的环境注入返回 `false` 的兼容 stub。验证结果：`EnvironmentsPage` 与 `FileBrowserPage` 针对性 13 tests 通过，完整前端测试 43 files / 248 tests 全部通过，修改文件 ESLint 通过。关联提交：本提交 `test: support Monaco in jsdom`。

- 04:12 changelog：完成五层混合 CI 的总体设计并落地 L0/L1。新增统一 `scripts/ci.sh`、GitHub-hosted backend/frontend/docs 三路确定性门禁和有界 worker；后端全量测试按 597 项并行安全测试与 58 项 race/contention 测试真串行分区，测试 HOME、JWT secret 与 tenant root 均隔离到临时目录；前端 correctness/perf suite 分离，MSW 未处理请求改为失败，Vitest 默认最多 4 workers，并修复 Monaco/jsdom、`reportWebVitals` 与 React `act(...)` 基线；Ruff、format 与 ty 历史基线清零。退役失真的 performance workflow，将 staging 入口改为要求候选 SHA、校验环境 identity 且不管理容器生命周期的非破坏性 GET smoke，并明确 L2-L4 后续边界。最终验证：完整 L1 通过（后端共 655 tests、Ruff、format、ty；前端 42 files / 233 tests、lint、production build；docs-site 24 pages build），独立前端 perf 1 file / 15 tests 通过，脚本语法、定向 CI/staging/backup/session-store 回归与 `git diff --check` 通过；未启动或操作 staging/production 容器。关联提交：c1a4f4a `test: support Monaco in jsdom`；4922d29 `fix: clear deterministic Python quality baseline`；c90d06c `refactor: establish deterministic L0/L1 CI`；d39ae69 `test: make frontend CI signals deterministic`；642e84b `docs: define five-layer hybrid CI`；8606321 `docs: align CI governance`。

- 11:23 changelog：完成 UI 品牌分支与最新 `master` 的整合。将品牌提交 rebase 到五层 CI 基线，按时间顺序合并双方 worklog，保留 `.ainrf` 路径、`ainrf_processes` 字段、旧存储键和环境变量回退等兼容 contract；重新验证前端 L1 全部通过（lint、43 files / 235 tests、production build），docs-site 24 pages build 通过，`git diff --check` 无误。关联提交：52c51aa `fix: complete OpenScience UI branding`。

- 12:36 changelog：完成文档站构建栈升级与正式 PromQL 高亮。将 Astro、Starlight、sharp 升级到当前最新版本并迁移到 Astro 7 `docsLoader()`，固定引入 Prometheus Community 的 Apache-2.0 PromQL TextMate grammar；新增 Shiki 固定 fixture 和构建产物验证，并用自定义 Starlight 404 路由消除最新版本探测缺失条目的构建噪声。验证结果：干净 `npm ci` 成功，Node 24 `--trace-deprecation` 构建 24 pages 与 Pagefind 索引成功，未再出现 DEP0190、PromQL 或 404 warning，grammar fixture 产生 18 个着色 token / 6 种颜色，依赖树与 `git diff --check` 通过。关联提交：本提交 `chore: upgrade documentation toolchain`。

- 12:38 changelog：完成前端重依赖拆包和 Monaco 真正按需加载。移除聚合全部依赖的 catch-all vendor，收窄页面对根组件 barrel 的导入并停止从根 barrel 导出 file-browser/terminal；将完整 Monaco、loader 配置、编辑器设置和主题探测移入独立 `MonacoTextViewer` 动态入口，仅在打开文本文件时加载，同时新增 lazy 行为测试与基于 Vite manifest 的入口、路由和大资产预算门禁。验证结果：frontend L1 全部通过（lint、44 files / 236 tests、production build），构建不再产生大 chunk warning；入口静态 JS 为 601.5 KiB / 190.1 KiB gzip，FileBrowser 打开文本前为 666.1 / 211.3 KiB，Monaco 增量 lazy 为 3641.7 / 920.0 KiB，所有非终端页面静态闭包均不含 Monaco/xterm。关联提交：本提交 `perf: lazy-load Monaco editor`。

- 14:53 changelog：修复 Tasks、Workspaces、Literature 三个 split-layout 页面在 PageShell 取消默认 padding 后贴边显示的问题，为三页补回与 Resources 一致的 12px 页面级 inset，并增加对应页面布局回归测试。验证结果：针对性 3 files / 33 tests、完整前端 45 files / 238 tests、ESLint 和 production build/bundle budget 全部通过；无头浏览器 computed style 与截图确认四个页面的实际内容起点一致，未改动 Projects、FileBrowser 等全幅布局。关联提交：本提交 `fix: restore split-page insets`。
