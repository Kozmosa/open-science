# 2026-05-25 工作日志

## 00:50 — 任务列表修复与转换对话框

修复了用户报告的 4 个问题：

1. **任务列表分页限制**：前端 `getTasks` 调用增加 `limit: 200`，解决始终只显示 50 个任务的问题。
2. **任务排序**：TasksPage 增加排序下拉框（更新时间/创建时间/名称 A-Z），后端 `list_tasks_cursor` 支持 `sort` 参数映射到 `ORDER BY` 子句。
3. **论文转任务对话框**：新建 `ConvertToTaskDialog` 组件，支持选择 workspace、environment 和 execution engine，用户需二次确认后才执行转换。集成到 `LiteraturePage`。
4. **Toast 暗色模式**：将硬编码的浅色 hex 颜色替换为基于 Tailwind opacity 的语义类（`bg-emerald-500/10`、`text-emerald-600` 等），在暗色模式下自动适配。

验证结果：
- `cd frontend && node_modules/.bin/tsc -b` 通过
- `cd frontend && npm run test:run` 137/137 通过
- `uv run pytest tests/ -k task` 通过

Commit: `7d483ce` feat: task pagination, sorting, convert dialog, and toast dark mode

## 02:30 — 文献卡片 UI 改版 + 外观/字体设置

### 1. 文献卡片 PaperCard 改版
- 标题布局改为双主标题：第一行中文标题（`title_zh`），第二行英文标题（`title`）
- 原副标题样式改为摘要展示，使用 `text-xs line-clamp-3` 小字多行截断
- 作者/期刊信息移至摘要下方，保持紧凑

### 2. 设置页新增 Appearance/外观卡片
- 在 `SettingsPage` 「General」标签页下新增「Appearance」卡片
- 提供字体选择下拉框：Sans-Serif（默认）/ Serif
- 设置通过 CSS Variable（`--font-text`、`--font-display`）全局生效，覆盖整个 WebUI

### 技术实现
- `types.ts`：新增 `AppearanceSettings` 接口，扩展 `general` 字段
- `defaults.ts`：默认字体为 `sans-serif`
- `storage.ts`：加载设置时向后兼容补全 `appearance` 字段
- `context.tsx`：`useEffect` 监听字体变化并更新 CSS 变量；新增 `saveAppearanceSettings` / `resetAppearanceSettings`
- `messages.ts`：添加中英文 i18n 翻译（`pages.settings.appearance.*`）

验证结果：
- `cd frontend && node_modules/.bin/tsc -b` 通过（0 errors）
- `cd frontend && npm run test:run` 137/137 通过
- `uv run pytest tests/` 通过

相关文档：
- `docs/superpowers/specs/2026-05-25-appearance-font-family-design.md`
- `docs/superpowers/plans/2026-05-25-appearance-font-family.md`

Commit: （多文件变更已提交）

## 16:40 — 全局 LLM Provider 配置管理

### 功能
- SettingsPage 新增「LLM Providers」Tab，支持创建/编辑/删除全局 LLM Provider
- 支持 Anthropic 和 OpenAI 两种 API 格式，UI 自适应模型字段（Anthropic 三行 / OpenAI 一行）
- TaskConfiguration 的 profile 编辑区新增「从提供商填充」下拉框，选择后自动回填 apiBaseUrl/apiKey/模型字段

### 新增文件
- `frontend/src/pages/settings/LlmProvidersTab.tsx` — Provider 列表页（增删改查 + 空状态）
- `frontend/src/pages/settings/LlmProviderEditDialog.tsx` — Provider 编辑对话框（格式自适应表单）

### 修改文件
- `types.ts`：新增 `LlmProviderFormat`、`LlmProvider` 类型，扩展 `WebUiSettingsDocument`
- `defaults.ts`：`createDefaultWebUiSettings()` 返回 `llmProviders: []`
- `storage.ts`：新增 `normalizeLlmProviders()`，向后兼容缺失字段
- `context.tsx`：新增 `saveLlmProvider` / `updateLlmProvider` / `deleteLlmProvider`
- `messages.ts`：添加中英文 i18n（`pages.settings.llmProviders.*`）
- `settings/index.ts`：导出新增类型

验证结果：
- `cd frontend && node_modules/.bin/tsc -b` 通过（0 errors）
- `cd frontend && npm run test:run` 137→138/138 通过

相关文档：
- `docs/superpowers/specs/2026-05-25-llm-provider-config-design.md`
- `docs/superpowers/plans/2026-05-25-llm-provider-config.md`

Commits:
- `7682e05` feat: add LlmProvider types, defaults, and storage compat
- `7e7eaed` fix: remove duplicate WebUiSettingsDocument definition
- `8b39831` feat: add LLM provider CRUD to settings context
- `8d6c7cf` feat: add LLM provider i18n translations
- `da9ed52` feat: add LlmProviderEditDialog component
- `b209fce` feat: add LlmProvidersTab component
- `b6e5053` feat: integrate LLM Providers tab into SettingsPage
- `c5b5bc8` feat: add provider selector with auto-fill to profile editor
- `3b13bd6` test: add LLM Provider tab rendering and CRUD test
