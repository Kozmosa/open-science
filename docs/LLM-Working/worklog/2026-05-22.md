# 2026-05-22 Work Log

## i18n coverage: component-level files

Added i18n (internationalization) translation support to 12 component-level files and the i18n messages file.

**Files modified:**
- `frontend/src/i18n/messages.ts` -- Added 40+ new i18n keys for common, components.skills, components.token, components.resources, and pages.fileBrowser sections in both EN and ZH locales
- `frontend/src/components/common/Layout.tsx` -- Replaced hardcoded strings (sidebar header, footer, logout dialog) with `t()` calls; updated `buildTaskStatusSummary` to accept the `t` function and use translated strings with interpolation
- `frontend/src/components/ui/SectionCard.tsx` -- Added `useT` import, replaced hardcoded aria-label strings
- `frontend/src/components/ui/SkillToggleGroup.tsx` -- Added `useT` import, replaced hardcoded '未分组' and 'mixed' strings with translations
- `frontend/src/components/token/TokenFlowBar.tsx` -- Added `useT` import, added `tKey` to SEGMENTS config, translated token labels and legends
- `frontend/src/components/resources/CpuRing.tsx` -- Translated "CPU Usage" and "cores" labels
- `frontend/src/components/resources/GpuBar.tsx` -- Translated "No GPU detected"
- `frontend/src/components/resources/MemoryBar.tsx` -- Translated "Memory" label
- `frontend/src/components/layout/CardGrid.tsx` -- Translated "Drag to reorder" tooltip in DraggableCard
- `frontend/src/components/resources/DraggableResourceCard.tsx` -- Translated "Drag to reorder" tooltip
- `frontend/src/pages/FileBrowserPage.tsx` -- Translated file browser labels and titles
- `frontend/src/App.tsx` -- Translated "Loading..." text using `t('common.loading')`
- `frontend/__tests__/components/ui/SkillToggleGroup.test.tsx` -- Updated test expectations to match translated English text ('Ungrouped')

**Validation:**
- TypeScript type check: passes (only pre-existing error in `src/api/mock.ts`)
- Test suite: 28 files passed, 137 tests passed, 0 failures
