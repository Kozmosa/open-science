## 00:49 CST — stable frontend development credentials

- 将 synthetic frontend fixture 的 owner/editor/viewer/admin 登录密码从每次新建时随机生成，改为固定且按角色区分的本地开发密码；凭据文件仍保持仓库外存储和 `0600` 权限，生产代码路径不受影响。
- Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_frontend_dev_fixture.py -q`（9 passed）、Ruff check/format check 与 `git diff --check` 通过；开发环境 reset/smoke 验证待合并后完成。

## 01:24 CST — compact task creation form

- 收紧任务创建表单的间距与控件高度，在窄屏可用宽度允许时将项目/工作区、预设/执行引擎组织为双列；项目、工作区、预设和执行引擎统一复用 design system 的 Radix Select，并补齐 dispatcher 状态、Workspace 操作、环境推导及研究员类型等中英文文案。
- 将 authenticated shell 的 entry raw bundle budget 从 825 KiB 小幅调整到 832 KiB，以容纳共享 Radix Select 闭包；gzip 上限仍保持 264 KiB。Validation: 定向任务表单测试 25 passed，frontend lint、build、完整 frontend 测试 330 passed、full-profile smoke 与浏览器 505×540 交互检查通过；entry 实测 827.5 KiB raw / 261.5 KiB gzip。
- Commit: `fd2fe70 feat: compact task creation form`。
