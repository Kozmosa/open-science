## 00:49 CST — stable frontend development credentials

- 将 synthetic frontend fixture 的 owner/editor/viewer/admin 登录密码从每次新建时随机生成，改为固定且按角色区分的本地开发密码；凭据文件仍保持仓库外存储和 `0600` 权限，生产代码路径不受影响。
- Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_frontend_dev_fixture.py -q`（9 passed）、Ruff check/format check 与 `git diff --check` 通过；开发环境 reset/smoke 验证待合并后完成。

## 01:12 CST — agent instruction governance alignment

- 将根 `CLAUDE.md` 从自然语言软连接收口为显式导入 `PROJECT_BASIS.md` 与 `AGENTS.md` 的纯 import stub；重组 `AGENTS.md` 为 authority、工作原则、任务路由、验证入口、生成物、工作区/生产安全和 contributor/runtime 两套提示平面，减少重复与隐式上下文加载。
- 将 frontend/backend README 改为对齐 current contract 的导航层，移除 `src/ainrf/tasks/`、旧 Task pause/resume 和 backend config alias 方向等已确认漂移；在文档治理中定义各提示平面的 authority、加载和生命周期。
- 新增 `scripts/check_agent_instructions.py`，检查 import stub、必要章节、本地链接、退休路径和 runtime guardrail 注入链，并接入 L0/L1 backend gate；CI 脚本测试同步覆盖新命令和 checker 成功路径。
- Validation: 定向 Ruff、format、18 个 `tests/test_ci_scripts.py`、Bash 语法、instruction drift 与 `git diff --check` 通过；完整 `bash scripts/ci.sh l1` 通过，覆盖 dependency direction、Ruff/format、Ty、后端并行与串行测试、generated transport、frontend lint、63 files / 330 tests、production build 和 docs-site build；未操作 Docker、staging 或 production。关联提交：本提交 `docs: align agent instruction governance`。

## 01:24 CST — compact task creation form

- 收紧任务创建表单的间距与控件高度，在窄屏可用宽度允许时将项目/工作区、预设/执行引擎组织为双列；项目、工作区、预设和执行引擎统一复用 design system 的 Radix Select，并补齐 dispatcher 状态、Workspace 操作、环境推导及研究员类型等中英文文案。
- 将 authenticated shell 的 entry raw bundle budget 从 825 KiB 小幅调整到 832 KiB，以容纳共享 Radix Select 闭包；gzip 上限仍保持 264 KiB。Validation: 定向任务表单测试 25 passed，frontend lint、build、完整 frontend 测试 330 passed、full-profile smoke 与浏览器 505×540 交互检查通过；entry 实测 827.5 KiB raw / 261.5 KiB gzip。
- Commit: `fd2fe70 feat: compact task creation form`。
