# 2026-07-18

- Conversation v3 contract freeze：新增纯领域契约模块，固定 Task、Turn、TurnSubmission、RuntimeExecution、control、approval 状态机，以及 runtime projection、capability、Item、usage、Fork、authority、稳定错误和 action-specific idempotency vocabulary；增加 fail-closed Codex enum translation、opaque/sanitized native receipt contract 与 legacy continue/retry/pause/resume/Sessions compatibility declarations。专门单测覆盖合法和非法状态迁移、单 active Turn、legacy `migration_required`、provider/adapter capability 差异、请求接受与 provider/terminal evidence 分离、secret-bearing receipt metadata 拒绝及 410 deprecation。验证：focused pytest 19 tests、Ruff 和 ty 均通过。未修改 persistence、API、engine adapter 或 frontend。
- Conversation v3 core persistence：增加 additive migration 027，建立 `task_turns`、`turn_items` 与 `engine_conversation_bindings`，通过 restrictive FK、Task/Turn 双序列、partial unique index、provider-scoped dedupe、same-Task retry/causal guards 及 append-only/immutable triggers 固定核心历史不变量；新增 transaction-neutral `SqliteConversationRepository`，只提供 accepted Turn、canonical Item 与 binding lineage 的 SQL primitive，不创建事务、不迁移 Attempt 历史，也不引入 queued canonical Turn。正反测试覆盖单 active Turn、terminal immutability、跨 Task retry 拒绝、binding supersession/repoint 拒绝、Item causal ordering/dedupe/JSON/append-only，以及 legacy tables 不回填。验证：conversation/schema regression pytest 58 tests、Ruff、ty 与 `git diff --check` 均通过。未实现 submission、runtime execution、control、approval、service、API、adapter 或 frontend。
- Conversation v3 execution persistence：增加 additive migration 028 与 transaction-neutral `SqliteConversationExecutionRepository`，持久化 `TurnSubmission`、`RuntimeExecution`、`TurnControlRequest`、runtime-scoped approval，以及 Fork preview/transfer receipt；以 restrictive composite FK、partial unique index、compare-and-swap SQL、状态转换 trigger、runtime generation/expected-Turn scope、delivered-Turn proof、terminal immutability 和 preview hash/revision/mode/expiry/disclosure binding 固定 admission 与执行因果边界。正反测试覆盖 pre-acceptance 不生成 canonical history、delivery unknown 收敛、runtime generation、interrupt acceptance 与 completion 分离、stale control/approval、Fork explicit full-transcript confirmation、legacy no-backfill 和 repository transaction neutrality。验证：conversation/schema regression pytest 66 tests、Ruff format/check、ty 与 `git diff --check` 均通过。未实现应用服务事务、outbox/claim/lease/launch fence、adapter/provider 调用、API、projection 或 frontend。
- Conversation v3 persistence hardening：根据 Codex 独立不变量审查新增 additive migration 029，以不可变 `conversation_task_authorities` 显式区分新 `conversation_v3` Task 与只读 legacy Task，并在所有 v3 persistence 入口实施 authority fence；同时要求 RuntimeExecution 只绑定 active 且 native identity 匹配的 Turn、approval approve/deny 在决策时重新验证 live runtime generation、`tool_result` 必须引用同 Turn 更早的 `tool_call`，以及 Fork confirmation 时间落在 preview validity interval 内。repository 新增 transaction-neutral authority primitive；正反测试覆盖 legacy write rejection、无 authority backfill、terminal/native-mismatched runtime rejection、stale approval 拒绝与 expired/invalidated cleanup、orphan tool result rejection、Fork 过早/过期 confirmation rejection。验证：52 个 conversation/schema regression tests、Ruff format/check、ty 与 `git diff --check` 均通过。
- Conversation v3 persistence scope sealing：根据 Codex follow-up 审查新增 additive migration 030，移除 RuntimeExecution native Turn identity 的全 NULL bypass，要求 active Turn 上的 kind/ref 严格非空相等；Fork transfer 进入 `transferred` 时要求 target Task 已显式具备 `conversation_v3` authority；approval 首次离开 `pending` 后整行不可变，同时保留 `pending -> expired|invalidated` stale cleanup。回归测试覆盖 omitted native identity、legacy Fork target 与 resolved approval mutation 的拒绝路径，并将 schema version 推进至 30。验证：52 个 conversation/schema regression tests、Ruff format/check、ty 与 `git diff --check` 均通过。
- Conversation v3 application service：新增 additive migration 031、Task work-state/submission-intent/next-turn schema 与唯一 `ConversationApplicationService`，由 service 统一持有 `BEGIN IMMEDIATE`、maintenance fence、owner authorization、v3 authority、action-specific durable idempotency、atomic mutation、audit 和 commit 后通知。实现 create/retry、provider-acceptance 后 canonical Turn/首个 user Item 原子物化、显式 complete/cancel/reopen、runtime-scoped steer/interrupt/approval、deferred next-turn promotion、Fork preview/confirm receipt，以及受限 provider evidence sanitizer；普通 user/steer 文本不误作 credential evidence。Task work-status closure 只阻止新 Turn admission，不伪造 Turn 终态，也不阻止 active Turn 的 control/approval 收敛。补强 Task-wide create/retry key 冲突、authorization-before-replay、trusted harness/binding lineage、terminal evidence replay、waiting/ready deferred reservation、closure-before-promotion cancellation、transition field preservation、canonical Fork metrics/revision，以及跨时区 RFC3339 approval/Fork expiry 比较；另修复 literature summarizer cache test 对 ambient model 环境变量的依赖。验证：83 个 focused conversation service/persistence/contract/migration tests 通过；Ruff、format、ty 与 `git diff --check` 通过；L0 和 L1 的 Python lint/format、type check 与 backend deterministic tests 均通过，但两者最终均因 frontend 依赖中缺少 `eslint` 而在 frontend lint 退出 127，未将完整 deterministic gate 误报为成功。
