# 2026-07-18

- Conversation v3 contract freeze：新增纯领域契约模块，固定 Task、Turn、TurnSubmission、RuntimeExecution、control、approval 状态机，以及 runtime projection、capability、Item、usage、Fork、authority、稳定错误和 action-specific idempotency vocabulary；增加 fail-closed Codex enum translation、opaque/sanitized native receipt contract 与 legacy continue/retry/pause/resume/Sessions compatibility declarations。专门单测覆盖合法和非法状态迁移、单 active Turn、legacy `migration_required`、provider/adapter capability 差异、请求接受与 provider/terminal evidence 分离、secret-bearing receipt metadata 拒绝及 410 deprecation。验证：focused pytest 19 tests、Ruff 和 ty 均通过。未修改 persistence、API、engine adapter 或 frontend。
