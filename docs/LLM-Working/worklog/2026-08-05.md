2026-08-05 00:51 changelog：完成 Agent 任务列表展示与重命名交互更新；默认隐藏 failed/cancelled 任务并新增“显示失败/取消”筛选，任务标题支持列表内原地重命名，状态旁新增 AI 刷新名称占位按钮及中英文文案；补充任务页筛选、重命名和既有重试流程回归测试。验证通过：任务页定向测试 25 项、前端完整测试 333 项、ESLint、transport drift、TypeScript、Vite 生产构建、bundle budget 与 document metadata 检查。
2026-08-05 01:07 changelog：完成工作区页面高信息密度布局重构；将大标题和双重大卡收敛为紧凑工具栏、可搜索/按可用性筛选的工作区列表及单一详情面板，在 560px 以上启用窄版双栏，合并重复状态并压缩路径、运行信息、项目关联和操作区；第一版新增布局文案按要求使用中文，保留原有业务对话框和操作行为。验证通过：工作区页面测试 4 项、前端完整测试 334 项、ESLint、transport drift、TypeScript、Vite 生产构建、bundle budget 与 document metadata 检查。
2026-08-05 01:17 changelog：完成紧凑工作区页面窄屏适配修正；顶部入口改为“新建”，可用性下拉筛选收敛为“仅显示可执行”复选框，状态徽标固定单行，并重排详情头部让状态、路径和描述使用完整面板宽度。验证通过：工作区页面测试 4 项、前端完整测试 334 项、ESLint、transport drift、TypeScript、Vite 生产构建、bundle budget 与 document metadata 检查。
2026-08-05 01:33 changelog：完成工作区页面响应式主从布局调整；将双栏启用阈值后移到 xl 宽度，较窄视口不再渲染独立滚动的列表栏，改用顶部工作区下拉选择器和下方单一详情区，同时让单栏详情名称与操作区上下排列以保留完整标题宽度。验证通过：工作区页面测试 4 项、前端完整测试 334 项、ESLint、transport drift、TypeScript、Vite 生产构建、bundle budget 与 document metadata 检查。
2026-08-05 01:43 changelog：完成工作区可用性文案与页面国际化；将“可执行/不可执行”统一改为“可用/不可用”，把环境授权原因改为面向用户的权限说明，并将紧凑页面新增的标题说明、计数、搜索、筛选、选择器、复制、统计和项目关联文案完整接入中英文资源及本地化日期格式；同步清理旧版未使用翻译项以保持 bundle budget。验证通过：相关定向测试 12 项、前端完整测试 334 项、ESLint、transport drift、TypeScript、Vite 生产构建、bundle budget 与 document metadata 检查。
2026-08-05 02:06 changelog：完成现有工作区不可用原因映射补全；统一覆盖后端稳定原因代码和旧版文本返回，包括环境停用、环境授权缺失、工作区注销、项目归档、项目关联失效及所有者权限，并对旧文本进行忽略大小写的规范化匹配；同步将原因提示中残留的“可执行”术语改为“可用”，暂不引入尚未定义的环境健康故障判定。验证通过：相关定向测试 12 项、前端完整测试 334 项、ESLint、transport drift、TypeScript、Vite 生产构建、bundle budget 与 document metadata 检查。
2026-08-05 03:21 changelog：完成遗留工作区关联 Interface 与文本原因兼容层清理；删除仅被环境引用 Adapter 间接调用、且结果大部分被丢弃的 `workspace_links` 浅 Interface 和实现，环境引用读取改为复用稳定 Project console projection；移除不可达的自然语言原因返回、前端文本别名和旧原因 mock/测试数据，使产品路径只传递稳定原因代码。验证通过：API 定向测试 17 项、前端定向测试 21 项及完整 L1 门禁，包括依赖方向、Ruff、格式、ty、后端全量确定性测试、transport drift、前端 334 项测试、生产构建、bundle budget、document metadata 与文档站构建。
2026-08-05 13:10 changelog：完成 issue #71 的 conversation-only worker 运行图收敛；生产 CLI 不再注入 legacy engine factory，`TaskDispatcher` 在 conversation-only 模式下只构造 committed-v2 Conversation 与必要 cutover/maintenance 控制面，Attempt、Overview、Literature writable 依赖改为 legacy 分支惰性加载，并补充通过 Conversation Interface 验证 Task/Turn/Item 完成语义的 worker 回归测试。验证通过：worker、Conversation、cutover、dependency-direction、lifecycle 与 CLI 定向测试全绿，Ruff、格式、ty 和模块导入探针通过；`bash scripts/ci.sh l1` 的静态 gate 全绿，但后端全量 pytest 在出现未带摘要的失败后长时间无输出，已中止未完成的进程。关联提交：422fb7a `refactor: remove legacy worker initialization`。
2026-08-05 13:17 changelog：完成 issue #75 控制面深 Module 收敛；建立四个目标 Module 的 Interface/caller/invariant/side-effect inventory：`domain_telemetry` 由 API scrape、事件记录器和 Prometheus 消费，保留 durable counters、source readiness、committed-v2 fuse 与 last-known-good fail-closed；`backup.service` 由 CLI/release 调用，保留 manifest 完整 inventory、SQLite/文件校验、隔离恢复、reconciliation、generation journal、原子 promotion 和 rollback；`domain.service` 由 API/application services 调用，保留授权、幂等、审计和唯一 workspace write path；`overview_jobs` 由 API、no-port worker 和 CLI planner 调用，保留 durable job store、maintenance participant、租约、重试和只读降级。删除无 caller 的 Overview 同步 compatibility facade、workspace replay-only Protocol/实现、备份历史 SQLite 白名单，以及 telemetry 对 migration issue/run/result/attention 的重复 aggregate、fixture、指标和告警；未修改 #71/#76 范围。扩展目标回归 198 项通过，专项 telemetry/backup/domain/overview 94 项通过，Ruff、format、ty 与 diff check 通过；完整 L1 在未改动的 `tests/api/test_files_routes.py::test_read_file_localhost[asyncio]` 因 AnyIO/client 30 秒超时中止，单测独跑复现该环境级失败。
2026-08-05 13:20 validation continuation：backup/restore/rollback/release contract 回归 54 项通过；frontend L1（transport drift、ESLint、334 Vitest、TypeScript/Vite build、bundle budget、document metadata）与 docs-site VitePress production build 均通过。完整 L1 的唯一未通过项仍是未改动文件中的 `test_read_file_localhost[asyncio]` AnyIO timeout。
2026-08-05 13:21 changelog：完成 issue #74 的前端 feature 依赖收敛；删除无 caller 的 SeedFileUploader 和两条过时循环边，改由 app 页面 wrapper 组合 TaskCreateFlow，并以 EnvironmentSelectionPreferences public Interface 让 settings 注入环境选择状态；新增覆盖 alias、barrel、side-effect、lazy/dynamic import、self-barrel、显式 allow-list、分层方向与 cycle 的 dependency checker 及 Vitest 门禁。图谱从 14 节点/28 条 feature 边/2 个 cycle/7 个 self-barrel import 收敛为 14 节点/24 条边/0 cycle/0 self-barrel，24 条实际 feature 边全部显式允许。验证通过：frontend lint、63 个测试文件/335 个测试、transport drift、TypeScript、Vite build、bundle budget 与 document metadata；L1 的 Python 依赖方向、Ruff、格式、ty 通过，但后端全量测试在出现既有失败标记后 contention lane 长时间无输出，已中断，未将 L1 记为通过。
2026-08-05 13:25 changelog：完成 issue #72 页面 locality 清理。职责/data-flow inventory 确认 Environments 页面保留表单、mutation 与 cache-sync composition，selection hook 继续拥有列表 fallback/选择状态；Literature 页面继续拥有 URL filters、query polling 与 mutation orchestration，删除已失效的 localStorage intent 恢复路径并让研究任务使用页面内 idempotency manager；Tasks 页面继续拥有 URL/navigation、响应式布局和 mutation composition，删除 no-op `useTaskStream`、`streamConnected`、旧 `sidebar` 兼容状态及无 caller 的 stream props。同步删除无 caller 的环境 project-reference form/update wrapper、重复 fallback/debug logging 和直接测试旧 output helper implementation；保留 `useTaskMessages` 的 message/stream conversion seam 与 output 性能测试以维持 streaming 能力。状态 owner 收敛为 selection hook、各页面 query/mutation、TaskDetailPage/useTaskMessages；无 shared type、generated transport 或跨 feature dependency 变更。Interface 变化：Environment page helper exports 减少 6 个，Tasks feature barrel exports 减少 3 个，TaskDetailPage props 由 16 个减至 11 个，Literature durable localStorage state owner 减至 0。验证通过：相关页面/组件测试 38 项、前端全量 329 项、ESLint、frontend build（transport drift/TypeScript/Vite/bundle budget/document metadata）及 docs-site build；L1 的依赖方向、Ruff、格式、ty 通过，但 backend gate 中既有 `tests/api/test_files_routes.py::test_read_file_localhost[asyncio]` 因 `domain_sqlite_error/OperationalError` 超时，独立复跑仍失败，故完整 L1 未能绿灯。
2026-08-05 13:55 changelog：完成 issue #73 前端类型权威收敛；删除 shared 产品类型和 legacy transport alias，迁移 feature-owned view model 与 transport boundary adapter，移除 domain 对环境类型/API 的错误归属，拆分环境查询与 mutation 以保持 FileBrowser 静态 bundle 预算，并新增 type-authority 静态门禁及同步 mock/fixture/test。验证通过：transport check、ESLint、前端 334 项测试、TypeScript/Vite production build、bundle budget 与 document metadata；L1 Python 依赖方向、Ruff、格式和 ty 通过。完整 L1 backend race/DB lane 在未改动的 backend 测试中出现失败并卡住后中止，docs gate 因 worktree 未安装 `docs-site` 的 `vitepress`（`vitepress: not found`）未完成。
2026-08-05 14:39 changelog：完成 Issue #76 的 legacy retirement 批次；删除已完成的 migration chain、importer/reconciliation/cutover、Attempt/RuntimeSession repository/projection/worker、旧 API/generated transport/frontend fixture 与专属测试，收口 Task/Literature/Context 到 Conversation authority，建立 `agentic_researcher=33`、`auth=7`、`literature=7`、`terminal=1` fresh-install baseline，并保留显式 `migration retire-legacy preflight|apply|verify`、只读 admin audit、backup/restore 与维护检查点；归档过时领域 spec，补充 caller inventory 和生产 rollback/pre-merge checklist。验证通过：Ruff、compileall、migration/audit/backup/maintenance/conversation/literature 专项测试、frontend lint/transport/build；production-contract 的 SPA 静态入口测试在环境中的 Starlette `StaticFiles`/`FileResponse` 最小复现同样超时，待最终 L1 复核。未访问或操作生产容器、数据库、端口、日志或数据，未执行生产 migration。
2026-08-05 15:09 changelog：完成 #71 rebase 后的 CLI、Ruff、ty 和格式收口，提交 `9d4ce6d`、`5ade3db`、`668da06`、`82b8846`；最终 L1 已通过依赖方向、Ruff、格式和 ty，frontend lint、transport drift、production build 及 Conversation/Literature/backup/API 专项通过。L1 backend 全量 gate 未能完成：非 production-contract 的 `tests/api/test_files_routes.py::test_read_file_localhost` 在孤立 serial 重现中卡在 `anyio.to_thread.run_sync(Path.read_bytes)`，最小 AnyIO `to_thread` 读取也复现同一环境超时；此前 SPA/静态路由测试同样在 AnyIO/Starlette 环境超时。该 files/AnyIO 路径未被本 Goal 修改，未为绕过环境问题改变生产代码。未执行生产 migration、生产检查或 merge。
2026-08-05 15:13 changelog：补充最终无 caller 审计；删除仅剩自身定义、没有静态/动态 caller 的 `src/ainrf/domain/dispatch_wakeup.py`，并在 inventory 中记录 deletion test 证据。验证通过：全仓 caller 扫描和工作树清洁检查；未访问或操作生产环境。
2026-08-05 19:08 changelog：完成 PR #82 在 #77/#78 合并后的 rebase 修复与当前 authority 收口；将 domain telemetry、Prometheus/Gatus 和 sidecar schema 从旧 Attempt/outbox/cutover 模型切换到 TurnSubmission，增强 `retire-legacy apply/verify` 对旧 snapshot、legacy-write counter 和 anchor 的清理验证，使用正式 Project Module 修复 Overview fixture，并把 retry E2E mock 迁移到返回 `202 TurnSubmissionResponse` 的 Turn retry Interface；删除无 caller 的 `TaskLifecycleModule` marker，将误导性的 `v2_ready` / `record_first_v2_write` 收敛为当前语义的 `ready` / `validate_write`。验证通过：定向 telemetry/migration/Overview 47 项、Ruff、format、ty、dependency direction、E2E ESLint；沙箱外 4 workers 后端完整 L1 两次通过，前端 L1 62 文件/334 项测试及生产构建通过，docs-site VitePress build 通过。未访问或操作生产容器、数据库、端口、日志或数据，未执行 production migration 或合并 PR。关联提交：b2f8d7a `fix: align legacy retirement with current conversation authority`。
2026-08-05 19:26 validation continuation：PR #79 已在 #77/#78/#82 合并后的最新 `origin/master` 上完成 rebase；冲突仅涉及当日 append-only worklog，保留主线记录并按时间顺序恢复 issue #74 条目。最终验证通过：Python dependency direction、前端 transport drift、feature dependency checker（14 nodes、24 edges、0 cycles、24/24 allow-listed）、ESLint、63 个 Vitest 文件/335 项测试、TypeScript/Vite 生产构建、bundle budget 与 document metadata。未访问或操作生产环境。
2026-08-05 19:39 validation continuation：PR #81 已在包含 #79 的最新 `origin/master` 上完成 rebase；冲突解决保留 `settings -> environments` 单向依赖及注入式 EnvironmentSelectionPreferences Interface，同时迁移到 feature-owned view model/type 和唯一 generated transport authority。首次生产构建发现 FileBrowser 静态闭包为 852.5 KiB、超出 850 KiB 门限，随后让 FileBrowser 直接依赖窄的环境选择 hook Interface，避免经 feature barrel 携带无关环境管理实现，最终收敛到 840.2 KiB。完整 frontend L1 通过：transport drift、type-authority（295 source files、29 个合法 generated transport imports）、feature dependency checker（14 nodes、23 edges、0 cycles、23/23 allow-listed）、ESLint、63 个 Vitest 文件/335 项测试、TypeScript/Vite build、bundle budget 与 document metadata。未访问或操作生产环境。
