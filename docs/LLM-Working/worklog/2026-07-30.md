2026-07-30 00:36 CST changelog：完成 P4→P5 compatibility debt handoff，修正 inventory 中已过期的 P4 owner/removal deadline，将 retained route、field 与 Task response compatibility 明确交由 P5；补齐 deprecated route/contract telemetry key、完整观察窗口、canonical caller migration 和 removal evidence，并增加 fail-closed guard，确保 production deprecated-traffic 仍为 observed gap 时不得把缺失指标解释为零调用或提前删除兼容 surface。local-only architecture cleanup suite、专用 Ruff/format、JSON 与 diff 检查通过；关联提交：本提交 `chore: hand off compatibility debt to p5`。

2026-07-30 01:55 CST changelog：完成 P5 前端分层收口实现批次：清空 frontend layer allowlist，移除 legacy `components/` source，将 Layout/shell 归入 app、环境/终端/任务/项目/资源/文件/设置 UI 归入对应 feature，中立 loading/i18n/Pill 归 shared 或 design-system；根 pages 收窄为 composition entry，并将集中 endpoint 与测试 mock 调用迁移到 feature API；Monaco viewer 改为由调用方注入主题和字体偏好，generated transport 继续作为 endpoint、真实 contract 与 MSW transport adapter 的权威 metadata。兼容 route/field surface 因缺少生产完整观察窗口 telemetry 继续 fail-closed 保留。验证：frontend dependency scanner 无违规，local-only architecture cleanup 20 tests、frontend lint、60 files/327 frontend tests、production build、transport drift、`scripts/ci.sh l0` 与完整 `scripts/ci.sh l1`（backend 并行/串行、frontend、VitePress）全部通过；L1 仅报告 3 个既有 unused type-ignore warning。关联提交：`23749cf refactor: close frontend feature layers`。

2026-07-30 02:42 CST changelog：完成主工作区合并后卫生收口，将两份一次性中英文架构审查 HTML 从仓库根目录移至上级个人归档目录 `/data/yile.chen/code/`；确认项目级 `codebase-design`、`handoff`、`improve-codebase-architecture` skills 为长期开发工具，将 `.agents/` 与来源哈希 `skills-lock.json` 纳入版本控制，并补入遗漏的 `2026-07-28.md` 工作日志。验证：核对归档文件存在、skills inventory 与 lockfile 一致，提交前 Git 状态仅包含本批预期文件；关联提交：本提交 `chore: track project agent skills`。

2026-07-30 03:30 CST changelog：完成架构清理 P6 仓库卫生与长期文档收口批次：从包含 `5d5243f` 的最新 `master` 创建独立 worktree，删除完整 cleanup-only suite、tracked `test/` 一次性研究/export 树及重复大型 PDF，并删除零产品调用的 deprecated DB instrumentation；保留 `.agents/`、`skills-lock.json`、migration fixtures、generated transport 和全部缺少生产零流量证据的 compatibility surface。将当前 v2 authority、`/api` canonical prefix、generated transport、frontend 分层、release/rollback contract、compatibility owner/telemetry/removal condition 迁移到 VitePress 长期文档，并标记架构清理 spec P0–P6 已执行。验证：backend graph 198 modules / 489 edges / 0 cycle，non-API→API 与 frontend reverse dependency 扫描无违规；Ruff、format、Ty、transport drift、frontend lint、60 files / 327 tests、frontend build、VitePress build、host-context L0/L1 全部通过；sandbox 内 thread/process tests 可复现挂起，按仓库规则在正常 host context 完成标准 gate；仅保留 3 个既有 unused type-ignore warning 与 TodayPage React act warning；关联提交：`9e9176f refactor: remove architecture cleanup artifacts`，本提交 `docs: archive architecture cleanup p6`。

2026-07-30 03:42 CST changelog：完成 P6 根级长期约束收口，将 committed-v2 authority、canonical `/api`、compatibility fail-closed、generated transport owner、frontend 单向依赖和长期验证入口写入 `PROJECT_BASIS.md`，并按治理文档规则保持独立提交；复核 `git diff --check` 与完整 L1 结果仍有效；关联提交：`0c9dd47 docs: align project architecture constraints`，本提交 `docs: record p6 governance closure`。

2026-07-30 14:35 CST changelog：完成文档系统 authority 与生命周期整治：清除 4 份误提交的 transient implementation plans，将 50 份已实现、被取代或与当前契约冲突的增量 spec 迁入 `docs/superpowers/specs/archived/` 并统一增加历史 warning，将完成 proposal 与 4 份 working notes 移入通用 archive；活跃 spec 收敛为 CI、Console/设计系统、Project/Task/Workspace、Literature 和待实施 Conversation/Runtime 共 8 份。新增文档治理与 active inventory，明确 `docs-site/docs/` 是当前产品 contract、`docs/` 是内部长期知识库，并确立 OpenScience 为品牌、`ainrf` 为稳定内部工程身份、`osci` 仅用于前端设计系统/紧凑品牌展示。验证：tracked plans 为空，活跃 spec 8 份、归档 spec 55 份，移动范围内 Markdown 相对链接零缺失，`git diff --check` 与 VitePress production build 通过；关联提交：本批次文档与治理提交。

2026-07-30 14:50 CST changelog：完成 compatibility telemetry 正确性设计，基于 staging 审计确认的 canonical 误算、Task/Session alias 漏算和 prefix/operation 混算，定义长期 HTTP contract telemetry 优先、cleanup-only 指标严格例外的双层方案；固定 canonical/root/v1/external-compatible 分类、稳定 operation 与低基数标签，设计 durable release aggregate、临时 registry 双重删除门禁、旧指标迁移和 staging delta/restart 验收。本轮明确只修复准确识别，不拆 router、不删除兼容 surface、不改变 transport contract；验证结果：`git diff --check` 通过，安装 lockfile 固定的 docs-site 依赖后 VitePress production build 通过；关联提交：本提交 `docs: define compatibility telemetry correctness`。

2026-07-30 14:55 CST changelog：完成 browser doctor 会话状态与跨平台工具链修复批次：移除 `--browser` 即硬编码“必须重启”的错误判断，对 shell 无法观察的 Codex task 工具注册表明确返回 unknown，并提供 `chrome-devtools list_pages` 的 task 内验证动作；补充 Codex 全局/项目 TOML MCP 配置发现，保留 Claude/OMP JSON 兼容，扩展 Puppeteer Linux/macOS/Windows 缓存布局发现及非 POSIX 浏览器进程清理降级。验证：browser 专项 8 tests、Ruff、format、Ty、L0 与完整 L1 均通过（仅 3 条既有 unused type-ignore warning及 TodayPage React act warning）；修改后的 doctor 在 headless Linux 上识别 Chrome for Testing 149、Codex/Claude/OMP 配置并完成 CDP 1.3 握手，正确输出 `session_restart_required: null`；当前 Codex task 通过 chrome-devtools MCP 完成 data URL 的 snapshot/click/DOM 状态断言，并在 worktree 隔离 full dev stack 上验证真实 OpenScience 登录页、synthetic API smoke、首屏 Network 200 与零 console warning/error；关联提交：本提交 `fix: report browser doctor session state accurately`。

2026-07-30 15:08 CST changelog：完成 compatibility telemetry correctness 实现与长期 authority 收口：新增按真实请求路径区分 canonical、root、`/v1`、external-compatible 和 non-product 的长期 HTTP contract counter/histogram，以冻结 operation/method/status class 防止高基数；新增按日聚合、180 天 retention、跨 backend 重启连续的 durable evidence 与 delivery failure latch；将 Task request field、response field emitted、body idempotency alias 和 `AINRF_*` config alias 迁入带 owner/review deadline/双重门禁的临时 cleanup registry，并保留旧 deprecated 指标仅作一个 release 的 superseded 对照。Grafana、Prometheus alert、架构/observability 文档和 spec 生命周期已同步；未拆 router、未删除接口、未改 schema/generated transport/HTTP 行为。验证：新 telemetry Interface/API matrix 及相关 Task/domain/resource/session 定向测试通过，API lane、Ruff、format、Ty、transport drift、Grafana JSON、Prometheus YAML、VitePress build 与完整 L1 全部通过；仅保留 3 个既有 Ty unused-ignore warning和 TodayPage React act warning。关联提交：`e3ab8df fix: correct compatibility telemetry accounting`，本提交 `docs: publish compatibility telemetry authority`。

2026-07-30 15:31 CST changelog：完成 production-mode staging 暴露的 FastAPI lazy included-router telemetry 修复：启动时展开 `_IncludedRouter.effective_route_contexts()` 并冻结 route path regex、method 与稳定 operation inventory，请求结束后用冻结路由匹配真实 path，避免 production app 的 product operation 全部退化为 `unmatched`；补充 lazy inventory 回归测试，保持 HTTP route、schema 与 transport 行为不变。验证：compatibility telemetry 定向测试 17 项、Ruff 与 Ty 均通过；关联提交：本提交 `fix: freeze lazy FastAPI route inventory`。

2026-07-30 15:40 CST changelog：完成 compatibility telemetry production-mode staging 最终验收：在隔离的 `telemetry-acceptance` state generation 上验证 Project、Workspace、Environment、Session、Task 的 canonical/root/`v1` 与 external-compatible 共 16 个 cell 精确增量均为 1；backend restart 前 durable aggregate 为 22 行/210 次，重启探针后连续增长且 `external_compatible/get_models` 精确 7→8，完整 staging recreate 与标准 smoke 后仍为 22 行/254 次、首次时间不变、delivery failure latch 为 false。原始 metrics、Prometheus 查询与已 provision 的 Grafana panel PromQL 一致，Grafana/Prometheus/后端/nginx 均健康，frontend/backend provenance 均为最终提交。完整 L1 再次通过 backend 并行/串行、transport drift、frontend lint、60 files/327 tests、production build 与 VitePress build；仅有 3 条既有 Ty unused-ignore 与 TodayPage `act(...)` warning。关联提交：本提交 `fix: freeze lazy FastAPI route inventory`。

2026-07-30 16:50 CST changelog：完成 active spec 生命周期合规批次，为 8 份活跃设计统一补充机器可读的 `status`、`last_reviewed` 与 30 天内的 `review_by`；修正文档治理中的 authority 漂移，明确 `PROJECT_BASIS.md` 与工程事实冲突时必须停止并交由用户裁定，Agent 不得擅自修改该文件；将 Engine Runtime 设计中的后端 provider credential 环境变量规范名收敛为 `AINRF_PROVIDER_KEY_*`，旧 `OPENSCIENCE_*` 仅作兼容 alias。用户人工审阅并明确批准具体 patch 后，将最高 authority、冲突处理、后端变量命名和 `.claude/worktrees/` 约定落实到 `PROJECT_BASIS.md`。验证：active spec 元数据审计与命名冲突扫描通过，`git diff --check` 和 VitePress production build 通过。

2026-07-30 18:09 CST changelog：完成三环境部署边界与 production immutable release 收口：保持 `scripts/dev.sh` 本地无 Docker 开发模式及默认 development staging 热重载不变；为 production 增加按同一 Git release ID 构建 API、Web、Prometheus、Grafana 镜像及 mode-0600 release manifest 的统一入口，API、domain worker、literature worker 固定复用同一 API image，WebUI 与 nginx/监控配置全部烘焙入镜像，CPU production Compose 删除源码、host frontend dist 和配置 bind mount，并让旧前后端 production redeploy 命令统一委托整套发布入口。同步当前架构、部署规则和用户文档，新增 dirty-build fail-closed、无 repository mount、同版本 worker 与 build-before-deploy 契约测试；未启动、重启、读取或修改任何 production 容器、数据、日志或端口。验证：production Compose render、四个 Docker target `--check`、shell syntax、定向 deployment tests 和完整 L1 通过；L1 仅保留 3 条既有 Ty unused-ignore warning与 TodayPage React act warning；关联提交：本提交 `refactor: make production releases immutable`。

2026-07-30 19:10 CST changelog：完成实验室内部部署模型的文档同步：依据用户人工更新的 `PROJECT_BASIS.md`，将 L4、当前架构、Docker 部署说明和仓库级 Agent 规则统一收敛为允许约 2–3 小时停机的简单维护窗口流程；保留完整备份与隔离恢复验证、版本一致的不可变制品、只读 smoke 和可执行人工 rollback，明确独立 release staging、自动回滚、复杂 ledger 与高保证供应链证明均非默认要求，并修正过时的 production 部署命令。本批仅修改文档，未操作 production；`git diff --check`、重型发布默认承诺扫描和 VitePress production build 通过。
