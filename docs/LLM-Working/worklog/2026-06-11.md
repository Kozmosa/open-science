# 2026-06-11

## Markdown Streaming Performance Fix (Phase 1-3)

**14:30 — Phase 1: Frontend rendering optimizations**

- `SafeMarkdown`: wrapped `marked.parse()` in `useMemo([content])` + `React.memo()`
- All 7 message block components wrapped in `React.memo()`
- `writeCache` debounced to 300ms, flushed on unmount/task switch
- Files: `MessageBlocks.tsx`, `useTaskOutputStream.ts`

**14:50 — Phase 2: Frontend data flow optimizations**

- Incremental `convertOutputEventsToMessages` via `lastProcessedSeqRef` — only processes new events
- New `pruneSupersededDeltas()` in `output.ts` — keeps only latest event per `block_id`, drops superseded deltas
- Pruning applied to `appendOutput`, cache load, and initial page load paths
- Files: `output.ts`, `useTaskMessages.ts`, `useTaskOutputStream.ts`

**15:10 — Phase 3: Backend root cause fix**

- `agent_sdk.py:_convert_stream_event` now sends delta text (not accumulated) in `content_block_delta`, adds `is_delta: true`
- `content_block_stop` still sends full accumulated text with `is_partial: false`
- Frontend merge logic: `is_delta: true` → append content; absent → replace (backward compatible)
- `tasks.py`: new `_filter_superseded_deltas()` filters superseded partial events on historical fetch and SSE stream
- Files: `agent_sdk.py`, `useTaskMessages.ts`, `output.ts`, `types/index.ts`, `tasks.py`

**15:30 — Performance benchmark suite**

- Unit benchmarks: `__tests__/perf/markdown-streaming.perf.ts` — 12 tests measuring `marked.parse`, `pruneSupersededDeltas`, `convertOutputEventsToMessages`, `mergeOutputItems`, storage comparison
- Integration benchmarks: `__tests__/perf/streaming-render.perf.tsx` — 8 tests measuring memoization, streaming pipeline, batch processing
- All 20 benchmarks pass
- Key results:
  - Storage: 100K chars accumulated = 1032 MB → delta = 1.9 MB (0.18%)
  - Convert speed: delta 7.5ms vs accumulated 323ms (43x faster) for 50K chars
  - Prune: 10002 events → 2 events after final block
  - Memo: same ref re-render → 1 DOM container maintained
- Reports saved to `.cache/perf-report/2026-06-11/`
- Exported 3 pure functions from `useTaskMessages.ts` for testability
- Updated `vitest.config.ts` to include `*.perf.{ts,tsx}` pattern

**Verification**: type-check clean, 458 backend tests pass, 34/36 frontend test files pass (3 pre-existing failures unchanged)
