2026-08-11 00:03 CST changelog：根据 reviewer 反馈修复 Task Fork preview/confirm 的两个 stale/idempotency 边界：MSW preview/confirm 现在按 scope、Idempotency-Key 与规范化 source/payload 重放相同 receipt，并对 key 重用时的 source 或 payload 变化返回 409；TasksPage confirm 绑定 preview.source_task_id，Task 列表切换会失效旧 preview，避免 A→B 后向 B 发送 A preview。补充 endpoint/MSW replay/conflict 与页面切换回归；保留 legacy route、typed transport 和原 Conventional Commit 主题。验证：transport drift、全量 Vitest（358 passed）、frontend lint/build、fork-focused pytest（5 passed）、Ruff/format、Ty、dependency/instruction checks、docs-site build 与 git diff --check 均通过；本次 amend 不写入未来 SHA。
2026-08-11 00:29 CST changelog：根据 r2 reviewer 反馈收紧 fork MSW 的幂等 key 边界：preview/confirm 对缺失或空白 `Idempotency-Key` 在任何 preview、transfer、Task counter 状态变化前返回 backend 对齐的 409；保留同 key replay 与 changed source/payload conflict，并通过 legacy wrapper 保持其他 mock mutation 的既有行为。补充 endpoint/mock missing+blank preview/confirm 与状态不增长回归；前一真实提交为 `f339629 feat: migrate task fork confirmation flow`，最终 amend SHA 见交付报告。验证：transport drift、全量 Vitest（359 passed）、frontend lint/build、fork-focused pytest（5 passed）、Ruff/format、Ty、dependency/instruction checks、docs-site build 与 git diff --check 均通过。
2026-08-11 00:39 CST changelog：在 Conversation admission 深模块统一校验实际 Workspace Environment 的显式 active durable execution grant，覆盖 create_task、create/next/retry Turn 与 fork confirm；保留完全相同幂等 replay 在撤销后的 durable receipt 语义，并让 worker 在 adapter start 前再次 fail-closed 校验，移除 Environment owner 特例。新增共享只读 grant 查询与 Conversation/service/worker/fixture 回归，覆盖缺失/撤销零写入、changed-payload 冲突、target fork revoke 和 owner revoke 不启动 adapter。focused Conversation/API/auth pytest、受限并发 unit/engine/api lanes、Ruff、Ty、dependency/instruction checks 与 git diff --check 通过；L0 backend fast 通过，L0 transport check 因该 worktree 缺少 frontend/node_modules/.bin/openapi-ts（ENOENT）未完成；首次完成提交：`2fdb540 fix: enforce conversation environment grants`；最终 amend SHA 见交付报告。
2026-08-11 01:07 CST changelog：基于前一真实提交 `bd1ec0b feat: migrate task fork confirmation flow` 修复 TasksPage Fork preview/confirm 的 late callback race：请求快照携带单调 flow、request、source 与 preview identity，切换 Task、关闭、Back、重新打开或启动新请求后旧成功/错误回调只结束 mutation，不再覆盖当前 dialog 或导航；confirm 仅在 invalidate 后再次通过 live identity 检查才关闭并选择目标 Task。新增 deferred Promise 页面回归，覆盖 preview 切换/关闭、URL A→B→A 清理及 confirm 切换/关闭。验证：TasksPage focused Vitest（35 passed）、全量 Vitest（364 passed）、frontend lint、transport drift/type-authority 与生产 build、git diff --check 均通过；最终 amend SHA 见交付说明。
2026-08-11 01:14 CST changelog：按 reviewer r1 修正 CREATE_TASK replay 的安全顺序，先解析已存 Task 并校验当前 owner，再区分相同 payload replay 与 changed payload conflict；环境 execution grant denial 统一记录 durable permission telemetry，并新增 service/API ownership-transfer 回归及 missing/revoked、Turn admission、fork confirm 的单次计数断言。Conversation/API/worker/execution、auth/telemetry/runtime focused pytest 与 L1 Python/backend deterministic gate 通过；L1 transport check 仍因该 worktree 缺少 frontend/node_modules/.bin/openapi-ts（ENOENT）未完成；最终 amend SHA 见交付报告。
2026-08-11 01:33 CST changelog：按 reviewer r2 为共享 Environment grant helper 增加 regular-main-file、SQLite header、必要 schema 与连接前后文件签名验证，保留正常 WAL 中最新 grant/revoke 的读取；新增 stale-WAL 下主文件损坏、短 header、非 SQLite、缺 schema 与 Conversation 零 submission/单次 telemetry 回归。相关 focused pytest 131 passed，Ruff/Ty/dependency/instruction checks 与 L1 backend deterministic gate 通过；L1 transport check 仍因该 worktree 缺少 frontend/node_modules/.bin/openapi-ts（ENOENT）未完成。header/connect 间仍存在无法跨 main+WAL 文件原子化消除的残余 TOCTOU，代码只对常规路径替换做前后签名拒绝；最终 amend SHA 见交付报告。
2026-08-11 01:44 CST changelog：基于前一真实提交 `fee2c9d feat: migrate task fork confirmation flow` 完成 Fork r4/r5 收口：MSW transcript revision 现按 formal Turn/Item 内容重算并在 confirm 前执行 stale guard，preview expiry 与 backend 的 900 秒有效期一致且按当前时间生成，confirm 明确拒绝过期 preview；unchanged confirm replay 保持单一 target/edge/transfer receipt；TasksPage preview/confirm callback 额外绑定 history-entry `location.key`，覆盖同一 act 内 A→B→A 的 late success/error，且 confirm 幂等语义不再受 UI location identity 影响。补充 endpoint、页面与 MSW 状态不增长回归。验证：focused 与全量 Vitest（64 files、370 passed）、transport drift/type-authority、frontend lint/build、fork-focused pytest（5 passed）、Ruff/format、Ty、dependency/instruction checks、docs-site build 与 git diff --check 均通过；最终 amend SHA 见交付报告。
2026-08-11 02:43 CST changelog：基于前一真实提交 `289865e feat: migrate task fork confirmation flow` 修复 r6 review 发现的 Fork relationship mock/backend 漂移：保持 Conversation Module 对 `derived_from` 的正式语义（新 Task/source derived from 原 Task/target），让 MSW confirm edge 与真实持久化方向一致，并在 frontend endpoint 与真实 HTTP contract 测试中固定该方向，避免 ProjectCanvas 在 mock 与 backend 下绘制相反关系。验证：endpoint focused Vitest（16 passed）、fork-focused pytest（6 passed）、frontend 全量 Vitest（64 files、370 passed）、transport drift/type-authority、frontend lint/build 与 git diff --check 均通过；最终 amend SHA 见交付报告。
