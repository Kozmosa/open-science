2026-08-11 00:03 CST changelog：根据 reviewer 反馈修复 Task Fork preview/confirm 的两个 stale/idempotency 边界：MSW preview/confirm 现在按 scope、Idempotency-Key 与规范化 source/payload 重放相同 receipt，并对 key 重用时的 source 或 payload 变化返回 409；TasksPage confirm 绑定 preview.source_task_id，Task 列表切换会失效旧 preview，避免 A→B 后向 B 发送 A preview。补充 endpoint/MSW replay/conflict 与页面切换回归；保留 legacy route、typed transport 和原 Conventional Commit 主题。验证：transport drift、全量 Vitest（358 passed）、frontend lint/build、fork-focused pytest（5 passed）、Ruff/format、Ty、dependency/instruction checks、docs-site build 与 git diff --check 均通过；本次 amend 不写入未来 SHA。
2026-08-11 00:29 CST changelog：根据 r2 reviewer 反馈收紧 fork MSW 的幂等 key 边界：preview/confirm 对缺失或空白 `Idempotency-Key` 在任何 preview、transfer、Task counter 状态变化前返回 backend 对齐的 409；保留同 key replay 与 changed source/payload conflict，并通过 legacy wrapper 保持其他 mock mutation 的既有行为。补充 endpoint/mock missing+blank preview/confirm 与状态不增长回归；前一真实提交为 `f339629 feat: migrate task fork confirmation flow`，最终 amend SHA 见交付报告。验证：transport drift、全量 Vitest（359 passed）、frontend lint/build、fork-focused pytest（5 passed）、Ruff/format、Ty、dependency/instruction checks、docs-site build 与 git diff --check 均通过。
2026-08-11 00:39 CST changelog：在 Conversation admission 深模块统一校验实际 Workspace Environment 的显式 active durable execution grant，覆盖 create_task、create/next/retry Turn 与 fork confirm；保留完全相同幂等 replay 在撤销后的 durable receipt 语义，并让 worker 在 adapter start 前再次 fail-closed 校验，移除 Environment owner 特例。新增共享只读 grant 查询与 Conversation/service/worker/fixture 回归，覆盖缺失/撤销零写入、changed-payload 冲突、target fork revoke 和 owner revoke 不启动 adapter。focused Conversation/API/auth pytest、受限并发 unit/engine/api lanes、Ruff、Ty、dependency/instruction checks 与 git diff --check 通过；L0 backend fast 通过，L0 transport check 因该 worktree 缺少 frontend/node_modules/.bin/openapi-ts（ENOENT）未完成；首次完成提交：`2fdb540 fix: enforce conversation environment grants`；最终 amend SHA 见交付报告。
2026-08-11 01:07 CST changelog：基于前一真实提交 `bd1ec0b feat: migrate task fork confirmation flow` 修复 TasksPage Fork preview/confirm 的 late callback race：请求快照携带单调 flow、request、source 与 preview identity，切换 Task、关闭、Back、重新打开或启动新请求后旧成功/错误回调只结束 mutation，不再覆盖当前 dialog 或导航；confirm 仅在 invalidate 后再次通过 live identity 检查才关闭并选择目标 Task。新增 deferred Promise 页面回归，覆盖 preview 切换/关闭、URL A→B→A 清理及 confirm 切换/关闭。验证：TasksPage focused Vitest（35 passed）、全量 Vitest（364 passed）、frontend lint、transport drift/type-authority 与生产 build、git diff --check 均通过；最终 amend SHA 见交付说明。
2026-08-11 01:14 CST changelog：按 reviewer r1 修正 CREATE_TASK replay 的安全顺序，先解析已存 Task 并校验当前 owner，再区分相同 payload replay 与 changed payload conflict；环境 execution grant denial 统一记录 durable permission telemetry，并新增 service/API ownership-transfer 回归及 missing/revoked、Turn admission、fork confirm 的单次计数断言。Conversation/API/worker/execution、auth/telemetry/runtime focused pytest 与 L1 Python/backend deterministic gate 通过；L1 transport check 仍因该 worktree 缺少 frontend/node_modules/.bin/openapi-ts（ENOENT）未完成；最终 amend SHA 见交付报告。
2026-08-11 01:33 CST changelog：按 reviewer r2 为共享 Environment grant helper 增加 regular-main-file、SQLite header、必要 schema 与连接前后文件签名验证，保留正常 WAL 中最新 grant/revoke 的读取；新增 stale-WAL 下主文件损坏、短 header、非 SQLite、缺 schema 与 Conversation 零 submission/单次 telemetry 回归。相关 focused pytest 131 passed，Ruff/Ty/dependency/instruction checks 与 L1 backend deterministic gate 通过；L1 transport check 仍因该 worktree 缺少 frontend/node_modules/.bin/openapi-ts（ENOENT）未完成。header/connect 间仍存在无法跨 main+WAL 文件原子化消除的残余 TOCTOU，代码只对常规路径替换做前后签名拒绝；最终 amend SHA 见交付报告。
2026-08-11 01:44 CST changelog：基于前一真实提交 `fee2c9d feat: migrate task fork confirmation flow` 完成 Fork r4/r5 收口：MSW transcript revision 现按 formal Turn/Item 内容重算并在 confirm 前执行 stale guard，preview expiry 与 backend 的 900 秒有效期一致且按当前时间生成，confirm 明确拒绝过期 preview；unchanged confirm replay 保持单一 target/edge/transfer receipt；TasksPage preview/confirm callback 额外绑定 history-entry `location.key`，覆盖同一 act 内 A→B→A 的 late success/error，且 confirm 幂等语义不再受 UI location identity 影响。补充 endpoint、页面与 MSW 状态不增长回归。验证：focused 与全量 Vitest（64 files、370 passed）、transport drift/type-authority、frontend lint/build、fork-focused pytest（5 passed）、Ruff/format、Ty、dependency/instruction checks、docs-site build 与 git diff --check 均通过；最终 amend SHA 见交付报告。
2026-08-11 02:43 CST changelog：基于前一真实提交 `289865e feat: migrate task fork confirmation flow` 修复 r6 review 发现的 Fork relationship mock/backend 漂移：保持 Conversation Module 对 `derived_from` 的正式语义（新 Task/source derived from 原 Task/target），让 MSW confirm edge 与真实持久化方向一致，并在 frontend endpoint 与真实 HTTP contract 测试中固定该方向，避免 ProjectCanvas 在 mock 与 backend 下绘制相反关系。验证：endpoint focused Vitest（16 passed）、fork-focused pytest（6 passed）、frontend 全量 Vitest（64 files、370 passed）、transport drift/type-authority、frontend lint/build 与 git diff --check 均通过；最终 amend SHA 见交付报告。
2026-08-11 00:11 CST changelog：继续返工 Literature external-call durable attempt：将 provider response 已接受后的 reporter/observability 异常隔离为本地 telemetry warning，禁止 batch 降级 single 或二次外调；移除历史 ETag fingerprint 候选，仅允许当前完整请求 fingerprint，非终态 response evidence 只作为恢复证据；补齐两条精准回归。验证：Literature/API/migration/metrics focused pytest（通过）、Ruff check/format、Ty、dependency-direction、agent-instruction checker 与 `git diff --check` 通过；frontend transport drift 与 docs-site build 通过（临时复用主工作区 node_modules symlink，随后已移除且未提交 node_modules）。关联提交：本次唯一提交（最终 SHA 由交付报告记录）。
2026-08-11 00:39 CST changelog：完成 reviewer r5 amend：将 Anthropic batch/single 的完整 Messages 请求身份（base URL、`/v1/messages`、API version、model、`max_tokens`、完整 messages content）纳入 canonical fingerprint，并补齐参数变更新 attempt 与全参数一致 durable replay 回归；修正 `asyncio.CancelledError` 先于其他 `BaseException` 分类，按 cancelled unknown fence 持久化后传播原异常，同时保留 KeyboardInterrupt 的 request_uncertain contract。验证：Literature/API/migration/metrics focused pytest（117 passed，新增回归通过）、Ruff check/format、Ty、dependency-direction、agent-instruction checker、`git diff --check`、frontend transport/lint/Vitest/build 与 docs-site build 全部通过；临时复用主工作区 node_modules symlink，随后已移除。关联先前提交：`8937f910 feat: persist literature external call attempts`；本次为 reviewer amend，最终 SHA 见交付报告。
2026-08-11 01:17 CST changelog：完成 reviewer r6 amend：durable Anthropic batch 对无效 JSON/缺失论文持久化 definitive_failure 并禁止回退 single；隔离 response-return 边界的取消、处理与未知异常，补齐 response evidence replay 晋级及迁移 guard 事务一致性；统一 RSS category/validator 归一化以保持 provider/worker fingerprint 一致。验证：相关 Literature/API/migration pytest（126 passed）、Ruff check/format、Ty、dependency-direction、agent-instruction checker、`git diff --check`、frontend transport/lint/Vitest（342 passed）/build 与 docs-site build 全部通过；临时复用主工作区 node_modules symlink，随后已移除。关联先前提交：`fd8cff8 feat: persist literature external call attempts`；本次为 reviewer amend，最终 SHA 见交付报告。
2026-08-11 02:07 CST changelog：完成 reviewer r7 amend：provider await 后的 durable/metrics secondary failure 不再触发 batch→single，普通 telemetry Exception 仅 warning，CancelledError/KeyboardInterrupt 等 BaseException 保留原样传播；matching started attempt 的 fence 失败保持 recovery-block；RSS checking 不再放宽 ETag/Last-Modified identity，并对 response payload、304 evidence 与 snapshot body/hash 做一致性校验，篡改证据 fail-closed 且 reconciliation 不误晋级；补齐 provider secondary、取消/中断、validator 变化、payload/hash/snapshot 篡改回归。验证：Literature/API/migration focused pytest（140 passed）、Ruff check/format、Ty、dependency-direction、agent-instruction checker、`git diff --check`、frontend transport/lint/Vitest（342 passed）/build 与 docs-site build 全部通过；临时复用主工作区 node_modules symlink，随后已移除。关联先前提交：`222070e feat: persist literature external call attempts`；本次为 reviewer amend，最终 SHA 见交付报告。
2026-08-11 02:53 CST changelog：更正 02:07 条目后续仍被 reviewer r7 阻断的状态，并基于前一真实提交 `3cd023d feat: persist literature external call attempts` 完成实际收口：provider failure 的 durable/metric secondary fault 保持为禁止 batch→single 的 external-call fence，CancelledError/KeyboardInterrupt 原对象传播；RSS snapshot 在 `checking` 期间保留请求所用 validators，recovery 同时精确绑定 attempt/snapshot/current request fingerprint；304 recovery 强制验证 `response_hash == sha256(response_payload)`，缺失或篡改 hash 均 fail-closed。新增不同 fingerprint checking snapshot、snapshot fingerprint 篡改与缺失 304 hash 回归。验证：durable-attempt/summarizer focused tests、全部相关 Literature + migration tests、4-worker unit lane、Ruff check/format、Ty 与 `git diff --check` 均通过；最终 amend SHA 见交付报告。
2026-08-11 03:09 CST changelog：将 files list/read/stream/upload、terminal session read/create/delete/reset/exec、session-pair listing 与 attachment WebSocket 的 runtime I/O 权限统一收口到显式 active Environment execution grant，保持不可见 Environment 为 404、可见但无 grant 为 403 并记录统一 permission telemetry，owner/admin 不再从可见性隐式获得执行权。补充 attachment user/Environment identity、runtime Environment exact-ID、auth authority fail-closed 与 WebSocket PTY 前 grant 重校验；HTTP mutation seam 在 tmux/detach/exec 外部边界再次校验 grant，session-pair 二次解析漂移被过滤，纯 attachment lookup 不再提前停止 expired runtime。focused 权限/API/terminal pytest 85 passed，完整 API 与 engine lanes、Ruff format/check、Ty、dependency/instruction checks 和 git diff --check 通过；首次完成提交：`c3c59f0 fix: enforce runtime I/O environment grants`；最终 amend SHA 见交付报告。
2026-08-11 03:28 CST changelog：在 Conversation acceptance 深模块与 runtime Adapter seam 正式接线 `EngineConversationBinding`：Adapter 仅在 native conversation + Turn identity 均完整时开放 acceptance，worker 暂存此前 engine events；同一 acceptance 事务创建/复用 binding、按 native conversation 变化单向 supersede，并把同一 binding 写入 canonical Turn 与 RuntimeExecution，delivered replay 可验证已 supersede 的历史 binding 且 runtime 冲突会整体回滚。Codex 新 Turn 启动前清除 checkpoint 中的历史 native Turn，避免 thread 已恢复但新 Turn 尚未开始时错误接受旧 identity。focused Conversation/repository/worker/adapter pytest 100 passed，4-worker engine lane、Ruff format/check、Ty、dependency/instruction checks 与 git diff --check 通过；engine lane 首轮曾出现一次既有 corrupt-auth WebSocket 用例的间歇性 4503，目标用例隔离重跑及完整 lane 重跑均通过，保留为后续全仓审计观察项。首次完成提交：`3b93d3d feat: bind runtime executions to engine conversations`；最终 amend SHA 见交付报告。
2026-08-11 04:04 CST changelog：将 Task summary status 的唯一 authority 收口到 Conversation projection：定义精确 `queued/running/succeeded/failed/cancelled/completed` contract，保持 Turn 成功与 `work_status` 正交，Task list/detail/health、Project/Workspace activity、Overview、usage telemetry 均不再读取 legacy shadow；migration 036 在确认所有既存 Task 已有 Conversation authority/state 后移除 `tasks.status` 及旧索引，否则 fail-closed。generated transport 现在携带精确 union，frontend 直接复用 transport 类型并删除宽 union/cast、旧状态样式与 mock 漂移。验证：migration pytest 29 passed、受影响后端综合 pytest 202 passed、4-worker API 与 unit lanes、完整 frontend Vitest 370 passed、transport drift/type-authority、frontend lint/build、Ruff format/check、Ty、dependency/instruction checks、L0 与 `git diff --check` 均通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`65044a8 fix: make conversation task status authoritative`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 04:16 CST changelog：删除无生产调用方的旧 Literature `LiteratureService`、APScheduler `LiteratureScheduler`、fetcher/arXiv client cluster 及其专用测试和无 producer Prometheus series，移除 API app 的旧 state/lifespan wiring 与 APScheduler 依赖；保留现行 arXiv provider 所需 `arxiv` 依赖、`LiteraturePaper`、旧 SQLite 表、tracking/planner/work/task saga 以及已批准的 compatibility HTTP routes。更新 metrics 文档以只描述当前 durable planner/worker authority。验证：caller/import 扫描无旧实现引用，Literature/API/metrics/observability/overview focused pytest 166 passed，4-worker API 与 unit lanes、Ruff format/check、Ty、dependency/instruction checks、完整 L0（含 transport drift、frontend lint 与 370 Vitest tests）及 `git diff --check` 均通过；L0 首次仅因新 worktree 未安装前端依赖缺少 `openapi-ts` 而中止，`npm ci` 后重跑通过。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`d0769f4 refactor: retire legacy literature fetch cluster`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 04:29 CST changelog：清理无 caller 的 `sync_legacy_topic()` 与 `literature_topics.legacy_subscription_id` 双写残留，同时保留已批准的 Literature compatibility HTTP operations；兼容 transport 现在明确直接把 canonical `topic_id` 呈现为 `subscription_id`。新增 literature migration 010，通过事务内暂存并重建 topics/topic-matches 移除 UNIQUE legacy 列，保持 topic、match、外键和 canonical schema 等价；迁移 DDL 失败会回滚数据、临时表与 schema version，随后可安全重试。验证：migration pytest 31 passed、全部相关 Literature/API/domain focused pytest 165 passed、4-worker API 与 unit lanes、Ruff format/check、Ty、transport drift/type-authority、完整 L0（含 frontend lint 与 370 Vitest tests）及 `git diff --check` 均通过；首次 transport 检查仅因新 worktree 缺少 `openapi-ts` 中止，`npm ci` 后重跑通过。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`85f7ebf refactor: retire legacy literature topic mapping`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 04:48 CST changelog：经生产 caller、frontend caller 与 runtime Adapter Interface 审计确认 Runtime Approval route/table 只有孤立状态写入、没有 producer，也无法把用户 decision 送回 provider；删除误导性的 Conversation approval HTTP/schema/service/repository/contract surface，并从 fresh agentic baseline 移除 `runtime_approval_requests`。新增 agentic migration 037：旧表不存在时 no-op，空表安全删除，非空表 fail-closed 拒绝静默丢数据；保留 Adapter 现有自动处理 provider approval 的语义，以及仍用于引擎状态翻译和通用 typed item vocabulary 的 approval 状态词汇。重新生成 transport。验证：Ty、Conversation/migration/worker/harness/API focused pytest 221 passed、4-worker API/unit/engine lanes、transport drift/type-authority、frontend lint、完整 Vitest 370 passed、production build、完整 L0 与 `git diff --check` 均通过；API lane 首轮出现一次 Environment grant fixture 的既有并行偶发，目标用例隔离重跑及完整 API lane 重跑均通过。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`e80f9ca refactor: retire dead runtime approval surface`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 05:04 CST changelog：将 Conversation worker 的 durable delivery Seam 从 claim 后提前标记改为紧贴 Runtime Adapter 外调边界：execution context、adapter 构造与最终 Environment grant preflight 均成功后，才由 `begin_delivery()` 原子校验 Task open 并执行 `claimed → delivering`。确定性 setup failure、pre-start cancellation 与 grant authority 暂时不可用现在保持为可回收 `claimed`，复用既有 stale-claim 30 秒退避和同一 submission identity 安全重试；Adapter 已被调用但 provider acceptance 未证明、worker crash 或 acceptance race 的 `delivery_unknown` 防重复外调语义保持不变。删除 Seam 移动后无 caller 的 `ensure_submission_open()`，避免重复 Task-open authority。新增 context/adapter setup 重试、pre-start cancellation、grant revoke 零 adapter start 与 stale reclaim 回归。验证：worker final pytest 28 passed、Conversation worker/execution/repository/service/adapter focused pytest 89 passed、4-worker engine 与 unit lanes、Ruff format/check、Ty、完整 L0（含 backend fast、transport drift/type-authority、frontend lint 与 370 Vitest tests）及 `git diff --check` 均通过。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`32399d9 fix: retry deterministic worker setup failures`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 05:32 CST changelog：把已持久化但此前只写不读的 Environment `max_concurrent_tasks` 正式接入 Conversation worker 的 durable delivery Seam：worker 在最终 grant revalidation 后把 authoritative quota 传给 `begin_delivery()`，该深 Module 在同一 `BEGIN IMMEDIATE` 事务内按“用户 + Environment”的不同 Task 统计并预留外部执行槽；`null` 不限、`0` 禁止新外调，queued/claimed 不占用，`delivering`、`delivery_unknown` 与 active RuntimeExecution 占用，同一 Task 不得重复持槽。grant authority 缺失、schema 损坏、负值或类型损坏均 fail-closed；admin HTTP round-trip、generated transport、设置页数字输入与公开文档同步真实配额语义。新增 WAL grant、损坏 quota、API/auth round-trip、active Task 去重、零容量、同 Task 双 delivery 与双 dispatcher 原子 reservation 回归。验证：focused pytest 70 passed，4-worker API/middleware/engine/unit lanes、Ruff、Ty、transport drift/type-authority、frontend lint、完整 Vitest 370 passed、frontend production build、docs-site build、完整 L0 与 `git diff --check` 均通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`8dab8f3 fix: enforce environment task capacity`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 05:35 CST changelog：当前会话 whole-commit review 要求 amend 同一 quota 提交：补齐 `EnvironmentAccessResponse` 的非负 OpenAPI contract，前端改用 `Number` 保留小数输入并让 backend schema 明确拒绝、避免 `parseInt` 静默降额，同时更新 worker 冲突注释以准确覆盖容量、同 Task 与 cancellation 三种 reclaim/terminal 结果。关联先前真实提交：`a7cd624 fix: enforce environment task capacity`；最终 amend SHA 见交付报告。
2026-08-11 05:49 CST changelog：全量 L1 审计发现 closed-world frontend fixture engine 在 EngineConversationBinding 接线后没有 acceptance identity，导致 engine events 一直缓冲、submission 被错误 fence 为 `delivery_unknown`，同时 fixture worker cycle 仍报告 processed/completed。新增 development-only `FrontendFixtureRuntimeAdapter`，只在 synthetic Seam 暴露稳定的 fixture conversation/Turn identity，不为任何生产 engine 发明 Task/Turn fallback；回归同时固定 active binding 与 canonical Turn/RuntimeExecution 的 binding 一致性。验证：frontend fixture focused tests 4 passed、cli marker 133 passed/1 skipped、4-worker engine lane、Ruff、Ty、完整 L1（backend all、transport drift/type-authority、frontend lint、370 Vitest tests、production build、docs-site build）与 `git diff --check` 均通过；首次 L1 仅因新 worktree 缺少 docs-site dependencies 在最后一步退出 127，按锁文件安装后完整重跑成功。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`abe59f2 fix: bind frontend fixture runtime identity`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 06:01 CST changelog：修复公开安全/可观测性 contract 与生产 file routes 的长期脱线：files list/read/stream/upload 在显式 active Environment grant 与 Workspace tenant-owner 校验通过后、实际 I/O 前调用统一敏感路径检测；`files.sensitive_path_access` 仅记录 basename、固定 pattern、user/Environment identity，同时写入按固定 pattern 分类的 `ainrf_files_sensitive_path_access_total`，不再保留无 producer 指标。补充四条正式 file route 的 audit/metric 回归并校正内部 metrics reference。验证：聚焦 file/security pytest 34 passed、4-worker middleware 与 API lanes、Ruff check/format、Ty 和 `git diff --check` 均通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`deff5f3 fix: audit sensitive file access`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 06:11 CST changelog：完成 Prometheus producer-consumer 全量对齐：删除从未有运行时 producer 的 terminal exec allow/deny、Environment update、已退役 Code-Server session 与 SQLite latency/slow-query 六条预注册 series，并同步移除空告警、Grafana 面板和 current metrics 文档；保留正式 domain permission/SQLite error telemetry。修复活跃 SSH 指标在隐私边界已聚合为 `target="all"`、consumer 却仍读取 `host` 的标签漂移，以及敏感路径面板读取不存在 `path` 标签的问题；example lockout reason 对齐真实 `locked`。新增告警/Dashboard 只能引用已注册指标、current docs 不得恢复退役 series、SSH/sensitive 标签一致性的回归。验证：metrics pytest 20 passed、4-worker unit lane、Ruff check/format、Ty、Dashboard JSON 与 Prometheus YAML 解析、docs-site build 和 `git diff --check` 均通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`1447708 refactor: retire orphan observability series`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 06:15 CST changelog：校正公开审计日志目录与真实 producer：移除不存在的 auth、Code-Server、files read/upload、legacy Task 与 SSH-field structlog 事件，只保留正式 terminal lifecycle 和 sensitive-file access 事件及其真实字段；明确 `component=audit` structlog、事务内 `domain_audit_events` durable ledger、Auth login metrics/lockout state 是不同 Interface。请求日志说明改为真实的稳定 route template 边界，不再声称 WebSocket 自动继承 HTTP request ID 或存在可安全接收任意凭据 payload 的全局自动脱敏器；安全检查命令同步只查询现存事件。验证：全量 current-doc caller/event 扫描无已删除事件残留，docs-site build、agent-instruction checker 与 `git diff --check` 通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`4110231 docs: correct audit event catalog`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 06:18 CST changelog：删除无任何生产、脚本、文档或 package export caller 的 `ainrf.security.redaction` 及其自证测试。该孤立模块从未接入 structlog processor 或 HTTP request logging，保留只会误示一个不存在的全局脱敏 authority；真实边界继续由稳定 route request logging、`domain_telemetry` 字段脱敏和 Conversation output redaction 分别拥有。验证：全仓 caller/export 扫描为零，4-worker middleware 与 unit lanes、Ruff check/format、Ty 和 `git diff --check` 均通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`81eb112 refactor: retire unused security redaction`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 06:23 CST changelog：删除已被 `prepare_workspace_skills()` runtime Adapter seam 取代、且无生产/export caller 的旧 `SkillInjectionService`、`.ainrf → .claude` sync/marker cluster、env placeholder resolver 及其专用测试；保留 Settings preview 仍使用的 `deep_merge_settings()`。现行 runtime 继续从 registry load dir 把选定 skill 以 tenant-safe symlink 挂载到 Workspace，不再保留复制 `.ainrf/settings.json`、tool manifest 或 backup 目录的平行 authority。验证：active code/current docs caller 扫描为零，skills/API/harness focused pytest 119 passed，4-worker unit/API/engine lanes、Ruff check/format、Ty 和 `git diff --check` 均通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`168e418 refactor: retire legacy skill injection`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 06:49 CST changelog：把 Task 选择的 skill 正式接入 Conversation worker 与 runtime Adapter Seam：worker 从 canonical `~/.ainrf_workspaces/default/skills` registry 解析 dependency-first 顺序，并在 durable delivery 与 Adapter 构造前完成 mount/cleanup preflight；缺失、损坏、disabled、循环、不安全 ID、source escape、Workspace real-path shadow 或 tenant `sudo -n -u` 操作失败均保持 submission 为可回收 `claimed`，不创建 RuntimeExecution。`skills.mount` 现在以严格 Interface 集中解析、挂载和 tenant-authoritative cleanup，两个 Claude Adapter 不再用 backend identity 清理 tenant symlink；Task picker 隐藏 manifest-disabled skills，但 Settings inventory 继续展示完整 metadata。验证：mount/harness/Conversation worker focused pytest 81 passed，Ruff check/format、Ty、transport drift/type-authority、frontend lint、全量 Vitest 371 passed、frontend production build、两次完整 L0 与 `git diff --check` 均通过。关联先前真实提交：`2fbf40d fix: wire selected skills into runtime`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 07:07 CST changelog：按深 Module/Adapter Seam 审计结果退休没有 runtime consumer 的 skill `settings_fragment`、skill-local MCP/hooks/allowed-agent 元数据及其 settings preview API/UI/mock/merge helper，保留真正驱动 Conversation runtime 的 identity、描述、依赖、inject mode、package 与 `SKILL.md`；旧 manifest 的额外键继续被 loader 兼容忽略。FastAPI schema 与 generated transport 同步收缩，设置页只呈现 runtime-backed metadata，并新增 preview 404、旧键忽略和 UI surface 回归。验证：skills/API focused pytest 37 passed、Ruff check/format、Ty、transport drift/type-authority、frontend lint、全量 Vitest 372 passed、frontend production build、完整 L0 与 `git diff --check` 均通过。关联先前真实提交：`a2f9e2d refactor: retire unsupported skill metadata`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 12:52 CST changelog：按 Settings 深 Module/Conversation runtime Seam 的 caller 审计结果，退休只有浏览器设置页 producer/self-consumer、从未进入正式 Task create/runtime 的本地 Task Configuration、Research Agent profiles、skill modes、LLM provider credentials、per-Environment Task templates 及 write-only Workspace 偏好；保留主题、字号、默认路由和被 Environment selector 实际消费的项目默认/最近 Environment。Settings document 升级到 v6，v1-v5 兼容读取后重写并清除上述凭据与脱离 runtime 的字段，同时修复旧 `containers` route 无法迁移到 `environments` 的判断顺序。删除空 `/api/settings/codex-defaults` compatibility route/schema/mock/client，重新生成 transport，并校正设置页、i18n 与公开文档。当前会话 whole-commit 自审 PASS。验证：focused Vitest 21 passed、全量 Vitest 360 passed、backend focused pytest 2 passed、Ruff check/format、Ty（修改前后端边界未再变化）、transport drift/type-authority、frontend lint/build、docs-site build、4-worker 完整 L0 与 `git diff --check` 通过；一次误用脚本默认 8 pytest workers 的 L0 在完成前中止且未计入，随后以显式 4-worker 限制重跑通过。关联先前真实提交：`5fcdf01 refactor: retire detached browser execution config`；最终 amend SHA 见交付报告。
2026-08-11 12:55 CST changelog：全仓 caller/export 审计确认 `ainrf.harness_engine.context` 与 `ainrf.harness_engine.events` 只是把 `base.py` 的现行 Interface 再次导出，package root 已直接解析到 `base.py`，active code、tests、scripts 与 current docs 均无这两个模块路径的 caller；删除两个孤立兼容文件，不改变 `ainrf.harness_engine` 的公开 exports。当前会话 whole-commit 自审 PASS。验证：harness/recovery/token focused pytest 65 passed、Ruff check/format、Ty、caller scan 与 `git diff --check` 通过。关联先前真实提交：`4ce9d68 refactor: retire unused harness reexport modules`；最终 amend SHA 见交付报告。
2026-08-11 13:00 CST changelog：修正 Conversation worker cycle 的可观察结果语义：`ConversationDispatcher.run_once()` 的真值只表示本轮 claim 被处理/尝试，确定性 context/grant/setup 失败仍可能保持可回收 `claimed`，因此 domain worker 和 frontend fixture 不再把真值谎报为 `completed`；cycle Interface 收紧为 `processed / idle / maintenance_drained`，fixture task outcome 收紧为 `processed / idle`，正式 Task/Turn/Runtime completion 继续只由 Conversation 持久化状态拥有。新增可回收 setup failure 映射为 processed 且零 RuntimeExecution 的回归。当前会话 whole-commit 自审 PASS。验证：Conversation worker/frontend fixture focused pytest 37 passed、CLI pytest 38 passed、Ruff check/format、Ty、caller/status scan 与 `git diff --check` 通过。关联先前真实提交：`7b4afc4 fix: report worker cycles as processed`；最终 amend SHA 见交付报告。
2026-08-11 13:04 CST changelog：校正公开架构文档的 current migration inventory：保留真实 fresh-install baseline anchors `agentic_researcher=33/auth=7/literature=7/terminal=1` 与一次性 legacy 32→33 维护边界，同时明确正常 startup 应用 forward migrations 后的最终支持版本为 `37/7/10/1`，并记录 agentic 034–037 与 Literature 008–010 的现行职责，避免把 Literature 错误停在 version 9 或遗漏最近的 authority retirement。当前会话 whole-commit 自审 PASS。验证：current-code/doc version scan、agent-instruction checker、docs-site production build 与 `git diff --check` 通过。关联先前真实提交：`4d6d8f8 docs: update current migration inventory`；最终 amend SHA 见交付报告。
2026-08-11 13:15 CST changelog：全仓 schema/caller 审计确认 `literature_research_task_links` 只有 completion-time mirror INSERT、没有 read/update/delete consumer；正式 create/list/replay/filter/recovery 全部由 `literature_research_task_intents` 拥有 Task relation。删除重复写和 fresh-baseline 表，新增 Literature migration 011：既有 link 行必须逐字段匹配 canonical completed intent（identity、user/paper/Task、payload、timestamps 与无错误状态）才允许删除，孤儿或漂移行 fail-closed 保留 version 10 与原表；active spec 和公开 architecture inventory 同步更新至 current Literature version 11。当前会话 whole-commit 自审 PASS。验证：migration pytest 35 passed、全部相关 Literature/API tests 145 passed、Ruff check/format、Ty、agent-instruction checker、docs-site production build、caller/schema scan 与 `git diff --check` 通过。关联先前真实提交：`f1bce89 refactor: retire duplicate literature task links`；最终 amend SHA 见交付报告。
2026-08-11 13:36 CST changelog：在继续开放式审计前，以 `OPENSCIENCE_PYTEST_WORKERS=4` 与 `OPENSCIENCE_VITEST_WORKERS=4` 对主工作区 `1e86d99` 运行完整确定性 L1；agent instruction、dependency direction、Ruff、format、Ty、backend all、generated transport drift/type-authority、frontend lint、360 Vitest tests、production build 与 docs-site build 全部通过，`git diff --check` 通过且主工作区保持 clean。L1 未启动 Docker，也未访问 production、staging、真实用户环境或 legacy `testing/e2e/`。
2026-08-11 13:36 CST changelog：按授权 Module/持久化 Interface 审计结果，退休只有 grant/revoke 双写和测试直读、没有产品/管理/观测 reader 的 `environment_access_audit_events`；`environment_access` 继续作为单一 authorization authority，保留单调 `grant_version`、active/revoked tombstone、actor、reason、quota 与时间信息。新增 auth migration 008，只有 event versions 从 1 连续到当前版本、没有孤儿且最新 event 与当前 authority 逐字段一致时才删除 ledger，任何漂移都保留 version 7 与原表；fresh baseline、权限测试和公开 migration inventory 同步更新。当前会话 whole-commit 自审 PASS。验证：migration/permissions focused pytest 59 passed、Ruff check/format、Ty 与 `git diff --check` 通过；worktree docs build 因未安装独立 docs-site dependencies 未启动，将在 merged state 使用主工作区依赖验证。关联先前真实提交：`00d3b51 refactor: retire write-only environment access events`；最终 amend SHA 见交付报告。
2026-08-11 13:42 CST changelog：完成 Environment access event retirement 的 merged-state 验证与清理：主工作区 migration/permissions focused pytest 59 passed、docs-site production build 与 `git diff --check` 通过，临时 worktree/local branch 已删除，仅保留指定 production-readiness worktree；最终合入提交由交付报告记录。
2026-08-11 13:42 CST changelog：caller/capability 审计确认 Task 列表的“AI 刷新名称”按钮只有空 `onClick` 与 coming-soon 文案，仓库不存在对应 HTTP、Conversation 或 runtime title-generation Interface；删除该可点击死 surface、图标和中英文文案，保留正式的 manual inline rename，并把页面回归改为断言不再暴露未实现 action。当前会话 whole-commit 自审 PASS。验证：TasksPage focused Vitest 38 passed、frontend dependency/lint、transport drift/type-authority、production build 与 `git diff --check` 通过；首次传入带 `frontend/` 前缀的 Vitest filter 未匹配用例，使用 frontend cwd 相对路径重跑后通过；临时 node_modules symlink 已删除。关联先前真实提交：`184c3cf refactor: retire dead task title action`；最终 amend SHA 见交付报告。
2026-08-11 13:47 CST changelog：完成 dead Task title action 的 merged-state 验证与清理：主工作区 TasksPage focused Vitest 38 passed、transport drift/type-authority、frontend production build 与 `git diff --check` 通过，临时 worktree/local branch 已删除；最终合入提交由交付报告记录。
2026-08-11 13:47 CST changelog：修复 file browser transport 类型 authority 漂移：领域 `FileKind` 已限定为 `file/directory/symlink`，HTTP `FileEntryResponse.kind` 现在直接复用该类型并重新生成 OpenAPI/TypeScript transport；frontend view model 从 generated `FileEntryResponse['kind']` 派生并删除 `as FileKind`，避免 backend 扩宽为任意字符串和前端手写 union 双重漂移。新增 transport generation 回归固定精确 union。当前会话 whole-commit 自审 PASS。验证：transport/runtime-access pytest 12 passed、transport focused pytest 5 passed、FileBrowserPage Vitest 2 passed、Ruff check/format、Ty、transport drift/type-authority、frontend dependency/lint、production build 与 `git diff --check` 通过；临时 node_modules symlink 已删除。关联先前真实提交：`40193c4 fix: preserve file kind transport union`；最终 amend SHA 见交付报告。
2026-08-11 13:51 CST changelog：完成 FileKind transport union 的 merged-state 验证与清理：主工作区 transport/runtime-access pytest 12 passed、两次 transport drift/type-authority、frontend production build 与 `git diff --check` 通过，临时 worktree/local branch 已删除；最终合入提交由交付报告记录。
2026-08-11 13:51 CST changelog：删除 Task create flow 对 `skillsQuery.data.items` 的 `as SkillItem[]` 类型逃逸；Settings Adapter 的 `getSkills()` 已正式返回归一化 `SkillListResponse`，Task picker 现在直接消费该 Interface，让未来 skill contract 漂移由 TypeScript build 暴露而不是被断言掩盖，同时移除只为 cast 存在的 import。当前会话 whole-commit 自审 PASS。验证：TaskCreateFlow Vitest 6 passed、frontend dependency/lint、transport drift/type-authority、production build 与 `git diff --check` 通过；临时 node_modules symlink 已删除。关联先前真实提交：`3db537b refactor: remove redundant task skill cast`；最终 amend SHA 见交付报告。
2026-08-11 13:58 CST changelog：将独立 Status 页面 localStorage 与 DOM dataset 的 locale/theme 输入统一收口到 status model 的解析 Interface，删除四处类型断言；合法值保持精确 union，非法或缺失值分别回退到 `en` 与 `system`，避免损坏 translations 索引或主题状态。新增纯函数回归覆盖合法、非法、null/undefined 输入；当前会话 whole-commit 自审 PASS。验证：status model Vitest 6 passed、transport drift/type-authority、frontend dependency/lint、production build 与 `git diff --check` 通过；临时 node_modules symlink 已删除。关联先前真实提交：`ec835ee fix: validate status page preferences`；最终 amend SHA 见交付报告。
2026-08-11 14:07 CST changelog：校正公开 Project/Workspace 文档与当前 Domain Interface、generated transport 和 WebUI 的长期漂移：明确 WebUI route 与 `/api` HTTP Interface 的区别，Project 通过 Workspace link/primary Workspace 获得执行上下文而非直接持有 Environment refs，Project PATCH 只修改名称/描述；Workspace 改为显式 Environment + canonical path 注册和 unregister 生命周期，删除内置默认 Workspace、自动路径与 DELETE 旧说法。Task 文档同步 Conversation status union、当前创建字段和显式 relationship，删除自动线性持久化边；文件浏览器同步 `/workspace-browser`、只读 Monaco、正式 file routes、grant/owner 权限与实际大小限制。当前会话 whole-commit 自审 PASS。验证：agent-instruction checker、docs-site production build、current route/schema/caller scan 与 `git diff --check` 通过；临时 docs-site node_modules symlink 已删除。关联先前真实提交：`36363ae docs: correct project and workspace guides`；最终 amend SHA 见交付报告。
2026-08-11 14:15 CST changelog：在合入 Status preference 校验与 Project/Workspace 公开文档修订后，对主工作区 `1fef702` 运行 4-worker 完整确定性 L1；agent instruction、dependency direction、Ruff、format、Ty、backend all、generated transport drift/type-authority、frontend lint、361 Vitest tests、frontend production build与 docs-site build 全部通过。未启动 Docker，也未访问 production、staging、真实用户环境或 legacy `testing/e2e/`。
2026-08-11 14:18 CST changelog：全仓 caller 审计确认 frontend `features/workspaces` 的 `uploadFile()`、专用 `FileUploadResponse` view model 与 adapter 没有任何 WebUI、测试或 export caller；删除这条未接线的浅 frontend wrapper，避免暗示 FileBrowserPage 已提供上传能力。后端 `/api/files/upload`、generated transport 与 MSW handler 继续作为独立、受权限保护的 HTTP Adapter Interface 保留。当前会话 whole-commit 自审 PASS。验证：无残余 frontend caller，FileBrowserPage/FileViewer Vitest 3 passed、transport drift/type-authority、frontend dependency/lint、production build 与 `git diff --check` 通过；临时 node_modules symlink 已删除。关联先前真实提交：`c8d249c refactor: remove unused file upload client`；最终 amend SHA 见交付报告。
2026-08-11 14:27 CST changelog：修复 `openscience login --server` 仍调用已退休 root alias `/auth/login` 的 CLI 漂移；登录命令现在规范化 server 末尾斜杠并调用 canonical `/api/auth/login` HTTP Interface，保持既有凭据 payload、10 秒 timeout 与 token 文件格式。新增 CLI smoke regression 固定 URL、payload、timeout 和持久化结果。当前会话 whole-commit 自审 PASS。验证：目标回归通过、完整 `tests/test_cli.py` 39 passed、Ruff check/format、Ty 与 `git diff --check` 通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`3746936 fix: use canonical API path for CLI login`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 14:33 CST changelog：补齐 `AGENTS.md` 已承诺但 `scripts/test.sh` 未实现的 `cli` backend lane；runner 现在通过现有 partition Interface 以受限 worker 并行运行普通 CLI tests，并将带 `concurrent`/`db_race` 的 CLI tests 保持 `-n 0` 串行，usage 与脚本头部说明同步。新增仓库脚本 smoke regression 固定两条 pytest 命令，避免 lane 再次从实现中消失。当前会话 whole-commit 自审 PASS。验证：目标 smoke regression 通过、实际 `OPENSCIENCE_PYTEST_WORKERS=4 bash scripts/test.sh cli` 全部通过（普通分区 135 passed，race 分区 1 skipped）、`bash -n`、Ruff check/format、Ty 与 `git diff --check` 通过；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`5d6fec1 fix: add CLI backend test lane`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 14:39 CST changelog：校正公开 auth/quickstart/settings 文档与当前 HTTP、Auth、Domain permission Interface 的漂移：所有产品 auth/admin/member 路径恢复 canonical `/api` prefix；初始 admin 改为真实的 24 字符随机密码、`<state_root>/admin_initial_password.txt` 与 `0600` 文件权限；Environment registry 可见性、admin/owner 身份与 explicit active execution grant 明确分离。Project 协作者改为正式 `editor/viewer` membership、独立 `can_publish` 与 `/api/domain/projects/.../members` Interface，并说明全局 admin 不能仅凭角色获得 runtime execution 或 Project publish 能力。同步修正 Settings tab 可见性与用户字段说明。当前会话 whole-commit 自审 PASS。验证：current code/generated transport/caller scan、agent-instruction checker、docs-site production build 与 `git diff --check` 通过；临时 docs-site dependency symlink 已自动移除。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`b971e5c docs: correct authentication and permission guides`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 14:46 CST changelog：修复 Project membership 在 Settings UI 的正式角色漂移：Collaborators badge/selector 不再把 canonical `editor` 显示为 legacy “Member/成员”，中英文统一为 “Editor/编辑者”。在 domain feature 增加共享 role parser Interface，Settings 与 Project settings 两个 DOM Seam 都显式拒绝 `member`、owner 与未知值，移除 `as 'viewer' | 'editor'` 类型逃逸；member upsert adapter 直接复用 view-model role 类型，集中角色 contract。当前会话 whole-commit 自审 PASS。验证：parser Vitest 2 passed、transport drift/type-authority、frontend dependency/lint、production build、残余 legacy label/cast scan 与 `git diff --check` 通过；临时 frontend dependency symlink 已自动移除。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`b0ef833 fix: align project editor role labels`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 14:52 CST changelog：在 CLI canonical login、CLI backend test lane、公开 auth/permission 文档与 Project editor role UI 修复全部合入后，对主工作区 `299efbd` 运行 4-worker 完整确定性 L1；agent instruction、Python dependency direction、Ruff、format、Ty、backend all、generated transport drift/type-authority、frontend dependency/lint、63 files / 363 Vitest tests、frontend production build与 docs-site build 全部通过。backend 仅出现既有 multiprocessing fork deprecation warning，无测试失败。未启动 Docker，也未访问 production、staging、真实用户环境或 legacy `testing/e2e/`；首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`2d55fd7 chore: record latest L1 validation`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
2026-08-11 15:08 CST changelog：按用户裁定退休 auth SQLite 的 legacy `project_collaborators` surface：删除无产品 caller 的 AuthService CRUD、自证 permissions tests、perf EXPLAIN 查询及 fresh baseline 表/索引，让 Domain `project_members` 保持唯一 membership authority。新增 auth migration 009，旧表缺失时 no-op、空表安全删除，任何非空历史 row 都 fail-closed 保留在 version 8，禁止在无法证明跨数据库等价时静默丢弃；公开 architecture inventory 更新至 auth current version 9。当前会话 whole-commit 自审 PASS。验证：auth migration focused 9 passed、完整 migration tests 40 passed、permissions 18 passed、4-worker unit lane、Ruff check/format、Ty、agent-instruction checker、docs-site production build、caller scan 与 `git diff --check` 全部通过；临时 docs-site dependency symlink 已自动移除。首次提交 SHA 将按提交后 amend 流程补记，最终 SHA 由交付报告提供。
关联先前真实提交：`e00bb5b refactor: retire legacy auth collaborators`；本次仅在同一 commit 中补记提交身份，最终 amend SHA 见交付报告。
