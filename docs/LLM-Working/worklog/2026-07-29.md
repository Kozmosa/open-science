# 2026-07-29 Worklog

2026-07-29 02:22 CST changelog：完成项目级 Matt Pocock skills 精简安装，新增 codebase-design、improve-codebase-architecture 和 handoff，并生成 skills-lock.json 以记录来源；执行 `npx skills@latest list --json` 验证，三个技能均以 project scope 被 Codex 发现。

2026-07-29 02:32 CST changelog：完成 OpenScience 架构审查报告的中英文发布，根目录新增英文原版与完整中文译版，涵盖 Task 控制平面、Project–Workspace 拓扑、Today Overview 刷新和 Task 运行时准入四项加深机会；已检查两份 HTML 均包含 Tailwind、Mermaid、四张候选卡与首要建议。

2026-07-29 12:27 CST changelog：完成 OpenScience 架构清理与深模块重构高层 spec，明确保留 monorepo，并将 v2 cutover、兼容观察和 Release E 技术债关闭纳入 P1 内部交付范围；定义后端依赖修正、深模块重构、transport contract 生成、前端分层收口和 P0–P6 退出条件；cleanup-only guard 集中在默认测试范围外的 `testing/architecture-cleanup/`，仅允许本地显式运行，禁止接入现有 CI 或 GitHub required checks，并要求 P6 整体删除；本地链接检查、`git diff --check` 与 VitePress production build 均通过；关联提交：本提交 `docs: define architecture cleanup refactor`。

2026-07-29 12:38 CST changelog：启动 P1-A Release E readiness，在独立 `refactor/release-e-p1` worktree 中建立本地-only 技术债 ledger 和单调收缩 guard，覆盖 authority mode、旧领域 Module、legacy persistence、HTTP contract、worker/runtime、deploy config 与旧测试文档七类债务；临时检查位于 `testing/architecture-cleanup/`，默认 pytest、L0/L1 和 GitHub CI 均不会发现；本地 guard 3 项通过，后端 L0、frontend lint 与 49 项 frontend tests 通过；关联提交：本提交 `test: freeze release e legacy surface`。

2026-07-29 13:01 CST changelog：完成 P1-D 的 Session authority 收口，删除可写 `SessionService`、旧 Session 数据模型与只验证 legacy SQLite writer 的测试，将 `/sessions` Interface 固定为 v2 Task/Attempt 只读投影并保留 legacy write telemetry/405 fence，同时删除前端未使用的 Session mutation contract 和 mock writer；Release E ledger 的 `SessionService` ceiling 已收缩到 0。隔离 cutover controller 测试、本地 cleanup guard、Session v2 route tests、后端完整 deterministic gate、frontend lint/test/build、managed dev smoke 均通过；docs-site 依赖补齐后 production build 通过；关联提交：本提交 `refactor: retire legacy session service`。

2026-07-29 13:18 CST changelog：完成 P1-D v2 authority 配置收口，`DomainModelMode` 只保留 v2，API 与 domain worker 直接由 committed cutover fuse 和 artifact SHA 决定可写性；删除本地开发、systemd 与全部 compose 文件中的 domain mode 环境变量及 legacy 默认值，并从 API 装配中移除 Project、Workspace、Session、Task legacy writer 的构造分支。cleanup guard、ruff、ty 和核心 v2 API/dev-stack 测试通过；关联提交：本提交 `refactor: make domain authority v2 only`。

2026-07-29 13:27 CST changelog：完成 P1-D 旧领域 writer Module 删除，移除 JSON-backed Project/Workspace registry、旧 AgenticResearcher task store/scheduler、legacy backfill 及其直接 facade/并发/token-streaming 测试；保留轻量兼容 record/error 类型供尚待收口的 read adapter 使用，并将注册流程固定为 v2 default Project provisioning。cleanup guard 中三个旧 Module ceiling 均收缩到 0，ruff、ty、cutover controller 和核心 v2 Domain/Task/resource adapter 测试通过；关联提交：本提交 `refactor: remove legacy domain writers`。

2026-07-29 13:39 CST changelog：完成 Environment 与 Workspace HTTP reader/writer 收口，两个 route Module 均只通过 ready `DomainService` Interface 读写，不再包含 InMemory/JSON registry fallback、owner filtering 分叉或文件删除语义；Workspace DELETE 固定为只注销控制面记录，Environment DELETE 固定为 disable。ruff、ty 和 v2 resource adapter/frontend/runtime-access 测试通过；关联提交：本提交 `refactor: remove legacy resource route fallbacks`。

2026-07-29 14:02 CST changelog：完成 Project 与 Task HTTP route 的 v2 authority 收口，删除 Project registry、AgenticResearcher task lifecycle 和 route-local legacy reader/fallback，所有 mutation 与 projection 统一跨越 Domain/Task application Interface；保留有 telemetry 的协议 alias，但其实现只委托 v2 canonical path。ruff、ty 以及 Project/Task/Session v2 契约测试通过；关联提交：本提交 `refactor: remove legacy project and task route fallbacks`。

2026-07-29 14:16 CST changelog：完成运行时授权 seam 与测试基线的 v2-only 收口，删除 `DomainModelMode` 配置对象及 file、terminal、literature 的 legacy fallthrough，将 runtime transport 依赖缩小为显式 `RuntimeDomainReader` Interface；通用 API 测试改用每 pytest worker 只构建一次的真实 committed-cutover 模板，删除旧 Environment/v1 CRUD 和 Agentic task stream 测试，以 v2 resource/Task 契约覆盖替代。后端全量并行与串行测试、ruff、ty、diff check 均通过；关联提交：本提交 `refactor: remove legacy runtime read fallthroughs`。

2026-07-29 14:24 CST changelog：完成 P1-D Release E 最终收口，将 Literature 计划与 outbox 发布并入 `TaskDispatcher` 已有的维护 lease，删除独立 planner CLI、进程和 deploy participant，使领域 worker 成为该写路径唯一 Module；移除 Task create 的 legacy profile 字段，并用本地 compatibility budget 明确保留的 v2-backed transport Adapter 责任人与 P4/P5 删除期限。Release E ledger 全部关闭；`bash scripts/ci.sh l1`、worktree 隔离 full-profile dev smoke（v2 contract）、本地 cleanup guard 3 项及 `git diff --check` 均通过；关联提交：本提交 `refactor: complete release e runtime cleanup`。

2026-07-29 14:30 CST changelog：完成 P1 逐条完成审计并修复遗漏，删除隔离 L2 harness 中已失效的 `OPENSCIENCE_DOMAIN_MODEL_MODE=v2` 注入；强化 local-only cleanup guard，要求所有 P1 debt 均为 closed、全部 evidence path 存在，并验证保留 transport Adapter 的 P4/P5 owner 与截止阶段。当前 managed dev cutover status 显示 committed v2 epoch、首笔 v2 write、immutable artifact、backup/source digest、零 blocker 和稳定 legacy seal；8 项 import/reconcile/backup/restore/cutover/fail-closed 定向测试、本地 guard 4 项、完整 L1、full-profile dev smoke 与 diff check 全部通过；关联提交：本提交 `refactor: complete release e architecture cleanup`。

2026-07-29 14:59 CST changelog：完成架构清理 P0 基线、冻结与自动 guard，在 `testing/architecture-cleanup/` 建立 Python import graph/cycle 与 public Interface 摘要快照、`non-api -> api` 单调 allowlist、frontend layer 单调 allowlist、OpenAPI digest 与 canonical `/api` route inventory，并补齐 writer/reader/adapter/field/fixture/config 六类 compatibility inventory 和 Release E 的 domain authority、deprecated traffic budget、client prefix 证据；新增 guard 自检以禁止接入仓库 pytest、L0/L1 或 GitHub workflows，并记录整个临时目录在 P6 删除的 owner、最长生命周期与退出条件。显式本地 cleanup suite 13 项、其专用 Ruff/format/Ty 检查和完整 `bash scripts/ci.sh l1` 均通过；默认 pytest collect 未发现 cleanup-only tests；关联提交：本提交 `test: establish architecture cleanup p0 guards`。

2026-07-29 15:22 CST changelog：完成 P0 退出条件复审与缺口补齐，新增有界 `ainrf_deprecated_contract_calls_total`，按 route/request field/response field 持久计数，并在 body `idempotency_key`、Task legacy input/flat response 等实际兼容路径记录使用量；Grafana Overview 新增 deprecated contract 明细和兼容流量面板。通过只读 production HTTP/metrics 观测确认当前客户端主前缀为 `/api`（累计 45,254 次；仅 3 次 `/v1/models` 未授权探测），同时如实记录当前部署未导出 domain-mode/deprecated-traffic 指标，禁止把缺失证据解释为 v2 或零调用。local-only inventory 逐项冻结 38 个 deprecated surface、全部 OpenAPI body idempotency alias 与 Task legacy request/response field，并标记无产品调用的 deprecated DB instrumentation 为 P2 直接删除候选；cleanup suite 17 项、定向 telemetry/API 测试、Ruff、format、Ty、JSON 校验和完整 L1 均通过；关联提交：本提交 `feat: complete p0 compatibility observability`。

2026-07-29 15:36 CST changelog：修复 P0 兼容观测对已完成 P1 Task v2 canonical contract 的干扰：Task create/fork/retry 仅对 flat response field 记录 deprecated telemetry，不再向 canonical operation 附加 `Deprecation`、`Sunset` 或 `Link` header；补充 fork 回应回归断言，并同步 architecture cleanup scanner 与 public Interface 基线。Task v2/telemetry 定向测试 46 项、cleanup-only suite 17 项、Ruff、format、Ty 和完整 `bash scripts/ci.sh l1` 均通过，确认 P1 的 v2-only authority、无 legacy writer/read fallback 与 canonical Task 契约保持不变；关联提交：本提交 `fix: preserve canonical task contract telemetry`。

2026-07-29 17:16 CST changelog：完成《OpenScience Architecture Cleanup Refactor Design》P2 后端依赖方向修正，将 Prometheus recording/exposition 与 SLA telemetry 从 HTTP route 移至中立 `ainrf.telemetry` Module，将 API key/product config、SSH container profile 和 Linux tenant identity 收入中立 runtime Module；DB、domain telemetry、execution SSH 与 Literature 调用方不再 import `ainrf.api`。Task worker 只依赖 `harness_engine.base` Interface，concrete engine 与 Agent SDK session-store 配置移至 CLI composition Adapter；Project archive/unarchive 删除 `DomainService` compatibility facade，由 Task application Interface 唯一承接。HTTP request identity 与 metrics middleware 形成显式 transport mapper/Adapter，package compatibility exports 改为惰性解析，清除 eager barrel 和 lazy import 形成的全部 Python product cycle。P0 backend allowlist 从 9 项单调收缩为空，architecture baseline 为 484 条边、0 cycle；cleanup-only suite 19 项、后端定向回归、Ruff、format、Ty 和完整 `bash scripts/ci.sh l1` 均通过（frontend 60 files / 327 tests、production build、docs-site build），保持 P1 committed-v2 authority、无 legacy writer/read fallback；关联提交：本提交 `refactor: correct backend dependency direction`。

2026-07-29 18:08 CST changelog：完成《OpenScience Architecture Cleanup Refactor Design》P3 后端领域 Module 深化：删除横跨 Project、Workspace、Environment 的 public `DomainService`，以三个窄 application Interface 和更小的 runtime reader Interface 取代，并将共享 SQLite transaction、audit、authorization、idempotency 与 ordering 收入私有 domain write kernel；`SqliteDomainRepository` 私有化为单一调用集群的 internal implementation。Context 的 Task pin、snapshot assembly、transaction capability sentinel 和 preview/confirm 内部写路径移出产品 Interface，Task lifecycle 成为唯一 Task/Context mutation 入口；Attempt 改为 worker-only `AttemptWorkerModule` 并从产品领域 barrel 移除。按 replace-don't-layer 删除 700 行浅 facade/repository 测试，以 aggregate Interface observable-outcome 测试替换，architecture public Interface 从 1527 项收缩到 1513 项，后端图保持 486 条边、0 cycle、空 allowlist。cleanup-only suite 19 项、完整后端并行与 race/contention lanes、Ruff、format、Ty 以及完整 `bash scripts/ci.sh l1` 均通过（frontend 60 files / 327 tests、production build、docs-site build），保持 committed-v2 authority、无 legacy writer/read fallback、单向依赖和零 product import cycle；关联提交：本提交 `refactor: deepen backend domain modules`。

2026-07-29 23:10 CST changelog：完成《OpenScience Architecture Cleanup Refactor Design》P4 transport contract 主体实现：FastAPI/Pydantic OpenAPI 成为唯一 HTTP transport authority，root 与 `/v1` compatibility aliases 获得显式 deprecation metadata，operation ID 固定为 method+path；引入固定版本 `@hey-api/openapi-ts 0.99.0` 的确定性生成目录、schema SHA-256 manifest、canonical operation/path metadata 和 L0/L1/GitHub drift gate。Endpoint request wrapper、Environment/Skill feature mapper 与 MSW Adapter 改用 generated transport types/metadata，删除 shared types 中 20 组重复 request model，并把 route-local Pydantic model 收敛到 schema Module；首次 P4 按约束保留 compatibility field/route，并延续其 owner、telemetry、P5 deadline 与 removal evidence。frontend lint、60 files/327 tests、production build、transport contract tests及 cleanup-only suite 19 项通过；后端完整 gate 在共享主机出现既有 SQLite/API fixture 超时，待 clean checkout/CI 继续验证；关联提交：本提交 `refactor: generate authoritative transport contract`。

2026-07-29 23:18 CST changelog：完成 P4 clean-checkout 与首轮 GitHub CI 收口：从提交 `5259c9f` 建 detached 临时 worktree，标准 `uv sync --dev`、`npm --prefix frontend ci` 和字节级 `check:transport` 通过且 working tree 无漂移。针对 GitHub frontend lane 只有 Node、没有 `uv` 的既有职责分离，generation check 在 Node-only 环境使用 tracked OpenAPI 重建 TypeScript，而 backend contract test 继续独立验证 tracked OpenAPI 与 FastAPI authority 完全一致；本地分别验证有/无 `uv` 两条确定性路径，frontend lint 与 transport/CI 定向测试通过；关联提交：本提交 `fix: support node-only transport drift checks`。

2026-07-29 23:34 CST changelog：完成 P4 PR required-check 验收，GitHub clean runner 的 Backend deterministic gate（3m39s）、Frontend deterministic gate（1m50s）与 Documentation deterministic gate（22s）全部通过，证明完整 L1 backend/frontend/docs gate、标准 clean install、generated drift 和生产构建在 CI 环境闭合；本地共享主机此前的 SQLite/API fixture 超时未在 GitHub clean runner 复现。PR #54 保持 ready-for-review；生产环境、生产容器、生产数据、生产 HTTP 与 release smoke 按用户要求明确跳过；关联提交：本提交 `docs: record p4 ci acceptance`。

2026-07-29 23:43 CST changelog：修复 P4 最终 worklog 提交触发的 GitHub frontend flaky：Settings deployment-version 测试此前仅等待静态标题便同步断言异步 query 数据，在 runner 负载下偶发读取 `Unavailable`；改为等待 backend/frontend commit 与时间字段完成渲染，并同步加固 mismatch 场景。定向测试连续 10 轮、frontend lint、完整 60 files/327 tests 与 production build（含 transport drift check）均通过；关联提交：本提交 `test: stabilize deployment version assertions`。
