2026-05-12 21:20 changelog: 完成 Codex App Server 执行引擎首版接入，新增 `codex-app-server` task harness engine，扩展 research agent profile/API/frontend settings 以承载 Codex 本机 app-server 配置，并将 task create/detail 交互改为支持独立 execution engine 选择与会话型 pause/resume/send-prompt 能力；验证通过 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/task_harness/test_engines.py tests/test_task_harness_service.py -q`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py -q`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/task_harness/engines/codex_app_server.py src/ainrf/task_harness/service.py src/ainrf/task_harness/models.py src/ainrf/task_harness/artifacts.py src/ainrf/api/schemas.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/task_harness/engines/codex_app_server.py src/ainrf/task_harness/service.py src/ainrf/task_harness/models.py src/ainrf/task_harness/artifacts.py src/ainrf/api/schemas.py tests/task_harness/test_engines.py`、`cd frontend && npx vitest run src/settings/storage.test.ts src/pages/TasksPage.test.tsx`；仓库全量 `ty check` / `ruff check src tests` 仍受既有基线问题影响，未在本批次内清理。
2026-05-12 21:40 changelog: 完成 `feat/codex-app-server-engine` 分支上的静态基线清理，修复 `projects` route 类型标注、`code_server_binary` 版本 tuple 推断、`agent_sdk`/`claude_code` 引擎类型守卫，并删除 `environments/service.py` 中重复的 project reference 实现；同时修正若干测试辅助函数与 prompt/engine 测试夹具的类型声明，使全量 `ty check` 与 `ruff check src tests` 通过；验证通过 `UV_CACHE_DIR=/tmp/uv-cache uv run ty check`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src tests`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/task_harness/test_engines.py tests/test_task_harness_service.py tests/test_api_tasks.py tests/api/test_skills.py tests/api/test_skill_registries.py tests/task_harness/test_prompting.py -q`、`cd frontend && npx vitest run src/settings/storage.test.ts src/pages/TasksPage.test.tsx`。
2026-05-12 22:03 changelog: 为 `codex-app-server` 补充 API 与真实本机 smoke 级端到端测试，新增一个默认执行的非 live 校验用例验证 `execution_engine="codex-app-server"` 的任务创建与 detail 序列化，并新增一个由 `AINRF_RUN_LIVE_CODEX_APP_SERVER_TASK=1` 控制的真实 `codex app-server` smoke 用例，覆盖 create task、等待成功、读取输出、发送 follow-up prompt 与二次输出验证；验证通过 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py -q -k 'codex_app_server_engine or live_codex_app_server_task_output_and_followup or create_task_with_kimi_engine'`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check tests/test_api_tasks.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check tests/test_api_tasks.py`，并在本机执行 `AINRF_RUN_LIVE_CODEX_APP_SERVER_TASK=1 UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py -q -k live_codex_app_server_task_output_and_followup` 成功。
2026-05-12 22:39 changelog: 完成 `codex-app-server` 任务级隔离 `CODEX_HOME` 与原始 `config.toml` / `auth.json` 可选覆写支持：任务运行时现在会在 task 目录下复制本地 `~/.codex` 生成 `codex-home/`，可按 research agent profile 中的 `codex_config_toml` 与 `codex_auth_json` 覆写目标文件，并在启动 `codex app-server` 子进程时通过环境变量显式设置 `CODEX_HOME`；同时扩展 runtime/detail 与前端 Settings 表单以展示和编辑这些字段；验证通过 `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/task_harness/models.py src/ainrf/task_harness/artifacts.py src/ainrf/task_harness/service.py src/ainrf/task_harness/launcher.py src/ainrf/task_harness/engines/base.py src/ainrf/task_harness/engines/codex_app_server.py src/ainrf/api/schemas.py tests/test_task_harness_service.py tests/test_api_tasks.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/task_harness/models.py src/ainrf/task_harness/artifacts.py src/ainrf/task_harness/service.py src/ainrf/task_harness/launcher.py src/ainrf/task_harness/engines/base.py src/ainrf/task_harness/engines/codex_app_server.py src/ainrf/api/schemas.py tests/test_task_harness_service.py tests/test_api_tasks.py`、`cd frontend && npx vitest run src/settings/storage.test.ts`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_harness_service.py tests/test_api_tasks.py -q -k 'codex or kimi or normalizes_three_layer_task_configuration'`，并再次执行 `AINRF_RUN_LIVE_CODEX_APP_SERVER_TASK=1 UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_tasks.py -q -k live_codex_app_server_task_output_and_followup` 成功。
2026-05-12 23:03 changelog: 完成 Codex 默认配置读取链路，新增 `/settings/codex-defaults` 只读接口读取当前本机 `~/.codex/config.toml` 与 `auth.json` 原文，并在前端 `SettingsProvider` 初始化时把这些内容注入 `codex-app-server-default` profile，确保新建任务 profile 中 `config.toml` / `auth.json` 默认值来自当前宿主文件而不是固定模板；同时补充 API 与前端测试，验证默认值读取、task detail 中 `runtime.codex_home` 序列化，以及 Settings 页面展示的真实默认内容；验证通过 `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_health.py tests/test_api_tasks.py -q -k 'codex_defaults or codex_app_server_engine'`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/api/routes/settings.py src/ainrf/api/app.py src/ainrf/task_harness/service.py tests/test_api_health.py tests/test_api_tasks.py`、`UV_CACHE_DIR=/tmp/uv-cache uv run ty check src/ainrf/api/routes/settings.py src/ainrf/api/app.py src/ainrf/task_harness/service.py tests/test_api_health.py tests/test_api_tasks.py`、`cd frontend && npx vitest run src/pages/SettingsPage.test.tsx src/settings/storage.test.ts`。
2026-05-12 23:36 changelog: 完成 Codex 配置隔离实现的生产性收口，修复 `SettingsProvider` 在页面加载时无条件覆盖 `codex-app-server-default` profile 的问题，改为“只补空、不覆盖用户已保存值”；同时将任务级 `CODEX_HOME` 生成从整目录 `copytree(~/.codex)` 收窄为仅白名单复制 `config.toml` 与 `auth.json`，并从 Settings UI 移除当前并未驱动运行时的 `Codex base URL` / `Codex API key` 输入项以避免误导；验证通过 `cd frontend && npx vitest run src/pages/SettingsPage.test.tsx src/settings/storage.test.ts`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_task_harness_service.py tests/test_api_health.py tests/test_api_tasks.py -q -k 'codex or codex_defaults'`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/task_harness/artifacts.py tests/test_task_harness_service.py src/ainrf/api/routes/settings.py tests/test_api_health.py tests/test_api_tasks.py`。
2026-05-13 00:44 changelog: 修复 PR #38 最终 review 的三个阻断问题：为 `codex-app-server` 引擎补充 app-server 提前 EOF 时的 pending request 快速失败逻辑，避免任务在 `initialize` / `thread/start` / `turn/start` 阶段无响应卡死；为前端 Codex 默认值增加 `host_default/custom` 来源语义，使用户把 `config.toml` 或 `auth.json` 文本框清空后仍能稳定保持“显式清空”状态，而不会在刷新页面后被宿主 `~/.codex` 默认值重新灌回；并在 task harness 的 `create_task` 阶段直接拒绝 `codex-app-server` 绑定到非本地 environment，避免远端路径在本地子进程中晚失败。验证通过 `cd frontend && npx vitest run src/pages/SettingsPage.test.tsx src/settings/storage.test.ts src/pages/TasksPage.test.tsx`、`UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/task_harness/test_engines.py tests/test_task_harness_service.py tests/test_api_tasks.py -q -k 'codex or codex_app_server'`、`UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/task_harness/engines/codex_app_server.py src/ainrf/task_harness/service.py tests/task_harness/test_engines.py tests/test_task_harness_service.py`。
## 15:19:59 deck0 Anthropic-style pure frontend rewrite

Rebuilt `test/Mode1/slides/deck0` into a new `deck0_new/` static HTML/CSS deck without RevealJS, reflowing the original content into a 20-page landscape Letter whitepaper layout modeled on the Anthropic reference style. Added paginated print CSS, recreated the cover, contents, chapter dividers, editorial text pages, tables, showcase layouts, and closing page, then exported the final artifact to `test/Mode1/slides/deck0_new/deck0_new.pdf`.

Validation: exported with headless Chrome using `--no-sandbox`, confirmed `pdfinfo` reports 20 landscape Letter pages, rendered all pages to PNG with `pdftoppm`, and visually checked representative pages including the cover, card layout, tables, decision timeline, solution matrix, and closing page for clipping and pagination defects.

## 15:25:00 deck0 Chinese font replacement

Updated `test/Mode1/slides/deck0_new/styles.css` to embed `Noto Sans SC` and `Noto Serif SC` via `@font-face`, then rewired the font stacks so English text keeps the existing Georgia and Arial look while Chinese glyphs resolve to Noto instead of the local WenQuanYi fallback.

Validation: re-exported `test/Mode1/slides/deck0_new/deck0_new.pdf`, confirmed with `pdffonts` that the PDF embeds `NotoSansSC-Regular/Bold` and `NotoSerifSC-Regular/Bold`, and rendered representative pages to PNG to verify denser Chinese text pages and the closing page remain visually correct.

## 15:33:00 deck0 vector illustrations and offline font stabilization

Added lightweight inline SVG illustrations to the foreword page, the three-mode pipeline page, the Mode2 reproduction showcase, and the four-layer capability stack page so the deck gains vector diagrams without introducing Mermaid or runtime dependencies. The diagrams were styled to match the editorial orange line language already used in the deck.

Validation: localized the Noto font files under `test/Mode1/slides/deck0_new/assets/fonts/` to remove export-time network dependence, re-exported `test/Mode1/slides/deck0_new/deck0_new.pdf`, confirmed embedded fonts with `pdffonts`, and visually checked the illustrated pages as rendered PNGs for line visibility, layout fit, and non-overlap.

## 15:50:00 deck0 copyediting and mixed-language normalization

Reviewed and normalized the visible copy in `test/Mode1/slides/deck0_new/index.html` with a Chinese-led editorial pass: translated directory and chapter labels, unified Mode naming, cleaned several English headings and SVG labels, tightened Chinese paragraph phrasing, and normalized mixed Chinese-English spacing, punctuation, and capitalization where it was visibly inconsistent.

Validation: re-exported `test/Mode1/slides/deck0_new/deck0_new.pdf`, confirmed it remains 20 landscape Letter pages, and rendered representative pages including the table of contents, Mode 1 showcase, Mode 3 decision chain, and the final solution spread to verify that text cleanup did not introduce overflow or layout regressions.

## 17:50:00 deck0 pagination compression and section-header merge

Compressed `test/Mode1/slides/deck0_new/` by removing the four standalone section cover slides and merging each section title into the first content slide of that section. Updated the table of contents page numbers, tightened page padding and spacing, and introduced reusable in-page section headers so the deck carries more information per page without deleting core content pages.

Validation: re-exported `test/Mode1/slides/deck0_new/deck0_new.pdf`, confirmed with `pdfinfo` that the deck is now 16 landscape Letter pages, and rendered representative pages for the contents, merged section-start pages, decision chain, gap page, and final conclusion page to verify that the new pagination and denser layout render without overlap or missing content.

## 20:33:00 deck0 screenshots and document previews

Added image assets under `test/Mode1/slides/deck0_new/assets/` from two sources: WebUI screenshots taken from the local `frontend` dev server, and preview images derived from existing paper PDFs and the DynAGS reproduction report. Integrated the stable, high-signal images into the deck to reduce empty space and make the “system” and “artifact” sides of AINRF more concrete.

Validation: started the local Vite frontend, captured candidate UI screenshots, extracted document preview PNGs from local PDFs and report content, re-exported `test/Mode1/slides/deck0_new/deck0_new.pdf`, and visually checked representative pages after insertion to confirm that the new imagery appears in the layout without breaking the current 16-page structure.

## 20:46:00 deck0 image rollback

Rolled back the screenshot and document-preview insertion from `test/Mode1/slides/deck0_new/`, removing the temporary image blocks from `index.html`, deleting their associated CSS rules from `styles.css`, and cleaning the copied PNG assets from `deck0_new/assets/`. Kept the 16-page pagination, section-header merge, typography, and copyediting changes intact.

Validation: re-exported `test/Mode1/slides/deck0_new/deck0_new.pdf`, confirmed it remains a 16-page landscape Letter deck, and rendered representative pages to verify that the deck is back to the no-added-images state without layout regressions.
