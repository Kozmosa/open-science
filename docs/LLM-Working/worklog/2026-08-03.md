# 2026-08-03 Worklog

2026-08-03 01:10 CST changelog：完成自定义 uptime 状态页（`/status/`）收尾。前端新增多页面 Vite 入口（`frontend/status/index.html` + `frontend/src/status/`，消费 Gatus `/uptime/api/v1` 数据，含整体状态、30 天条带、响应时间、事件日历、明暗主题与中英文切换），`verify-bundle-budget.mjs` 从单入口断言改为双入口（主应用 + status）并为 status 页新增独立静态预算与依赖策略，entry gzip 预算因多页面 modulepreload polyfill 从 260 调至 264 KiB；修复 TS 6.0 提升函数中 `root` 窄化丢失（`root!`）与 `refreshTimer` prefer-const；五份 nginx 配置新增 `/status` 301 契约并在 `test_release_workflow.py` 补断言；docs-site 部署/可观测性文档补充 `/status/`。随后修复两个真实缺陷：(1) 30 秒自动刷新整体重建 DOM 会重置弹层/展开组/模态框，新增 `captureTransientState`/`restoreTransientState` + `openModal` 状态，仅数据刷新路径保留瞬态；(2) `applyTheme()` 在 `<html>` 上设置 `data-theme` 导致 `querySelectorAll('[data-theme]')` 误绑定 `<html>` 点击监听器，任何点击冒泡都会触发主题 handler 重建 DOM，且 `<html>` 不随 render 重建造成监听器指数累积、页面 30-180s 卡死——收窄为 `#language-popover [data-locale]` / `#theme-popover [data-theme]` 修复。验证：前端 production build（双入口、budget、metadata）、lint、status 专项 5/5、全量 330/331（唯一失败为 master 基线 `SettingsPage.test.tsx` 过时主题断言）、`tests/test_release_workflow.py` + `tests/test_gatus_uptime.py` 8 过 1 跳（Docker smoke 默认跳过）、docs-site build、`git diff --check` 通过；Chrome 真实点击 smoke 确认弹层/展开组/模态框跨 30s 刷新保留、语言切换即时生效并持久化、连续 10 次点击每次精确切换无监听器累积；未操作 production。
