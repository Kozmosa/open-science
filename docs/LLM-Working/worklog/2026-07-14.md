# 2026-07-14

## 00:05 Historical control-plane migration repair

修复了历史 `agentic_researcher.sqlite3` 的 schema version 已推进、但 maintenance barrier 表未实际落盘时的启动失败。007/008 现在共享幂等 barrier DDL；新增 025 只在 legacy cutover、无活动 mutation/participant 时修复已标记的旧库，prepared/v2 或未知未来 schema 一律 fail-closed。隔离 staging clone 中复制的实际控制面 generation 已成功升级至 schema 25，并通过 health endpoint 启动；仅检查 schema metadata，未读取用户数据、未接触生产或 shared staging。

验证：`tests/test_db_migration.py`、`tests/test_domain_maintenance.py`、`tests/test_output_redaction.py`、`tests/test_api_session_v2_projection.py`、`tests/test_domain_reconciliation_schema.py` 定向集 39 项通过；staging clone health 与 maintenance table/schema version 核验通过。

## 03:50 B1–B10 安全、恢复与遥测收尾

完成 B1–B10 复审后发现的跨阶段闭环补缺：restore 现在生成已验证 generation attestation，使用可恢复 journal 与原子 state-pointer 切换，并要求当前与 staged generation 的 maintenance fence；workspace/tenant 恢复根的稳定性、重叠拒绝、显式高风险 orphan/Git 审查报告一并落地。维护期 API、worker、Overview planner、terminal 和 CLI 均先加入持久 participant/lease 边界，避免启动或重启时偷偷创建领域状态；final Task guard 与 cutover 审计同时要求 active Project–Workspace link。授权路径补齐 Environment、Workspace runtime、dispatcher、Context、Literature 与 Overview 的 404/403 不泄露语义和有界 denial 指标；公开 client metrics 改为固定 Web Vitals 白名单，SSH/Literature 与 HTTP 指标移除私有或无限基数标签，结构化日志继续脱敏。

验证：全量 Ruff check/format、Ty 与 diff check 通过；backup/recovery 43 项、cutover/maintenance/source-root、Task/Context/Overview/Literature/terminal/telemetry 等定向回归均通过。前端 lint、45 文件/238 项 Vitest 及生产 build 通过。独占 staging 经前端 bind-mount/权限修复后 smoke 通过，`/metrics` 可见 control/auth/literature/overview 四个 telemetry source 均为 ready；历史 SQLite refresh error counter 为 2，重复 scrape 未增长。覆盖多模块的串行聚合 pytest 在宿主执行窗口约 58% 时被 runner 终止，未出现断言失败；不将其记为完整 L0/L1。

补充：拆分验证后，`tests/test_domain_telemetry.py` 与 `tests/test_metrics.py` 通过；`tests/test_literature_metrics.py` 的 4 个 async fetch 用例在 AnyIO runner 关闭阶段触发 30 秒超时，后续出现 `Runner is closed` 连锁失败。该形态与本机已知 AnyIO/background-thread 卡住一致，未出现业务断言差异；按本轮“卡住即跳过”约束停止 broad terminal/API 同类批次，并保留已通过的精确 maintenance terminal fence 回归，不将这些超时计为通过。

更正：`tests/test_terminal_pty.py` 的 7 个断言均已执行为成功标记，但 pytest 进程随后在宿主清理阶段未退出，已人工中止，因此同样不作为完整通过记录；`tests/test_api_maintenance_startup_read_only.py`（3 项）、output redaction/request logging/session projection 定向集（6 项）均正常退出并通过。

## 16:14 PR #48 L1 失败修复

修复 GitHub 后端 L1 的四项失败：Project Context publish 现在由 API 测试明确验证“同一 Project、同一 Idempotency-Key 的无请求体重放返回首次结果”，避免把服务端可变 Draft 错当作客户端请求哈希；structlog 的 CLI 与 server 配置不再缓存首次 logger，保证重配置和 `capture_logs()` 使用同一当前 processor 链，消除跨测试文件顺序污染，同时保留 JSON 脱敏日志行为。

验证：将会触发缓存问题的 `test_domain_service.py`、`test_cli.py`、`test_domain_telemetry.py` 与 Context API 回归组合串行运行，全部通过；Ruff、format 与 Ty 已在完整后端 L1 中通过。后端 L1 与 GitHub PR CI 正在按修复后的提交重新验证。

## 17:21 External URI route and Markdown safety repair

完成遗留 `fix-external-uri-links` worktree：工作区文件链接解析不再将带 scheme 或协议相对的 URL 当作默认 Workspace 相对路径，保留 HTTPS/arXiv 与 mailto 等外部目标；Markdown 在重写合法 in-app 路径后使用直接固定为 `dompurify 3.4.0` 的 sanitizer 清洗最终 HTML，并只允许 http/https、mailto、tel 与相对/in-app 导航目标。这样 raw HTML 的事件属性、`javascript:`、`data:`、`vbscript:`、`file:` 和自定义协议均不能成为可执行或可导航内容。

验证：URI 工具与 MessageBlocks 定向回归 2 文件/14 项通过；完整 `bash scripts/ci.sh l1-frontend` 通过（46 个测试文件、246 项测试、lint 与生产 build）。关联代码提交：`ea8d34d fix: preserve safe external markdown links`。

## 18:03 Framework bitter lessons consolidation

将本轮复盘中与当前领域 spec 无关、可长期复用的工程问题写入 `dev-bitter-lesson.md`：可重配置全局对象的生命周期、最终 HTML 渲染边界、验证终态与分层证据、前端命令 cwd、依赖 override/lockfile 一致性，以及 squash-aware 的 worktree/worklog 卫生。同步扩展常规开发检查清单，避免这些规则只留在会话记录中。

验证：文档标题编号连续、交叉引用保持有效，`git diff --check` 通过。关联提交：`a129bb9 docs: capture framework bitter lessons`。

## 19:31 Frontend phases Gate 0

在独立 `feat/frontend-phases` worktree 中完成 F1 前置 Gate 0：新增正式的 v2 Project/Workspace 前端读契约，集中返回角色、权限、Primary Workspace、可执行性、Task 活动和 attention 状态，并补齐 Vite proxy 所需的 `X-API-Key` 认证兼容；新增可重复的 synthetic committed-v2 fixture、CLI 与 `scripts/frontend-dev.sh`，可独立启动 API、空闲 domain worker 和 Vite，覆盖可执行 Workspace、已关联但无 Environment grant、无 Workspace Project 和已发布 Context。fixture 拒绝写入 Git worktree，不使用 Docker、production、shared staging 或 L2。headless 环境不补做 DevTools/浏览器 DOM、focus、computed-style 和窄屏验收，这些明确延后到客户端单独检查。

验证：新增 Gate 0 测试 5 项与 domain/auth 兼容回归 31 项通过；Ruff、format、Ty 通过；前端 lint、46 文件/246 项 Vitest、生产 build 与 bundle budget 通过；隔离 stack 实际启动后 `/health`、`/domain/capabilities`、`/domain/projects`、`/domain/workspaces` 均通过，dispatcher capability 为 ready，随后完整清理进程。受限沙箱内 L0 在 backend fast 断言末尾进入已知 AnyIO 清理停滞，未计为完整通过；改在沙箱外运行不含 Docker/外部服务/L2 的完整 `bash scripts/ci.sh l1`，后端 deterministic suite、前端和 docs build 全部通过。

## 23:41 F1 osci token contract

完成 F1 的首个公共契约切片：建立 palette、semantic、component 与独立 light/dark 主题层，固定 `--osci-*` 名称和 `data-osci-theme` 挂载方式；现有 Prism、Apple 与通用变量被收敛为单一兼容 alias 文件。全局样式和 React Flow 已开始直接消费 osci token；设计系统公共入口新增版本化第三方主题 manifest 与纯数据校验器，拒绝未知 token、可执行 URL/CSS 值。新增官方主题 WCAG AA 对比度、主题挂载和兼容 alias 数量基线测试。

验证：定向 Vitest 2 文件/4 项通过；前端 lint、production build 与 bundle budget 通过；`bash scripts/ci.sh l0` 正常退出并通过 Python lint/format 与 backend fast lane。浏览器 computed style、真实颜色渲染和深浅主题截图继续按约定延后到客户端验收。

## 23:56 F1 themes, fonts and user-scoped settings

完成 F1 其余内容：WebUI 设置升级至 v4，主题支持 light/dark/system，并以稳定 user ID 隔离存储；未认证表面固定安全 light，认证后在受保护 Shell 首次绘制前同步应用用户主题，system 偏好随系统变化。旧无用户设置只允许首个已认证用户有界领取一次，旧 serif/sans 不再影响主题。移除 Google Fonts 运行时依赖，锁定 `@fontsource/noto-sans@5.2.10` 的本地拉丁 WOFF2 400/500/600/700，记录来源、OFL 与 SHA-256；Open Orbit 现在用于 favicon、登录和注册品牌位。

验证：完整前端 48 文件/254 项 Vitest 通过；前端 lint、production build 和 bundle budget 通过，构建产物仅包含四个本地 Noto Sans WOFF2；`bash scripts/ci.sh l0` 正常退出并通过。真实浏览器首帧闪烁、字体 computed family/weight、loaded assets 与 system 切换继续延后客户端验收。

## 00:11 F2 osci overlay primitives

完成 F2 overlay 批次：锁定 Radix Dialog、Dropdown Menu、Popover、Tooltip、Select、Toast 与 `cmdk` 等底层依赖，新增设计系统 Dialog、Sheet、菜单、浮层、提示、复合 Select、Command 和 Toast 公共契约，并保留 `NativeSelect` 作为现有原生 `<option>` 调用方的显式兼容入口。所有旧 Modal 调用方已迁到 Dialog，Toast provider/hook 已迁入 `@design-system`，零调用方的旧 Modal、Toast 与共享 Drawer 实现已删除。Dialog 兼容 API现在具备 Escape 关闭、焦点圈闭/恢复和强制可访问名称；Toast 保留 `showToast(message, type)` adapter。由于认证 Shell 的 Radix Toast/FocusScope 增加约 30 KiB raw 模块开销，entry raw 预算精确调整到 800 KiB，gzip 250 KiB 上限保持不变。

验证：overlay 与受影响页面定向 6 文件/48 项测试通过；完整前端 49 文件/258 项测试通过；前端 lint、production build 与 bundle budget 通过（entry 780.9 KiB raw / 248.2 KiB gzip）；沙箱内 L0 在 backend fast lane 89% 后复现已知 AnyIO 清理停滞并中止，随后在沙箱外重跑同一 `bash scripts/ci.sh l0` 正常退出，Python lint/format、backend fast、frontend lint 与完整 Vitest 全部通过。真实浏览器焦点路径和浮层交互继续按约定延后客户端验收。关联提交：本提交 `refactor: migrate osci overlay primitives`。

## 00:26 F2 osci component primitives and import boundary

完成 F2 基础组件与公共导入边界：Button、Input、Textarea、Card、Badge/StatusBadge、Alert 和 Form/FormField 全部改用 osci semantic token；Button 新增 `icon`/`icon-sm` 的类型级可访问名称约束，loading 状态保留原内容尺寸并暴露 busy 状态。新增 Radix Checkbox、RadioGroup、Switch、Tabs，以及 Skeleton；现有 Tasks/Settings 原生 checkbox/radio 调用迁入设计系统。全仓业务与测试导入统一为唯一根入口 `@design-system`，ESLint 现在禁止业务层 `@design-system/*` 内部路径、直接 `@radix-ui/*` 和 legacy `components/ui`；设计系统内部仍负责合法封装底层依赖。补充 `--osci-color-on-accent` 主题契约和 ResizeObserver 测试环境适配，并修正 Codex 自定义设置用例，使 fixture 明确声明 custom source，消除异步 host default 覆盖时序歧义。公共 barrel 与可访问表单基础设施使 entry 稳定在约 798 KiB raw / 252 KiB gzip，因此预算收敛为 825/260 KiB，FileBrowser 与 Monaco 上限不变。

验证：基础控件/semantic contract 定向 4 文件/13 项测试通过；完整前端 50 文件/264 项测试通过；前端 lint、production build 和 bundle budget 通过（entry 798.3 KiB raw / 252.0 KiB gzip）；沙箱外 `bash scripts/ci.sh l0` 正常退出，Python lint/format、backend fast、frontend lint 与完整 Vitest 全部通过。真实浏览器控件焦点环、原生辅助技术公告与触控交互继续按约定延后客户端验收。关联提交：本提交 `refactor: complete osci component primitives`。

## 00:43 F3 OpenScience AppShell

完成 F3 Shell 重构：新增唯一 route registry，路由渲染、侧栏、Command Palette、document title、管理员过滤和英文检索关键词均由同一元数据生成；Workspace Browser 从一级导航移除但保留 `/workspace-browser` 深链和命令入口。原 Layout 拆为 Sidebar、TopBar、AccountMenu、CommandPalette、NavigationLinks，并为窄屏提供 Dialog-based Sheet 导航。侧栏折叠偏好使用 `openscience:preference:${userId}:sidebar-collapsed` 隔离；TopBar 不再展示页面标题，也不发起或轮询 Tasks，只订阅已有 React Query task-list cache。新增 PageShell `legacy|canvas` 契约（默认 legacy）、ViewToolbar、DetailDrawer、ConfirmDialog、UpdateStrip，并稳定 PageHeader/EmptyState。CardGrid 删除内部 localStorage 写入和动态 Tailwind class，保持纯展示/DnD；attention 即使不在用户顺序中也固定首位且不可拖动。Resources 现有卡片顺序迁到统一 user-scoped preference hook，并保留旧 key 一次迁移。

验证：Shell/route/PageShell/CardGrid 与 user preference 定向 3 文件/15 项测试通过；完整前端 51 文件/270 项测试通过；前端 lint、production build 和 bundle budget 通过（entry 807.0 KiB raw / 254.7 KiB gzip）；沙箱外 `bash scripts/ci.sh l0` 正常退出，Python lint/format、backend fast、frontend lint 与完整 Vitest 全部通过。TopBar blur/实色/reduced-transparency、真实窄屏导航和 Command Palette 浏览器交互继续按约定延后客户端验收。关联提交：本提交 `refactor: rebuild openscience app shell`。
