# 2026-07-12

- 00:37 changelog：完成 OpenScience Console、`osci` 设计系统与 Project/Task/Workspace 领域契约的合并前收口。领域设计已统一 Task → TaskAttempt → RuntimeSession 权威模型、Task 级 Context pin 与手动更新、Project/Task/Workspace 权限能力表、Workspace 首期不共享、Project–Workspace 单一 Primary 与原子幂等规则、默认 Project 和 queued Task 归档语义；Literature 转 Task 改为标准 Task 创建入口，Console 概览明确禁止触发 arXiv/LLM 昂贵调用。旧 Session Chain、Task Retry、Retry E2E 和权限规范已移入归档并标明替代入口；同时保留已确认的 Console、设计系统和 Open Orbit 标识草案。验证结果：相对 Markdown 链接检查与 `git diff --check` 通过，主工作区保持干净；关联提交：本提交 `docs: align console domain contracts`。

- 01:16 changelog：完成 OpenScience 领域与 Console 重构全面执行规范。将已接受的领域、Console、`osci` 和 Literature 契约拆为 S0 安全底座、B1–B10 后端阶段、F1–F10 前端阶段及五个发布批次，明确统一领域 SQLite、Environment 持久注册、shadow importer/reconciliation、Project/Workspace 权限、Context、Attempt/RuntimeSession、幂等 Task application service、Literature saga、Overview snapshot 和兼容退役顺序。补充完整 backup/staged restore、持久 maintenance barrier、数据库 cutover fuse、blocking issue 自动中止、dispatcher claim/CAS 与重复启动保护、旧前端兼容、L0–L4 门禁、生产 cutover 和回滚边界；根据三路交叉 review 修正 PageShell 兼容迁移、TaskCreateFlow 合流、用户主题启动、Project owner 单一权威、FK 过渡、授权二次检查和 Release C 分批。验证结果：相对 Markdown 链接检查和 `git diff --check` 通过；本批次仅新增长期执行 spec 和工作记录，未实施或部署代码；关联提交：本提交 `docs: add domain refactor execution spec`。

- 09:10 changelog：完成 GitHub Actions L1 后端门禁的环境稳定性修复。CLI 测试在断言 Typer/Rich 输出前移除 ANSI 控制序列，避免 CI 终端渲染将选项名和路径分段；WebUI 启动脚本默认缓存测试显式清除继承的 `UV_CACHE_DIR`，准确覆盖脚本的默认值契约，同时保留自定义缓存路径测试。验证结果：以 CI 环境变量运行的 3 项定向 pytest、Ruff 检查/格式化、Ty 检查与 `bash scripts/ci.sh l1-backend` 均通过。

- 01:34 changelog：在 `feat/domain-refactor-s0-b3` 隔离 worktree 完成领域重构首批实施计划。计划严格限定 S0、B1、B2、B3：备份与 staged restore、持久 maintenance barrier、迁移 CLI/隔离 fixture、统一 SQLite schema、shadow importer/reconciliation hard gate、Project/Workspace/Environment repository 与权限/原子关系操作；明确 legacy/validate 不双写、B7 前不切换旧 HTTP 契约、B4+ 和前端不在范围。验证结果：已对照执行 spec、当前备份/迁移/JSON registry/API 路由与 CI 入口；计划文件保持未提交，待后续实现批次使用。

- 01:44 changelog：完成 S0.1 备份与 staged restore 安全底座。备份 manifest 升级为带来源路径、schema version、SHA-256 的 v2 格式，纳入 `workspaces.json`；校验逐成员重新计算哈希并拒绝重复条目。restore 现只允许写入不存在的新 staged state root，先完成 archive 校验和 SQLite integrity/foreign-key 检查，再原子发布候选目录，原活动 state root 不被覆盖；CLI 改为要求 `--staged-state-root`。验证结果：备份定向 pytest（13 项）、`bash scripts/test.sh unit`、Ruff 检查/格式化、Ty 检查与 `openscience backup restore --help` 均通过；关联提交：本提交 `feat: stage backup restore before domain migration`。

- 01:51 changelog：完成 S0.2 持久 maintenance barrier。为 `agentic_researcher.sqlite3` 增加单调 epoch、active 状态和 in-flight mutation lease；API 对 Project、Workspace、Environment、Task、Session、Literature 与 task-edge 的写请求在 maintenance 期间统一返回 503，并在既有请求完成后 drain。新增 `OPENSCIENCE_DOMAIN_MODEL_MODE` 严格枚举和 `openscience domain-maintenance enter|status|exit` 管理入口，重启后仍从数据库读取状态。验证结果：领域 barrier 定向 pytest、`bash scripts/test.sh unit`、`bash scripts/test.sh concurrent`、Ruff、Ty 与 CLI help 均通过；关联提交：本提交 `feat: add persistent domain maintenance barrier`。

- 01:55 changelog：完成 S0.3a 迁移来源快照与 dry-run 基础。新增只读 legacy source manifest，JSON 记录 inode/mtime/size/SHA-256，SQLite 通过临时 backup snapshot 计算一致哈希，不修改 source；新增 `openscience domain-migration dry-run` 和合成 fixture 起点。实际 importer/checkpoint/reconciliation 规则仍留待 B2，其他异常 fixture 与隔离 L2 cell 留在后续 S0.3b。验证结果：定向 pytest、Ruff 与空 state-root CLI smoke 通过；关联提交：本提交 `feat: add domain migration source manifest`。

- 12:23 changelog：完成 B1 统一领域 additive schema。`agentic_researcher.sqlite3` 新增 Project、Environment、Workspace、link/member、Context、Attempt/Runtime、idempotency/outbox/audit、migration/cutover 等控制面表及 RESTRICT FK、partial unique index、Primary/immutable Context trigger；旧 Task 只增补兼容列。`auth.sqlite3.environment_access` 增加 grant version/active/revoked 元数据。验证结果：migration/schema 定向 pytest、Ruff 与 Ty 通过；legacy 写路径未切换；关联提交：本提交 `feat: expand domain control-plane schema`。

- 12:28 changelog：完成 B2.1 application-level shadow importer。导入流程在 DDL migration 外读取 legacy JSON、auth user 和 Task，记录 source manifest、run/checkpoint 统计和可定位 blocking issue；保留可映射 Project/Workspace ID，创建 seed Environment、legacy empty Context pin 与 legacy Attempt，canonical path/owner 缺失进入 attention，不静默授予权限或合并。`domain-migration apply|reconcile` CLI 已接入，重复同内容 source 复用 run 且不重复写入。验证结果：importer/source 定向 pytest、Ruff 与 Ty 通过；关联提交：本提交 `feat: add domain shadow importer`。

- 12:32 changelog：完成 B2.2 reconciliation hard gate。reconcile 现在核对 immutable source content、default Project、Primary link、Task Project/Workspace 引用、active Task Context/Attempt、Workspace Environment、SQLite integrity/foreign key 与 cutover constraints fuse，输出结构化计数和 blocker；任一 blocker 均持久化为 `cutover_allowed=false`，不提供忽略开关。验证结果：importer/reconciliation 定向 pytest、Ruff 与 Ty 通过；关联提交：本提交 `feat: add domain reconciliation gate`。

- 12:38 changelog：完成 B3 v2 Project/Workspace/Environment application service 与受控 adapter。新 SQLite service 集中处理 Project owner/editor、Workspace owner（admin 不能绕过 tenant 执行权限）、Environment 注册/disable、成员、attach/detach/Primary、idempotency/audit、default Project archive guard 与 queued/running Task workspace unregister guard；`/domain` 路由只在 `v2` mode 和 cutover fuse 就绪时启用，旧路由未切换。验证结果：Domain service/API adapter/importer/schema 定向 pytest、Ruff 与 Ty 通过；两次完整 L1 backend lane 在环境仅输出部分进度后提前结束且未返回失败诊断，需在稳定执行器复跑；关联提交：本提交 `feat: add v2 domain application service`。

- 12:43 changelog：完成 S0.3b fixture matrix 与 isolated migration cell。补齐空输入、缺字段、重复路径、owner 异常和无法映射 session 的脱敏 legacy fixture；`testing/domain_migration/run_cell.sh` 使用唯一临时 state root 执行 dry-run 并自动清理，不接触 Docker、shared staging 或生产资源。验证结果：fixture/cell pytest、Ruff 与本地 isolated-cell smoke 通过；关联提交：本提交 `test: add isolated migration cell fixtures`。

- 12:48 changelog：完成 B4 Project Context 首期闭环。新增 Draft 保存、immutable Context Version 发布、Active Version 切换、snapshot 创建与 Task Context pin；手动 Task Context 更新保留 Project editor 与 Task owner 两层授权，发布新版本不改动既有 Task。验证结果：Context 定向 pytest 与 Ruff 通过；关联提交：本提交 `feat: add project context snapshots`。

- 12:54 changelog：完成 B5 TaskAttempt/RuntimeSession 与持久调度基础。新增 Attempt/outbox claim metadata migration、Attempt 创建时 Context snapshot pin、`(task_id, attempt_seq)` 运行历史、条件 claim token、deterministic runtime launch key 与 RuntimeSession 写入，并提供按 Project 取消未启动 dispatch primitive。验证结果：Attempt/migration 定向 pytest、Ruff 与 Ty 通过；关联提交：本提交 `feat: add durable task attempts and outbox`。

- 13:01 changelog：完成 B6 标准 Task application service。新创建路径在同一 SQLite transaction 原子写入 Task、ContextSnapshot、initial Attempt、dispatch outbox、audit/idempotency；Retry 保持同一 Task ID 并新建 Attempt，Project Archive 同事务取消 queued/claimed dispatch 和 queued Attempt。验证结果：Task application 定向 pytest、Ruff 与 Ty 通过；关联提交：本提交 `feat: add standard task application service`。

- 13:08 changelog：完成 B7 Task API cutover compatibility layer。`/domain/capabilities` 以运行时 mode/fuse 声明 v2 能力；`/tasks` 在 `legacy/validate` 保持旧服务，在 `v2 + constraints/cutover fuse` 下通过标准 Task application service 完成创建、Retry、Archive，同时继续使用既有 Task response 读取兼容格式。验证结果：v2 API adapter/Task application 定向 pytest、Ruff、Ty 通过；L0 的 lint/format 与 backend fast lane 可见进度通过，但执行环境在后续汇总前提前结束且无失败诊断，需在稳定执行器复跑；关联提交：本提交 `feat: cut over task API through v2 adapter`。

- 13:14 changelog：完成 B8 Sessions projection。新增从 Task/TaskAttempt 生成的只读 Session projection；v2 模式下 `/sessions` 列表、详情和 attempts 读取新模型，create/update/delete 旧 Session 写入明确拒绝，legacy/validate 保持原 SessionService。验证结果：projection 定向 pytest 与 Ruff 通过；关联提交：本提交 `feat: project sessions from task attempts`。

- 13:20 changelog：完成 B9 Literature→标准 Task saga。literature DB 新增可恢复 saga 记录，先持久化 pending，再以确定性 idempotency key 调用标准 Task 创建，成功后更新文献关联为 converted；失败保存错误并可安全重试。兼容 convert 路由在提供 project/workspace 时走 saga，旧 task_id 请求仍保留。验证结果：saga/migration 定向 pytest、Ruff 与 Ty 通过；关联提交：本提交 `feat: convert literature through task saga`。

- 13:27 changelog：完成 B10 Today overview snapshot。控制面 DB 新增每日 owner snapshot；refresh 仅聚合本地 Project/Task/Attempt 状态并持久化 payload，`/domain/overview/today` 仅读取现有 snapshot，完全不触发 arXiv/LLM/外部调用；新增 CLI refresh 入口与 capability 声明。验证结果：overview/migration 定向 pytest、Ruff、Ty 与 CLI help smoke 通过；关联提交：本提交 `feat: add persisted overview snapshots`。

- 13:46 changelog：完成完整执行计划 Stage 1 的 backup manifest v3 收口。manifest 现在逐一记录所有归档 regular member（包括嵌套 session state、detection、可选 Workspace 与 tenant 文件）的路径、大小、SHA-256、mode、uid/gid 与稳定 tree digest；`includes_workspaces`/`includes_tenants` 根据实际归档内容生成。验证会拒绝未清单成员、重复/缺失成员、nested member 篡改、tree digest 或 include flag 不一致，同时保持 v2 archive 的 verify/restore 兼容；SQLite snapshot 侧车文件不会误入 archive。验证结果：17 项 backup 定向 pytest、Ruff check/format、Ty check 与 `git diff --check` 通过；关联提交：本提交 `fix: inventory and verify complete domain backups`。

- 13:55 changelog：完成完整执行计划 Stage 2 的全局 domain writer drain。控制面新增 participant registry、heartbeat、observed epoch、draining/drained/stopped 状态和 per-participant in-flight/unflushed-output 计数；maintenance preflight 现在同时检查活跃 Attempt、pending/claimed launch、稳定来源窗口、必需参与者和陈旧 heartbeat。API lifespan、terminal reconcile、Literature planner 与 admin preflight CLI 已接入该协议，HTTP lease 可关联 API participant 并提供提交前 epoch 校验接口。验证结果：28 项 maintenance/participant/migration/v2 API pytest、Ruff check/format、Ty check、CLI help 与 `git diff --check` 通过；关联提交：本提交 `fix: drain all domain writers during maintenance`。

- 14:03 changelog：完成完整执行计划 Stage 3 的 staged restore promotion 收口。restore 在候选 root 上额外核对 manifest schema version，并允许领域 reconciliation 等 validator 在任何 target 发布前执行；所有失败都会清理候选目录和 promotion journal。多 root promotion 写入可恢复 journal、顺序发布并在异常时回退已发布目标；仅显式 target 才会恢复 Workspace/tenant 数据。验证结果：20 项 backup/restore pytest、Ruff check/format、Ty check 与 `git diff --check` 通过；关联提交：本提交 `fix: validate and promote staged domain restores`。

- 14:14 changelog：完成完整执行计划 Stage 4 的权威领域 schema 加固。控制面 migration 补齐 Environment/Workspace、Context provenance、Task relationship、Attempt 输出与成本、Runtime probe/adoption、outbox claim/launch、migration checkpoint 和 cutover artifact 字段；idempotency 表重建为 actor-scoped 复合主键，Task 关系使用无歧义稳定 ID，cutover 状态机拒绝绕过 prepare 或从 v2 回退。auth migration 将 Environment grant 升级为单调版本、active/revoked 状态和不可变审计事件，服务层改为可审计 revoke/regrant 与仅查询 active grant。验证结果：48 项 schema/auth/domain API/Attempt/Context pytest、Ruff check/format、Ty check、`bash scripts/ci.sh l0` 与 `git diff --check` 通过；关联提交：本提交 `refactor: harden authoritative domain schema`。

- 14:40 changelog：完成完整执行计划 Stage 5 的可恢复、可审计领域导入。新增固定 SourceSnapshotSet：SQLite 全程经 backup snapshot 读取，JSON 以 inode/mtime/size/SHA 的前后观察拒绝变化来源，并以位置无关 manifest digest 绑定 run；导入按 Environment、Project/member/context、Workspace/link、Task/relationship、Session/Attempt/output/Runtime checkpoint 分阶段提交 checkpoint 与 heartbeat。每条参与导入的来源记录写入唯一 terminal result，无法映射项同时写 issue 和带 provenance 的 legacy artifact；中断 run 可仅在相同 artifact 与 manifest 下 resume，CLI 增加 resume/inspect/records。验证结果：43 项 importer/source/schema/migration/domain API/Attempt/Context pytest、CLI help smoke、Ruff check/format、Ty check、`bash scripts/ci.sh l0` 与 `git diff --check` 通过；关联提交：本提交 `refactor: make domain import resumable and auditable`。

- 15:16 changelog：完成完整执行计划 Stage 6 的 typed reconciliation/remediation 闭环。控制面新增 append-only typed resolution 与不可变 final manifest/restore evidence schema；owner、Environment、Primary Workspace 和 RuntimeSession 只能通过具名 resolution 并写入 audit，reconcile 会再次验证已应用 resolution 的目标不变量，直接篡改或后续回退会重新阻断 cutover。finalize 仅冻结 pre-prepare evidence，不改变 cutover fuse；CLI 增加 issue list/inspect/resolve/finalize，本地 importer 的 blocker 统计也验证 applied resolution 的类型匹配。验证结果：63 项 reconciliation/importer/migration/CLI 定向 pytest、Ruff check/format、Ty、CLI help 和 `git diff --check` 通过；`bash scripts/ci.sh l0` 的 lint/format 通过，fast lane 在未修改的 `tests/test_agentic_researcher.py::test_run_task_persists_output_and_succeeds` 卡住，已中断，未将该宿主不稳定项计作通过；关联提交：本提交 `refactor: complete domain reconciliation and remediation`。
