# 2026-06-12

## Development Bitter Lessons Documentation

**18:32 — Lessons distilled into dedicated reference doc**

- Added root-level `dev-bitter-lesson.md` to capture recurring high-cost pitfalls from recent work: host-vs-container frontend deploy mismatch, multi-tenant permission boundaries, browser/devtools setup, session-scoped config reload behavior, flex-layout debugging order, and rebuild-vs-evidence workflow.
- Updated `AGENTS.md` Instruction Priority to explicitly reference the new lessons file before debugging frontend deployment, browser/devtools tooling, multi-tenant permissions, or session-scoped config problems.
- Validation: markdown fences balanced in both `AGENTS.md` and `dev-bitter-lesson.md`.
- Commit: `7f333d0` — `docs: add development bitter lessons reference`

## Agent-SDK Thinking Noise Suppression

**19:21 — Suppressed transient agent-sdk progress chatter in task detail**

- Investigated production task `21dc76c151c4` and confirmed the detail page was showing a collapsed group with `30 system messages, 1 thinking messages`; `/api/tasks/21dc76c151c4/output` contained repeated lifecycle `system` payloads with subtype `thinking_tokens` plus a redundant raw `status` update from the Claude Agent SDK.
- Fixed the issue at three layers: `src/ainrf/harness_engine/engines/agent_sdk.py` now drops noisy agent-sdk `SystemMessage` subtypes `thinking_tokens` and `status` before persistence; `src/ainrf/api/routes/tasks.py` suppresses those legacy lifecycle records when converting stored output to task messages; `frontend/src/pages/tasks/useTaskMessages.ts` suppresses the same wrapped lifecycle payloads during live output conversion so existing historical tasks also stop rendering the noise.
- Added regression coverage in `tests/test_harness_engine.py`, `tests/test_api_tasks.py`, and `frontend/__tests__/pages/TasksPage.test.tsx` for noisy lifecycle suppression.
- Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_harness_engine.py tests/test_api_tasks.py` (31 passed); `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/harness_engine/engines/agent_sdk.py src/ainrf/api/routes/tasks.py tests/test_harness_engine.py tests/test_api_tasks.py` (passed); `cd frontend && node_modules/.bin/tsc -b && npm run test:run -- __tests__/pages/TasksPage.test.tsx -t "suppresses agent-sdk thinking token lifecycle noise from message conversion"` (passed).
- Commit: `0b0617f` — `fix: suppress agent sdk thinking noise`

## Thinking Block Rendering Optimization

**19:32 — Made streaming thinking blocks stay folded and coalesce adjacent segments**

- Tightened the task detail rendering path in `frontend/src/pages/tasks/useTaskMessages.ts` and `frontend/src/pages/tasks/MessageBlocks.tsx` so streaming thinking events no longer auto-expand by default. Partial thinking messages now remain folded unless the operator explicitly expands them, which removes live text repaint pressure for the common collapsed state.
- Added `mergeAdjacentThinkingMessages(...)` in `useTaskMessages.ts` to coalesce back-to-back thinking messages into one display block, preserving the latest sequence/timestamp and carrying streaming state forward. This keeps the detail pane closer to one logical thinking block instead of a stack of adjacent fragments.
- Added frontend regression coverage in `frontend/__tests__/pages/TasksPage.test.tsx` for folded streaming thinking metadata and adjacent thinking coalescing, plus `frontend/__tests__/components/tasks/MessageBlocks.test.tsx` to verify collapsed-by-default streaming behavior and expand-on-demand rendering.
- Validation: `cd frontend && node_modules/.bin/tsc -b` (passed); `cd frontend && npm run test:run -- __tests__/pages/TasksPage.test.tsx -t "thinking"` (3 passed); `cd frontend && npm run test:run -- __tests__/components/tasks/MessageBlocks.test.tsx -t "ThinkingBlock streaming behavior"` (passed).
- Commit: `cc4be8d` — `fix: collapse streaming thinking blocks`

## Thinking Stream Perf Batching

**20:04 — Batched collapsed thinking updates and manually re-verified task detail behavior**

- Added a third frontend perf layer in `frontend/src/pages/tasks/useTaskMessages.ts`: pure streaming thinking-delta batches are now buffered in-memory and flushed to React state on a short timer instead of forcing a render per delta. The merge path was factored into `mergeStreamMessages(...)`, while `shouldDeferMessageBatch(...)` limits deferral to thinking deltas only so final thinking blocks and non-thinking messages still land immediately.
- Manual verification surfaced a separate task-page crash path in `frontend/src/pages/TasksPage.tsx`: defaults were using `data?.items[0]`, which still throws when the response object exists but `items` is absent. Hardened those fallbacks to `data?.items?.[0]` so the page stays up during partial/empty query states and the local manual verification route is reliable.
- Added targeted regression coverage in `frontend/__tests__/pages/TasksPage.test.tsx` for deferred thinking batches and merged block updates. Used a temporary local harness page during browser QA to verify one collapsed thinking block during rapid streaming, successful expand-on-demand, final full content after completion, and normal assistant-message rendering; the harness files were removed after validation and are not part of the commit.
- Validation: `cd frontend && node_modules/.bin/tsc -b` (passed); `cd frontend && npm run test:run -- __tests__/pages/TasksPage.test.tsx -t "thinking|defers|merges deferred|renders prompt and replayed output for the selected task|creates a task with derived title semantics"` (7 passed); `cd frontend && npm run test:run -- __tests__/components/tasks/MessageBlocks.test.tsx -t "ThinkingBlock streaming behavior"` (passed); manual browser checks on local Vite app confirmed `/tasks` no longer crashes, streaming stays collapsed with a single thinking block, expand reveals accumulated content, and assistant output still renders after completion.
- Commit: `9606bda` — `perf: batch streaming thinking updates`

## Frontend Deployment Version Card

**20:17 — Exposed deployed commit metadata in Settings**

- Added a frontend-only deployment version mechanism for the Settings page instead of introducing a new backend round-trip: `frontend/vite.config.ts` now resolves the current Git short hash and commit timestamp at build/dev-server startup (with optional `AINRF_BUILD_COMMIT` / `AINRF_BUILD_COMMITTED_AT` overrides) and injects them as `__AINRF_BUILD_INFO__`; `frontend/src/buildInfo.ts` provides the typed runtime accessor.
- `frontend/src/pages/SettingsPage.tsx` now renders a dedicated Deployment Version card in the general settings tab, showing the current deployment commit hash (first 6 chars) and commit time (`YYYYMMDD-HHMM`) with localized labels from `frontend/src/i18n/messages.ts`.
- Added regression coverage in `frontend/__tests__/pages/SettingsPage.test.tsx` to verify the version card renders the injected commit metadata.
- Validation: `cd frontend && node_modules/.bin/tsc -b` (passed); `cd frontend && npm run test:run -- __tests__/pages/SettingsPage.test.tsx` (8 passed); `cd frontend && npm run build` (passed, Vite build completed with injected build info).
- Commit: `5b437fa` — `feat: show deployment version in settings`

## Unified Deployment Version Source

**20:36 — Moved Settings version card to a backend-served shared source**

- Replaced the frontend-only Vite constant with a unified backend source. `src/ainrf/deployment_version.py` now resolves deployment metadata from one precedence chain: explicit env overrides, shared `frontend/.../build-info.json`, then Git fallback for local/dev. `src/ainrf/api/routes/settings.py` exposes it at `/settings/deployment-version`, and production auth coverage now includes that route.
- Added a shared build-info generation step for frontend builds: `frontend/scripts/write-build-info.mjs` writes `frontend/public/build-info.json` before `npm run build`, `.gitignore` ignores that generated file, and the backend can read the same artifact from `frontend/public` in dev or `/opt/ainrf/frontend/dist/build-info.json` in built runtime images.
- Updated `frontend/src/pages/SettingsPage.tsx` to fetch deployment version data from `getDeploymentVersion()` instead of reading build-time globals, removed the old `frontend/src/buildInfo.ts` + Vite inline Git stamping path, and kept the Settings version card UI unchanged for users.
- Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run pytest tests/test_api_health.py tests/test_production_integration.py` (16 passed); `UV_CACHE_DIR=/tmp/uv-cache uv run ruff check src/ainrf/deployment_version.py src/ainrf/api/routes/settings.py tests/test_api_health.py tests/test_production_integration.py` (passed); `cd frontend && node scripts/write-build-info.mjs && node_modules/.bin/tsc -b && npm run test:run -- __tests__/pages/SettingsPage.test.tsx && npm run build` (passed).
