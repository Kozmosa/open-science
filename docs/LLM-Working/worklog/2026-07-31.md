2026-07-31 01:54 changelog：启动 WebUI 正式 domain 接口迁移批次；补齐 Project 元数据与生命周期、成员、Workspace 更新/关联/注销、Environment 管理及 Project 环境投影的 `/api/domain` 契约，并将 Project、Workspace、Environment 页面和测试 Mock 迁离对应兼容入口。新增正式写接口覆盖，后端定向测试 14 项通过，前端完整测试 327 项通过；沙箱内全量后端末段出现进程测试假挂起，切换至获批的正常主机环境后完整 `bash scripts/ci.sh l1` 通过（含后端全量、transport drift、前端 lint/327 项测试/生产构建和 docs-site 构建）；关联提交：`cd63f58 feat: complete canonical domain resource APIs`、`cb55e0d refactor: migrate UI to canonical domain APIs`；release staging 验收待后续补记。

2026-07-31 03:10 changelog：完成 WebUI 正式 domain 接口迁移的不可变 release staging 验收；基于 `7f2b41ef78b0` 镜像重建 disposable staging，标准 smoke 通过。通过真实浏览器完成 Project 创建、修改、归档/恢复、成员增删，Workspace 注册、修改、关联、Primary 替换、解绑、注销，Environment 创建、修改、检测、禁用，以及 Timeline、Settings 和文件列表/读取检查；telemetry 记录了全部正式 domain operation，未出现相关 Project/Workspace/Environment compatibility surface 调用。发现 release fixture 的标准 Workspace 路径未预先落盘，且 tenant-home Workspace 经既有 files API 直接读取时产生权限 500；补建 fixture 标准路径后文件列表与读取均为 200。该现象不由本批 domain 迁移引入，作为 release fixture/files 权限后续项保留；未操作 production 容器、日志、HTTP 或数据。
