2026-07-31 01:54 changelog：启动 WebUI 正式 domain 接口迁移批次；补齐 Project 元数据与生命周期、成员、Workspace 更新/关联/注销、Environment 管理及 Project 环境投影的 `/api/domain` 契约，并将 Project、Workspace、Environment 页面和测试 Mock 迁离对应兼容入口。新增正式写接口覆盖，后端定向测试 14 项通过，前端完整测试 327 项通过；沙箱内全量后端末段出现进程测试假挂起，切换至获批的正常主机环境后完整 `bash scripts/ci.sh l1` 通过（含后端全量、transport drift、前端 lint/327 项测试/生产构建和 docs-site 构建）；关联提交：`cd63f58 feat: complete canonical domain resource APIs`、`cb55e0d refactor: migrate UI to canonical domain APIs`；release staging 验收待后续补记。

2026-07-31 03:10 changelog：完成 WebUI 正式 domain 接口迁移的不可变 release staging 验收；基于 `7f2b41ef78b0` 镜像重建 disposable staging，标准 smoke 通过。通过真实浏览器完成 Project 创建、修改、归档/恢复、成员增删，Workspace 注册、修改、关联、Primary 替换、解绑、注销，Environment 创建、修改、检测、禁用，以及 Timeline、Settings 和文件列表/读取检查；telemetry 记录了全部正式 domain operation，未出现相关 Project/Workspace/Environment compatibility surface 调用。发现 release fixture 的标准 Workspace 路径未预先落盘，且 tenant-home Workspace 经既有 files API 直接读取时产生权限 500；补建 fixture 标准路径后文件列表与读取均为 200。该现象不由本批 domain 迁移引入，作为 release fixture/files 权限后续项保留；未操作 production 容器、日志、HTTP 或数据。

2026-07-31 03:25 changelog：处理 PR #64 首轮 CI 反馈；GitHub backend deterministic gate 在测试前因 `src/ainrf/api/routes/domain.py` 缺少 formatter 要求的空行失败，使用锁定的 Ruff 完成纯格式修复。修复后 `ruff check`、全仓 `ruff format --check` 与 `ty check` 通过；沙箱内后端全量进程测试再次出现已知 timeout 型假失败并主动停止，业务逻辑相同的获批主机环境完整 L1 已在本批前序验证通过，交由 GitHub 正常 runner 重新执行完整 gate。

2026-07-31 04:20 changelog：修复 tenant-home Workspace 文件访问权限错误；本地文件列表、读取、下载和上传统一在 `FileBrowserService` 内以对应 Linux tenant 身份执行，上传目录与文件也由 tenant 创建，并把租户身份纳入列表缓存键以避免跨身份复用。新增服务级回归测试覆盖全部四类操作；Ruff、ty 与 files/domain runtime 定向测试通过。关联提交：`fix: run local file access as workspace tenant`。

2026-07-31 04:30 changelog：修复 release staging fixture Workspace 路径未自动落盘；将 init 与 API 共享的 workspace 持久卷挂载点对齐 fixture 根据 state root 推导出的 `/opt/ainrf/state-workspaces`，并在运行镜像中预建该目录、赋予服务用户所有权，保证全新数据卷初始化时可写且创建的标准 Workspace 可被 API 持续看到。新增 release workflow 契约断言；定向测试通过。关联提交：`fix: align release staging workspace volume`。

2026-07-31 04:35 changelog：修复不可变 release 静态资源继承宿主机过窄权限而导致 `/build-info.json` 403；Web 镜像与后端运行镜像在复制前端产物后统一递归补齐全员读取和目录遍历权限，覆盖全部构建产物而非单个文件。新增 Dockerfile 契约断言；定向测试通过。关联提交：`fix: publish readable immutable web assets`。

2026-07-31 13:04 changelog：完成第三批兼容入口删除；在 WebUI 已迁移到正式 domain 入口的基础上，删除 Project、Workspace 和 Environment 资源的旧路由、专用 schema、生成传输契约和过时 Mock；Project 下仍被 UI 使用的 Task 列表、Task edge 和 cost summary 保留。同步更新长期 compatibility inventory 与遥测契约测试，已删除路径回归 404。定向后端测试 25 项、遥测测试 17 项、前端 327 项通过；在正常主机环境完整 `bash scripts/ci.sh l1` 通过，包含后端全量与串行竞态测试、transport drift、前端 lint/测试/生产构建和 docs-site 构建；未操作 production。

2026-07-31 13:30 changelog：完成 Task / Session 正式 HTTP 契约收口与兼容面退役 spec；确认 `/api/tasks` 是唯一 Task Interface，不新建 `/api/domain/tasks` 或正式 Session 资源。设计将 Project relationship / usage 收入正式 domain Interface，将 Runs UI 迁移到 Task / TaskAttempt，统一 create / fork / retry mutation response，并通过 Gate A 删除前验证和 Gate B 删除后验收，最终删除 Session projection、Project Task 兼容路由、Task 旧格式、旧别名及相关遥测债务；同时明确 pause / resume / continue 由已接受的 Conversation Domain spec 接管，不在本轮重新发明临时 Interface。

2026-07-31 15:05 changelog：完成 Task / Project 正式 HTTP Interface 与 caller 迁移的 Gate A 自动门禁；新增 Project task relationship 与 usage summary domain 路由，收紧 Task create/retry 契约并统一 create/fork/retry mutation response。WebUI、MSW、generated transport、性能脚本和产品文档迁移到 canonical Task/Project 路径，管理页从 `/sessions` 收口为 `/runs`，通过 feature Adapter 解包 mutation、映射 relationship，并展示 TaskAttempt 历史和 Project usage。caller scan 不再发现旧 Session/Project Task HTTP 调用；定向后端 11 项、前端 327 项及正常主机环境完整 `bash scripts/ci.sh l1` 全绿。删除前不可变 release staging 人工验收待下一批完成；未操作 production。
