# 2026-08-01 Worklog

2026-08-01 00:12 changelog：架构清理候选不可变 release staging 整体验收发现标准 Workspace 目录仍由 `ainrf` 创建，文件上传以 owner tenant 身份执行时返回权限 500。新增 tenant-owned runtime path provisioning，将 full/large frontend fixture 的可执行 Workspace 在容器内交给对应 Linux tenant，同时保持本地 fixture 直接创建语义；定向 fixture/files/release tests 与完整 `bash scripts/ci.sh l1` 通过。该修复仅作用于可丢弃 release staging fixture，未操作 production。
