# 2026-08-01 Worklog

2026-08-01 00:12 changelog：架构清理候选不可变 release staging 整体验收发现标准 Workspace 目录仍由 `ainrf` 创建，文件上传以 owner tenant 身份执行时返回权限 500；首次补 owner 后又发现 init/API 容器用不稳定用户遍历顺序分配数字 UID，导致同一 volume 在 API 容器中被解释为另一 tenant 所有。新增 tenant-owned runtime path provisioning，fixture 写完全部身份后按 auth DB row order 预建 Linux users，entrypoint 重启恢复也使用相同稳定顺序，再将 full/large fixture 的可执行 Workspace 交给 owner tenant；定向 fixture/files/release tests 与完整 `bash scripts/ci.sh l1` 通过。该修复仅在可丢弃 release staging 验证，未操作 production。

2026-08-01 00:36 changelog：继续执行最终 Task 成功完成验收时发现 container worker 的 Workspace 权限探针使用无效的 `test -rwx <path>`，即使 tenant 具备读写执行权限也恒定返回失败并把任务终止为 `stopped_permission_revoked`。改为在 tenant shell 中分别执行 `test -r`、`test -w`、`test -x`，新增精确命令回归测试；domain worker、fixture、files、release 定向测试及完整 `bash scripts/ci.sh l1` 通过。仅操作隔离 release staging，未操作 production。

2026-08-01 00:55 changelog：完成架构清理最终候选 release staging 的整体人工验收与重启持久性审计。不可变 API/Web 镜像和 manifest 对齐候选提交；登录、用户、设置、Project/Workspace/Environment、文件列表/上传/读取/下载及 tenant ownership、Task 创建/成功执行/详情/Retry/Fork/Move 冲突/Archive/Unarchive、Runs、Timeline、Literature、Skills、Terminal 与已删除 HTTP 入口 404 均通过。12 个真实浏览器页面无 Console/page error、真实 Network failure 或 5xx；API 重启后成功 Task、上传文件和 durable HTTP contract evidence 仍保留，长期 `ainrf_http_contract_*` 正常暴露且无 `ainrf_cleanup_*` / `ainrf_deprecated_*`，隔离 API/Web/init/worker 日志未见相关 schema、权限或未处理异常。后续以最终文档提交的准确 SHA 重建并重复同一验收；全程未操作 production。

2026-08-01 09:30 changelog：完成最新 `origin/master` 的只读、证据驱动全栈规模与架构审计交付，并依据人工裁定更新长期架构事实：动态/惰性/字符串入口均计入依赖，已删除 alias 的证据采用 caller audit、自动验证、隔离手动验收和用户批准，`/v1/models` 与 `/v1/messages` 明确为外部模型协议 Interface，当前 committed authority 仍为 Task/Attempt，Conversation Domain 尚未完成产品 cutover。新增 HTTP Adapter 依赖方向、Conversation Domain cutover 闭合、Literature transport contract 三份 active Spec，并将自包含单页 HTML 审计报告纳入 `docs/projects/`。验证包括 Spec frontmatter/链接、HTML source hash 与自包含性、`git diff --check` 和 docs-site production build；未操作 production。提交：`c866338 docs: align project basis with architecture cleanup`、`8d8fcb6 docs: add architecture audit follow-up specs`。

2026-08-01 12:56 CST changelog：完成 Conversation Domain closure 启动与主线同步批次；完整审阅最高 authority、closure Spec、三个依赖 Spec、架构与文档治理、deep Module 设计规则及近期 worklog，在既有 `feat/conversation-domain-v3` worktree 内无冲突合并最新 `origin/master`，保留原 6 个 Conversation commits。主线架构清理已将共享 SQLite repository 收为私有实现，修正 Conversation application Module 对已删除公开名称的依赖，新增 committed-v2 readiness fuse 及授权后的 Task/Turn/Item 正式读取 Interface，未恢复 `agentic_researcher` Task CRUD facade。验证：contracts、persistence、execution repository、正式 Conversation Interface 与数据库 migration 定向测试全部通过，Ruff、Ty 与 `git diff --check` 通过；未操作 production。关联提交：本提交 `refactor: align conversation module interface`。

2026-08-01 13:10 CST changelog：完整实现 HTTP Adapter 依赖方向闭合批次，将通用 CLI composition 收敛到 `ainrf.command` Module，由 `ainrf.api.cli` Adapter 组合并注册 `serve` / `stop`，将 FastAPI construction、uvicorn/reload、daemon 与 health lifecycle 全部收回 `ainrf.api.server`；删除旧 `ainrf.cli` / `ainrf.server` 反向知识链，开发栈改经稳定 CLI Interface 启动 reload，并保持 `openscience`、`ainrf` 与 `python -m ainrf` 用户行为。新增具有长期 owner 的 dependency-direction Module 与 L0/L1 guard，覆盖静态、惰性、动态、拼接字符串入口和 product import cycle，不引入 pass-through facade、单实现虚假 Seam 或 production 操作；同步提升 architecture contract、更新 active inventory并归档已实现 Spec。定向 CLI/server/dev-stack/guard 回归 88 项通过，L0 全绿，完整 `bash scripts/ci.sh l1` 通过（dependency guard、Ruff、format、Ty、后端并行与串行 race lanes、transport drift、frontend lint/60 files/330 tests/production build、docs-site build）；关联提交：本提交 `refactor: close HTTP adapter dependency direction`。

2026-08-01 13:22 changelog：完成 Literature transport contract 收口。为 accepted Literature 产品契约建立 19 个正式 Pydantic/OpenAPI operations，补齐 versions、分页 envelope、Summary `stale`、`summary_status` 与 `has_research_task`，并通过 presenter Adapter 将 transport schema、application result 与 SQLite persistence row 分隔；frontend 删除 shared 手写 transport payload，改为 generated transport → feature Adapter → view model，MSW 使用 generated request types 与正式 operation binding。7 个 legacy routes 已完成 repo caller audit并收敛到正式 topic/check/state application Interface，因缺少独立删除批准而 fail-closed 保留，未操作 production。定向 backend 40 项、frontend 60 files/330 tests、production build、OpenAPI drift、Ruff、Ty、docs build与完整 `bash scripts/ci.sh l1` 全绿；Spec 已归档并提升当前 inventory 到 reference/architecture 文档。关联提交：`8285aed feat: formalize literature transport contract`。

2026-08-01 13:06 CST changelog：完成 Conversation worker 私有 execution Seam 与首个 runtime Adapter 批次；新增仅供 worker 使用的 submission claim、delivery、RuntimeExecution 建立、canonical Item 追加和 control 读取 Interface，以 SQLite CAS 保证并发 claim 单赢家。新增统一 capability matrix，Codex Adapter 通过原生 `turn/steer` 的 `expectedTurnId` causal guard 和原生 interrupt，不让 one-shot Agent SDK 或 Claude CLI 冒充 same-Turn steer；普通 application/HTTP caller 不获得 execution Interface。验证：并发 claim/执行/Item 定向测试、runtime Adapter 与既有 harness 测试全部通过，Ruff 与 Ty 通过；未操作 production。关联提交：本提交 `feat: add conversation execution seam`。

2026-08-01 13:45 CST changelog：完成 Conversation runtime/worker/HTTP/frontend cutover 主切片。正式 `domain-worker` 在保留 Overview/Literature planner Leverage 与 maintenance fuse 的同时改为 claim TurnSubmission，不再从普通产品路径 claim Attempt；worker admission 恢复 Project/Workspace/Environment、grant、tenant Linux user、MCP 与 Workspace 权限检查，Turn 与 RuntimeExecution 终态在同一 SQLite transaction 内提交。Agent SDK native resume 失败不再静默 fresh-start，共同 Harness Interface 删除 pause/resume/send_input，旧控制只留在隔离 legacy Adapter 路径。HTTP 删除 Attempt、pause/resume/continue、output/messages/SSE 与 Task-level retry，新增 Task create→Submission、Turn/Item、steer/interrupt/Turn retry Interface；frontend history、send、interrupt、retry 与 polling 改走 canonical transport，MSW/generated transport 同步。验证：Ruff、Ty、Conversation execution/worker/application、CLI、完整 API lane、frontend lint/build 通过；首次 L1 backend gate仅发现新并发测试缺少 marker，修正后重新进入 L1。未操作 production。

2026-08-01 13:58 CST changelog：完成 ordinary Task/Project projection authority cutover 切片。新增只读 Conversation projection Module，将 Task 状态、Turn 时间边界、Item 序号与 usage、RuntimeExecution liveness 集中在一个深 Interface 后；Task list/detail/health/token usage 和 Project usage 对 `conversation_v3` authority 只读 Task/Turn/Item/Submission/Execution，明确 legacy Task 才使用 Attempt Adapter，兼容 transport 不暴露 runtime identity。新增正式 application 流程回归，证明无 Attempt 行时 running、health 与 usage 均由 canonical facts 得出。验证：定向 Conversation Interface test、API lane、Ruff 与 Ty 通过；未操作 production。

2026-08-01 14:08 CST changelog：完成 Conversation v3 standalone migration Module 与 CLI Interface。新增 `openscience migration conversation-v3 inspect/dry-run/execute/verify/cutover`，显式绑定不可变 source SHA/row manifest，只写独立 shadow generation，active/paused/launch-unknown legacy runtime 阻断 execute/cutover；Attempt 的 resume recovery 不机械生成用户 Turn，推断边界与 confidence/provenance 写入 canonical records。旧 raw Task credential 仅写 owner-only CredentialStore staging，shadow normal table、report 与 pointer 均清除 secret；verify 检查完整性、authority 覆盖、secret 与 source hash，cutover 通过原子 pointer replace 完成。验证：迁移 fixture、active blocker、deterministic ID、secret forensic absence、CLI help/定向 CLI tests、Ruff 与 Ty 通过；未操作 production。

2026-08-01 14:27 CST changelog：完成 Conversation application ownership 与 frontend compatibility closure 切片。Task cancel/archive/unarchive/title 进入正式 Conversation Interface；cancel 在同一事务更新 work status 并为 active RuntimeExecution 建立 causal interrupt request，archive 不再隐藏 active Turn。Approval HTTP 只传用户 decision，runtime generation/tool ref 在 application Module 内验证；新增 fork preview/confirm HTTP Interface，旧 one-shot fork 作为兼容 Adapter 映射到原子 Conversation Task+Submission，不再调用 Attempt writer。Runs、Inspector、Task drawer、generated transport 与用户 copy 改为 Turn/Item，删除未使用 Timeline Attempt view、旧 `/attempts` feature caller、全部 EventSource scaffolding 和 8 个 obsolete SSE skips，保留 canonical Item polling tests。新增 non-HTTP Module 不得 import HTTP Adapter 的依赖方向测试。验证：Conversation/application/API/dependency 定向测试、frontend 319 tests（0 skipped）、lint、generated transport drift 与 production build 通过；未操作 production。

2026-08-01 15:05 CST changelog：完成 canonical synthetic acceptance 与 worker recovery Locality 切片。full frontend fixture 在保留 legacy rows 作为 migration/compatibility evidence 的同时，为每个普通 Task 建立 `conversation_v3` authority、Task state、Submission、Turn、Item 与 RuntimeExecution；fixture worker 改走正式 Conversation execution Interface，不再通过 TaskDispatcher/Attempt writer 执行用户工作，并继续复用独立 Overview/Literature Module。worker Seam 新增 stale claimed CAS reclaim、stale delivering/无 provider acceptance 收敛为 `delivery_unknown` 且禁止 replay 的显式策略，回归证明 crash recovery 不创建额外用户 Turn。Task move 也进入 Conversation application Interface，HTTP 不再调用旧 Task CRUD facade；Vite `/api` proxy 修复为保留 canonical prefix，公开 architecture/Runs/Timeline 文档改用 Module、Interface、Depth、Seam、Adapter、Leverage、Locality 与 Turn/Item contract。验证：fixture/worker 定向测试曾通过，Ruff、format、Ty 与 `git diff --check` 通过；完整 L1 第二次运行在 backend 16% 后因共享主机 `/tmp` 2.5GB pytest 临时写入导致 runner 卡于 `balance_dirty_pages`，清理明确测试产物后仍观察到主机 I/O stall，故该次 L1 记为未完成而非通过；未操作 production。关联提交：本提交 `feat: close conversation runtime recovery`。

2026-08-01 21:20 CST changelog：完成 Fork materialization 与 compatibility Adapter 深化切片。Fork preview 现在固定目标 Project、Workspace、engine family/driver 与标题；confirm 验证当前 transcript revision 和显式 truncation/full-transcript confirmation 后，在 Conversation Module 单一事务内创建目标 Task、context snapshot、`conversation_v3` authority、queued Submission、fork relationship，并把 transfer receipt 收敛为 `transferred`，全程不创建 Attempt。删除 HTTP composition 中独立暴露但无 caller 的 Attempt projection state，MSW one-shot Fork 改为 Task+Submission response 并删除 mock Attempt state，generated transport 同步。验证：Fork/persistence/migration/application/API transport 定向测试 59/59，通过 Ruff、Ty、transport drift；frontend lint 与 319/319 tests（0 skipped）通过；migration/concurrency/recovery 定向套件 16/16 通过。隔离 full dev smoke 通过，真实 HTTP 验收覆盖 Task create→completed Turn→3 canonical Items、Retry 新建第二 Turn 且记录 `retry_of_turn_id`、stack restart 后两 Turn 持久存在。默认及降 worker L1 的 backend lane 在共享 `/data` 98% 占用下反复出现与改动无关的 files-route/fixture 等 60 秒 I/O timeout；相同 last-failed tests 使用 120 秒串行均通过，因此当前 L1 仍如实记为未完成，不能宣称门禁通过。未操作 production。
2026-08-01 21:33 CST changelog：完成 Conversation Domain cutover closure 最终工程门禁与主线同步切片。谨慎合并最新 `origin/master`，保留 Conversation Module 全部既有工作并吸收 HTTP Adapter 依赖方向与 Literature transport contract；generated transport 重新生成后保持零 drift。主线新增第 32 个 reconciliation migration 后，将 schema contract 断言从 31 对齐到 32；定向 schema test 通过。完整 `bash scripts/ci.sh l1` 以退出码 0 通过，覆盖 dependency-direction guard、Ruff/format、Ty、后端并行与串行 race lanes、generated transport、frontend lint/330 tests/production build及 docs-site build；`git diff --check` 通过。最终审计确认正式 Conversation Interface 承担 Task/Turn/Item/Submission/RuntimeExecution/Binding 行为，HTTP/frontend/common Harness Interface 不暴露 Conversation pause/resume，legacy Attempt/RuntimeSession 仅保留在显式 legacy-authority、migration/admin compatibility Adapter 与非 Conversation 语义中。既有隔离 full dev 人工验收已覆盖 canonical Items、Retry causal link 与 stack restart 持久性；未操作 production。Spec 归档仍等待人工更新最高 authority `PROJECT_BASIS.md` 中“Task/Attempt 为 committed authority”的耐久事实，agent 未修改该文件。
