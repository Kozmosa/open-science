2026-07-02 19:42 changelog：完成 OpenScience 兼容式品牌迁移前置规划；从最新 master 创建 `feat/openscience-brand-alias` worktree，验证当前 `ainrf --help` 基线可运行，并写入实施计划 `docs/superpowers/plans/2026-07-02-openscience-brand-alias.md`；验证：`git fetch origin` 与 `git pull --ff-only origin master` 均 up-to-date，`UV_CACHE_DIR=/tmp/uv-cache uv run ainrf --help` 成功，计划占位符复查无 `TBD`/`TODO`/尖括号占位符残留；关联提交：暂无。

## 2026-07-02 20:07 — Task 5: active docs/scripts/governance OpenScience rebrand

- Updated active README, PROJECT_BASIS, AGENTS, src/frontend READMEs, scripts, docs-site metadata/content, and deploy examples/comments from AINRF-facing wording to OpenScience.
- Preserved compatibility contracts for `src/ainrf`, `.ainrf`, `AINRF_*`, metrics, service names, tenant users, and legacy `ainrf` CLI where they remain runtime/API surface.
- Updated `scripts/webui.sh` to prefer `OPENSCIENCE_WEBUI_API_KEY` / `OPENSCIENCE_API_KEY_HASHES`, export legacy `AINRF_*` compatibility variables, and invoke `openscience serve`.
- Validation: `UV_CACHE_DIR=/tmp/uv-cache uv run openscience --help` passed; `UV_CACHE_DIR=/tmp/uv-cache uv run ainrf --help` passed; `npm --prefix docs-site run build` passed after installing docs-site dependencies; active-surface scan found no standalone old brand strings outside preserved runtime contracts.
