---
title: 快速开始
description: 安装依赖、初始化配置、首次启动 OpenScience，5 分钟上手。
---

## 前置依赖

| 依赖 | 版本要求 |
|------|---------|
| Python | 3.13+ |
| Node.js | 22+ |
| uv | `pip install uv` 或 `curl -LsSf https://astral.sh/uv/install.sh \| sh` |

## 初始化

首次使用必须执行 onboard，创建本地状态与配置：

```bash
uv run openscience onboard
```

## 启动服务

推荐使用 `scripts/webui.sh` 联合启动前后端：

```bash
scripts/webui.sh        # 生产预览模式（端口 4173）
scripts/webui.sh dev    # 开发模式 Vite HMR（端口 5173）
```

或单独启动后端：

```bash
uv run openscience serve
```

## 默认账户

首次启动时自动创建管理员账户：

- **用户名**：`admin`
- **密码**：`admin`
- 首次登录后提示修改密码

## 访问地址

| 服务 | 地址 |
|------|------|
| WebUI 开发 | `http://localhost:5173` |
| WebUI 预览 | `http://localhost:4173` |
| API | `http://localhost:8000` |
| API 文档 | `http://localhost:8000/docs` |

浏览器通过前端同源代理访问 `/api`、`/code` 与 `/terminal`，无需手动管理 API key。

## 后续步骤

- [CLI 参考](/scholar-agent/cli/) — CLI 命令详解
- [WebUI 总览](/scholar-agent/webui/) — WebUI 页面导航
- [认证与授权](/scholar-agent/auth/) — 登录与用户管理
