# ── OpenScience Secrets ─────────────────────────────────────────────
#
# ** DO NOT apply this file with real secrets. **
# Use kubectl create secret instead:
#
#   kubectl create secret generic ainrf-secrets \
#     --namespace ainrf \
#     --from-literal=JWT_SECRET=$(python3 -c "import secrets; print(secrets.token_urlsafe(48))") \
#     --from-literal=API_KEY_HASHES=$(python3 -c "from hashlib import sha256; print(sha256(b'YOUR_API_KEY').hexdigest())") \
#     --from-literal=ANTHROPIC_API_KEY=sk-ant-your-claude-key \
#     --from-literal=CODEX_API_KEY=sk-your-openai-key
#
# This manifest is intentionally blank and will fail validation
# if applied without replacing the values below.
# ──────────────────────────────────────────────────────────────
apiVersion: v1
kind: Secret
metadata:
  name: ainrf-secrets
  namespace: ainrf
type: Opaque
# No data — secrets must be created via kubectl create secret or
# an external secrets manager. This file is a schema reference only.
data: {}